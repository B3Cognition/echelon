# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-memory-reconciliation.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / common classification | Disk and captured adapters share lifecycle, metadata preconditions, ordering and report shape. | Extract one private loop/checker boundary; preserve public disk signature and native exception propagation. No duplicate loop or public callback framework. |
| 1 / canonical path | Legacy uses physical resolution and existing three-digit spec-directory policy; captured input has no physical root. | New adapter accepts only normalized project-relative metadata paths and shares the native canonical prefix rule. Legacy aliases/symlinks remain native. Element ID width remains unrelated. |
| 1 / capture completeness | No key does not demonstrate captured absence. | Distinguish artifact_unobserved, explicit None artifact_missing and present bytes (including empty); no disk fallback or supplied-hash trust. |
| 1 / mutable inputs | Existing report retains original drawers; byte-table membership could change during iteration. | Validate/copy the whole exact table before iteration, preserve original accepted drawer objects deliberately, make no deep-detachment claim. |
| 1 / errors | Shared extraction must not change legacy fault propagation. | Bound ordinary errors only at new public captured entry, outside handler; BaseException propagates; malformed metadata paths are rejection results rather than table validation errors. |
| 1 / scope | Current audit rereads canonical/support files and invokes live reconciliation. | This implements only the missing pure reconciliation dependency. Actual collection acquisition, audit report assembly, lifecycle authority and managed publication are not claimed. |
| 1 / test contract | Initial missing API RED must be grounded in native behavior. | Actual native old/candidate plans and legacy old-disk acceptance run before new API call. Later complete parity, no-I/O tripwires, absent/empty and bounded errors; four covering modules once. |
| 1 / files and authority | Three scoped changed/new files; existing audit and graph callers remain unchanged. | No state, schema, wire, provider, controller or activation changes. Root retains administrative plan/brief/progress and dispatches independent review. |

Root read the approved full design, current context_reconciliation implementation and all its tests, native artifact_hash, audit_spec_memory's full classification path, native planner test usage and captured graph byte-table boundary. Previous negative execution gate completed at17db03cd, with758 covering tests and clean independent review; no re-dispatch or unchanged rerun.

Ruling: 34. In the new inactive captured-memory reconciliation API, require normalized project-relative logical paths, distinguish omitted images as artifact_unobserved from explicitly captured None as artifact_missing, and retain present empty bytes as content; preserve existing disk alias/symlink semantics and canonical spec-directory policy unchanged — a supplied byte table cannot infer physical aliases or claim absence outside its observed membership, and stale disk fallback would defeat candidate-bound validation — if wrong, revise the inactive adapter/path/rejection contract and parity tests or supply another explicit captured-source association; no existing IDs, revisions, graph keys, native disk audit wire, receipts or canonical artifacts need rewriting, and no live consumer has changed. This is content reconciliation only, not provenance, identity revision or publication authority.

Self-review: one cohesive task with one shared private loop and one public inactive adapter; all interfaces are existing except the precisely specified new function. Complete validation and legacy-compatible classification orders are explicit. Table keys may contain unrelated project-relative artifacts; canonical eligibility applies to drawers. No inferred completeness, stale disk fallback, new memory gate policy or activation. Placeholder scan found no matches. Main/stopped smoke remain out of scope. Root will retain plan/brief/preflight before dispatch and record BASE.

Task1 dispatched fresh /root/captured_memory_reconciliation Sol/high at BASE91eed06ad6d8dc2a4be473a96873090a4f1ec44d with exact53-line brief and no-subagents contract. Root retains administrative files excluded/dirty; first native-grounded absent-API RED notification and four-module once-only covering required. Ruling34 selected, implementation/review outstanding. No subsequent protocol selected.

Worker notified first RED before production: exact parent pytest/workdir test_context_reconciliation_captured.py::test_captured_reconciliation_uses_candidate_bytes_not_published_disk -q failed1. Native old/candidate planner construction and disk acceptance/rejection assertions passed first; expected AttributeError for absent reconcile_captured_drawers followed. No fixture correction. Worker proceeding to scoped shared loop and captured checker.

Worker first GREEN: exact same focused command passed1 in0.50s after shared classification loop/captured adapter implementation, no fixture correction. Expanding parity, complete-table validation, purity, bounded errors and legacy controls before self-review/four-module covering once. No additional scope or blocker reported.

Worker focused expansion green37pass0.57s, covering native parity/report order, lifecycle filters, absent/unobserved/empty images, exact table shape, disk alias/symlink compatibility, strict captured paths, native spec-prefix regex, wide IDs, no-I/O/table-copy behavior, bounded ordinary errors, process-control propagation and unchanged native read errors. Explicit empty-table/excluded-validation assertions, self-review and four-module once-only covering remain. No additional architecture selected.

Worker DONE implementation4549c1288a976697a3d3b2cb19ed48b9da0f64c5, report-onlyf08c1a9ae9464eebdb9f4f5311586d7c3cd53c52. Root read complete105-line report; firstRED1fail0.53s, firstGREEN1pass0.50s, focused37pass0.57s, after explicit empty/excluded tests41pass0.57s, after terminal-coverage amendment41pass0.56s. Exact once-only four-module covering331pass1.30s pristine on staged implementation tree6d21e8b315de2de2f376e1e528f189b0a23f624b; root directly verified commit tree and later report-only diff. No production/test/storage-doc amendment after covering and no unchanged rerun. Fresh Sol/high /root/review_captured_memory_reconciliation dispatched against complete41,866-byte original-BASE91eed06a..f08c1a9a package, exact brief and full report/constraints. Review pending; root administrative progress remains excluded/dirty.

Task 1: complete (commits 91eed06a..f08c1a9a, review clean)

Fresh /root/review_captured_memory_reconciliation returned spec compliant and quality Approved with no Critical, Important or Minor findings. Complete package review confirmed one shared native loop, exact pre-iteration image-table validation/copy, byte-hash reconciliation, unchanged disk path/exception order and exact native spec-prefix regex, and accurate inactive-boundary documentation. Named-risk unchanged-code checks covered _source_path plus its pure normalization delegate and the existing native reconciliation tests. Cannot Verify temporal RED notification/historical test execution/outside-worktree absence is resolved by root's witnessed firstRED notification, complete105-line report, directly verified tested implementation tree and report-only diff, and this task's scoped tool/commit record. No tests rerun. Ruling34 now implemented and independently approved. Root checkpoint modifies only administrative records; next integration protocol remains unselected.

## Subsequent integration observations (read-only, not selected protocols)

Canonical memory snapshots live in mempalace_requirements.py, not spec_memory.py. CanonicalSpecSnapshot carries spec_id, Path spec_dir/spec_file, bytes, digest, logical source and metadata. load_canonical_spec_snapshot reads content and then artifact_hash rereads the file; supporting loader also reads twice after iterating the live directory and selects SUPPORTING_MEMORY_ARTIFACTS. Captured snapshot assembly must bind both content and metadata to the same bytes and retain explicit support membership rather than accepting an arbitrary incomplete list as completeness proof. Existing Path behavior has not been changed by this task.

audit_spec_memory uses native adapter planning, exact expected-ID fetch, live reconciliation and _scan_spec_extras. The latter reads at most MAX_AUDIT_SCAN_ROWS=1000 and can return a warning when bounded scan is unsupported; it is not a demonstrated complete managed collection observation. Existing scan_wing_rows_complete separately supports bounded pagination, probes short pages, verifies wing/duplicates/shapes, scans twice and compares order/content; its default maximum is also1000 but it explicitly rejects overflow rather than silently accepting a truncated view. Two equal scans are not a long-lived storage lease. No new scan limit, captured audit API, report policy, adapter injection or publication ordering has been selected here. Do not use a supplied pass status to evade these acquisition requirements.

Other graph memory domains use mempalace_spec_evidence.py and mempalace_re.py plus the shared mempalace_memory_audit.audit_artifact_memory. They already receive snapshot lists internally, unlike canonical audit's direct source reload; complete source selection/acquisition still needs explicit treatment. Root read-only discovery initially guessed absent spec_memory.py/spec_evidence_memory.py/re_memory.py filenames and got diagnostic exits, then corrected with rg --files/import inspection; these are not test runs or test failures.

Further call-site inspection: scan_wing_rows_complete is currently used by mempalace_retarget's purge/verification paths, not native spec audit, so changing its semantics would affect another owner. Its two reads retain parsed metadata objects without a general detachment claim. The new reconciliation phase does not change it. Graph audit maps native warn to warning and required unavailable to error; no strict managed completeness claim can be inferred from native warning acceptance.

Important ordering correction for future design: direct search found no build_spec_graph/audit_spec_graph/write_spec_graph calls in squad.py or squad_*.py. Existing graph building/auditing is in graph_read, CLI app, retarget graph/finalization and workspace graph refresh/assembly. Squad's _mine_published_context_after_publication invokes best-effort native mining after external publication clearance. Do not invent a current mandatory Phase A graph-audit gate or claim the existing phase already supplies a managed sealed graph. Truthful pre-mining memory observations can differ after actual mining, and a later managed graph-only refresh would itself need source-head/journal/completion ownership. No refresh ordering, additional completion state, gate change or other graph writer exclusion protocol has been selected in this read-only investigation.
