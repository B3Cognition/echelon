### Task 1: acquire canonical memory audit from a captured candidate tree

**Files:** Create `src/echelon/mempalace_captured_audit.py` and `tests/unit/test_mempalace_captured_audit.py`; modify `src/echelon/mempalace_audit.py` only to share existing private planning/classification, and `src/echelon/mempalace_requirements.py` only to share native snapshot construction/metadata literals; document in `docs/element-identity-storage.md`. No existing tests, scanner public behavior, graph module or other production files may change without root escalation. Root owns plan/brief/progress. No subagents.

**Public API:** Add only this opt-in function in the new module:

```python
def audit_captured_spec_memory(
    project_root: Path,
    *,
    spec_id: str,
    tree: ProjectTreeSnapshot,
    maximum_scan_rows: int,
    probe_retrieval: bool = False,
) -> SpecMemoryAuditReport:
    # Validate/detach one complete selected tree before adapter acquisition.
    # Build canonical logical snapshots from its exact bytes.
    # Plan through the native adapter and read existing collection only.
    # Require coherent expected-ID and complete bounded wing observations.
    # Share native reconciliation/classification/report assembly.
```

`maximum_scan_rows` is a required positive exact int operational read budget, passed to the existing complete scanner; it is not derived from an ID and does not cap numeric labels or allocation capacity. `probe_retrieval` must be exact bool; preserve the native report's skipped versus warn/checked=0 probe semantics (no new retrieval implementation). `project_root` must be a Path with an absolute path; do not resolve it or use it to reread the spec. It locates the existing adapter configuration and supplies the canonical logical report display paths. Source/config provenance binding remains the later owner’s responsibility.

**Captured source and native snapshots:** Use existing `_validate_scope(spec_id, "phase_a")` from spec_graph_structure for the established exact spec component contract, and `snapshot_source_manifest(trees=(tree,), files=())` to validate the full original tree, including ignored binaries/directories/modes/hashes. Do not render a graph. This is analogous to build_spec_graph_structure: the caller explicitly selects the physical tree; it can be canonical or run-local (or another explicitly selected normalized staging root), and this function does not authenticate that selection. Convert each file's relative suffix under the validated physical root to `specs/<spec_id>/<suffix>` in one detached byte lookup. Do not mutate/rebase the original tree or create a new manifest claiming canonical physical provenance. Copy/detach tree-derived data before invoking adapter/backend code; immutable bytes may be reused.

Require the selected tree to exist and contain a regular file `spec.md`. A missing/empty tree or a directory named spec.md is invalid input, not a successful zero-row audit. Present empty spec bytes are passed to native planning and retain native outcomes. Derive the main snapshot plus every present direct-child file named by existing `SUPPORTING_MEMORY_ARTIFACTS`, in the same filename order as the disk loader; nested similarly named files are not supports. Validate ignored original entries before filtering. Logical metadata always uses canonical spec paths, not the physical run-local path. Build every content hash from those same exact bytes, never from disk or a caller-supplied digest. Share private native snapshot/metadata construction in mempalace_requirements so canonical/support scope, artifact_kind, path, hash, source_file, lifecycle_status, provenance_type and added_by literals do not drift. Keep legacy load ordering and behavior intact, including its existing calls to artifact_hash; fixing old double-read acquisition is outside this task. No new public snapshot carrier/factory is needed.

Validate/detach all new API input shape/source data before any factory, planner, collection or unrelated configuration read. Ordinary invalid input must raise `SpecMemoryError("invalid captured spec memory input")` after leaving the exception handler, with neither cause nor context or source-bearing traceback; process-control BaseException propagates. Use the existing snapshot carrier rather than trusting arbitrary caller snapshots or an incomplete caller support list. Complete supplied-tree membership is an observation claim, not an acquired physical lease.

**Shared native audit transformation:** Preserve existing `audit_spec_memory` and cleanup behavior. Factor the exact existing expected-row classification/report body and the extras classification into focused private helpers, then call those helpers from both native and captured entry points. Do not copy the approximately190-line audit body or introduce another audit schema/policy engine. Private callbacks for reconciliation/extras are acceptable solely to retain legacy acquisition/error ordering; keep signatures and responsibilities concrete. The shared body must preserve missing/stale/wrong_wing/wrong_room/non_canonical/lifecycle_excluded/duplicate/duplicate_canonical/historical, exact expected and present counts, malformed-row handling, sorted native report collections, errors/recommendations and `_status_for_failures`. Preserve existing monkeypatch seams for audit_spec_memory's module-level reconcile_drawers and adapter factory. Existing `_scan_spec_extras` retains its bounded get/TypeError recommendation; factor its already-parsed row classification for the new full observation. Existing cleanup continues to use the same private native planning call. A private optional supplied-support tuple can let the shared planning helper avoid live support loads for the captured caller; default/native call order stays unchanged.

Captured reconciliation calls the reviewed `reconcile_captured_drawers` against the detached logical byte table, never reconcile_drawers. The new API must return actual native fail/warn/unavailable/pass outcomes from observed storage and shared classifications; no supplied status label or synthetic prospective success. Ordinary planner/reconciliation failures produce the corresponding native fail report with bounded error class/recommendation. Ordinary adapter/open/get/complete-scan/cohort failures produce a native unavailable report; expected_count is0 before planning and len(expected_rows) after planning. SystemExit at operational adapter/planner/backend seams follows the existing audit's bounded report convention; KeyboardInterrupt and other uncaught process-control BaseException propagate. Do not expose exception messages or chained source data in reports. Invalid request validation remains the separate bounded raising behavior above. No acquisition failure may silently fall back to disk audit or creating access.

**Read-only coherent acquisition:** Create the existing requirement adapter with run_id `audit`, plan the captured main/support bytes through its native methods, and obtain the collection through the existing `_collection_from_adapter` read-only entry. No direct backend/global collection construction. Acquire expected IDs using the native documents/metadatas get (or exact empty shape when there are no expected IDs), parse through `_as_collection_rows`, and obtain all wing rows via existing `scan_wing_rows_complete(..., wing=adapter.wing, maximum_rows=maximum_scan_rows)` with its existing page size and pagination/order checks. Do not change that scanner's public API, defaults or retarget callers.

Detach each raw get response before another backend call can mutate it. A small private get-only wrapper around the read-only collection, forwarding get and returning `copy.deepcopy(raw_response)`, may serve both expected fetch and the existing complete scanner. It is not a public abstraction and exposes no writes. Ordinary copy failures are unavailable observations. This avoids changing the shared scanner while preventing backend-owned nested metadata from mutating both supposedly independent scans together. No immutable-storage-lease claim follows from two equal scans.

Reject unexpected IDs returned by an expected-ID request, including malformed-row IDs outside that exact requested set. Let E be parsed valid expected-response rows whose metadata wing equals the selected native adapter wing, and let W be the complete scan restricted to expected IDs. Require E == W before classifying; this catches removal/addition/content or metadata drift between the two reads. A legitimately wrong-wing exact row is excluded from E and must still be classified by the shared native wrong_wing check; it does not become a guessed current row. Scanner rejection of malformed, incomplete, unsupported, truncated, duplicate, wrong-wing, overflowing or changing pages yields unavailable. Use the complete scan's parsed/detached rows for shared extras classification instead of issuing a new bounded extras query. The successful observation is bounded to this read interval and source image; it is not a storage lock or publication permission.

**First RED with native planning and real captured source projection:** Construct a real resolved temporary project with config and canonical old spec plus a direct support file. Use the existing publication transaction/source inspection/projection fixtures to obtain an exact proposed tree containing changed spec/support bytes without promoting them. Build native expected rows through a real RequirementMemoryAdapter/SpecMemoryMiner. Replace only the collection/writer storage boundary with a deterministic simulated collection that implements get and whose mutating/creating entry points raise. Do not replace planners, classifiers, reconciliation, source validation or the API under test. Keep MemPalaceContext's palace path in the temporary fixture (e.g. narrow fixture patch of its path lookup); do not open actual global memory.

Use matching candidate rows in the simulated collection. First call the existing disk audit and assert it cannot certify the old on-disk spec against those candidate rows. Then call the new missing API and expect a native pass with all main/support rows counted. Assert disk bytes remain old and all backend calls are read-only. This absent-API RED must occur only after real source/planner/native-audit setup succeeds. Name: `test_captured_audit_reads_candidate_tree_and_actual_collection`.

```python
legacy = mempalace_audit.audit_spec_memory(root, spec_dir)
assert legacy.status == "fail"
assert legacy.present_current_count == 0
report = captured.audit_captured_spec_memory(
    root, spec_id=spec_id, tree=projected_tree, maximum_scan_rows=2000)
assert report.status == "pass"
assert report.expected_count == len(candidate_rows)
assert report.present_current_count == len(candidate_rows)
assert report.missing == report.stale == report.non_canonical == []
assert spec_file.read_bytes() == old_spec_bytes
assert support_file.read_bytes() == old_support_bytes
assert collection.writes == []
```

- [ ] Read scoped native loaders/audit and current complete scanner/captured reconciliation; read the existing native adapter/miner fixture patterns. Run the first test before production edits using `/Users/michalbachorik/work/echelon_r/echelon/.venv/bin/pytest tests/unit/test_mempalace_captured_audit.py::test_captured_audit_reads_candidate_tree_and_actual_collection -q` from this worktree. Notify root of actual RED and any fixture corrections before production work.
- [ ] Implement shared private snapshot/planning/classification plus the new captured acquisition adapter, then rerun the first test to GREEN. Escalate if preserving native behavior requires additional files or a public scanner/graph/adapter change; no hidden scope expansion.
- [ ] Exercise native/captured complete report parity on an unchanged canonical tree with actual native plans: all expected present, missing, stale document/hash, wrong wing/room/path/scope/canonical flag/requirement ID/schema, terminal lifecycle, support artifact_kind, historical and active duplicate rows, ignored evidence-domain extras, probe false/true, empty native plan if supported. Compare full reports where observations/policy are equivalent, not only status. Preserve inherited classification quirks; do not redesign native audit policy in this extraction.
- [ ] Exercise run-local/staged physical trees producing the same canonical logical row keys/report metadata, absent versus empty spec, all selected direct support filenames and nested/nonselected artifacts, changed support bytes, full-tree validation of ignored damaged binary/hash/mode/membership, and original input detachment. A native or captured report from another spec/wing cannot be supplied in place of actual acquisition.
- [ ] Prove the strict acquisition difference with more than1000 rows and a relevant active duplicate beyond the old bounded extras window: the old native control demonstrates its bounded result, while the new API with adequate operational budget sees/classifies the extra; with too small a budget it returns unavailable, never pass/warn. This is a bounded read test, not a million-record capacity benchmark. Include exact-budget/empty/paged controls, unsupported pagination, short/truncated pages, excess pages, duplicate IDs, wrong wing, changed order/rows between scans and deep nested metadata mutation of reused backend objects.
- [ ] Test expected-fetch/full-scan coherence: an expected row added/removed/changed between reads yields unavailable; expected get returning an unrequested ID yields unavailable; wrong-wing expected rows still produce native wrong-wing failure when observations otherwise agree. Ensure extras use the acquired complete rows without another bounded get. Validate zero expected IDs uses the explicit empty response but still scans extras.
- [ ] Prove no source rereads: after tree preparation, replace native disk loaders/reconcile_drawers/creating or mutation seams with tripwires; allow only the existing config read and simulated read-only collection. Change/delete source files and retain the captured result. Validate malformed input before an adapter-acquisition tripwire, exact required budget/bool/root/spec types, complete source error bounding, operational errors/SystemExit reporting and KeyboardInterrupt propagation. Original source trees and backend collection state remain unchanged except explicit test-fixture mutation used to exercise drift.
- [ ] Compose a real returned report with the existing captured graph memory contribution in one test using native plans/source bytes and the existing GraphMemoryAudit origin conversion values. Assert an actual missing/stale/unavailable observation stays so in the graph; no fabricated pass and no current-revision/semantic claim. No production graph changes or full managed completion claim.
- [ ] Document the actual read-only acquisition, complete bounded scan/cohort check, native classification reuse, explicit physical-tree-to-logical-source claim, root/config and table provenance limitations, operational read budget versus unbounded ID labels, and still-missing lifecycle/publication/completion ownership. No Phase A graph-audit gate is added or claimed.
- [ ] Self-review then run once the exact covering set with the absolute pytest executable above: `tests/unit/test_mempalace_captured_audit.py tests/unit/test_mempalace_audit.py tests/unit/test_mempalace_requirements.py tests/unit/test_context_reconciliation_captured.py tests/unit/test_spec_graph_memory.py tests/unit/test_mempalace_retarget.py -q`. Verify these paths exist before running; if an exact module name differs, notify root for a brief correction before the covering run. No bare/full unit suite, actual backend/provider, capacity run, install or unchanged postcommit rerun. Later amendments get named scoped checks and separately reported tested trees.
- [ ] Commit only the scoped implementation/new tests/storage docs, excluding root administrative files. Retain a complete report at the authorized report path with all actual RED/GREEN/failures/fixture corrections/commands/results and exact staged implementation tree, then report-only commit if needed. Root dispatches fresh original-BASE review; no worker reviewer or subagents.

## Remaining integration

The new API observes canonical memory from captured bytes and actual read-only storage, but does not authorize the physical selection/configuration or identity lifecycle. Evidence and RE acquisition, managed source/graph publication and recovery, all producers, bounded repair, legacy writer exclusion and final offline/live checkpoints remain outside this task. Do not invoke the new API from a live runtime until those owners are integrated.
