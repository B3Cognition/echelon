# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-canonical-memory-audit.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / source capture and logical paths | Existing ProjectTreeSnapshot validation and spec-local graph structure already support explicit selected physical trees with canonical logical suffixes. | Validate the entire original tree before selecting main/direct support files; reuse exact spec component contract. Do not rewrite a physical manifest or claim source selection authority. |
| 1 / native metadata | Canonical and support snapshots repeat native metadata literals and read hashes separately. | Share private construction with exact native values/order; new path hashes its captured bytes, old loaders retain existing behavior. No separate metadata authority. |
| 1 / shared audit | Current audit combines planning, expected get, reconciliation, classification and bounded extras acquisition. | Extract concrete private planning/classification seams, preserve native public/mocking/error order, use captured reconciliation plus actual complete observations in new entry. No duplicated audit body or new report schema. |
| 1 / bounded versus complete | Native extras silently stop at1000; existing complete scanner rejects overflow and supports caller budget. | New API requires explicit positive operational maximum and uses scanner as-is. Overflow/unsupported/unstable observations are unavailable, never inferred completeness. This budget is not an ID-width cap. |
| 1 / expected versus wing read | Separate expected-ID and wing reads can observe different records. | Compare same-wing expected records to scan restricted to expected IDs before classification; unexpected response IDs reject; legitimate wrong-wing observations retain native failure classification. |
| 1 / backend ownership | Existing scanner stores references to parsed metadata; reused backend objects could alter both scans. | New private get-only detached-response wrapper, with ordinary copy failure unavailable. Do not change public scanner or retarget behavior. Two equal reads still are not a lease. |
| 1 / errors | Input damage is distinct from operational planner/reconciliation/backend failure. | Validate before acquisition with bounded no-chain input error. Native fail/unavailable report phases and SystemExit convention for operational seams; other process-control exceptions propagate. |
| 1 / tests | Authentic source/planner/classifier behavior must be exercised without global memory writes. | Real native projected tree and adapter/miner; simulate only storage boundary, preserve source bytes, test full report parity and new overflow/cohort differences. Six exact covering module paths verified to exist except intentionally new test. |
| 1 / graph boundary | Real audit reports can feed existing supplied-observation graph contribution, but don't authorize source/identity. | Test-only composition retains actual failure/unavailability. No production graph/controller/gate change or invented Phase A graph-audit guarantee. |
| 1 / scope | Five changed/new production/test/doc files plus root records, one cohesive actual-audit deliverable. | No live use, positive managed execution, creating/mining/deleting backend access, additional owner, wire or public scanner changes. Escalate scope before changes. |

Root read approved full design, native canonical/support snapshot loaders and their fields, native adapter/miner read-only paths, complete scan implementation and its existing retarget callers, native expected/extras audit classification, shared artifact audit domain paths, graph audit status policy and actual graph/Squad call sites. Captured reconciliation completed at30b3a1af with331 covering tests and independent review without findings; no duplicate dispatch or unchanged rerun.

Ruling: 35. The new inactive captured canonical memory audit requires an explicit complete-scan operational row budget, detached backend responses and agreement between expected-ID and complete same-wing observations; unsupported, truncated, overflowing or changing acquisition is unavailable instead of a pass or warning-based completeness claim. Derive canonical logical main/support snapshots from one explicitly selected fully validated physical tree without claiming that selection is authenticated — native bounded extras and mutable read objects cannot prove a coherent candidate-bound collection view, and the actual observation must be distinguishable from supplied pass labels — if wrong, revise the inactive acquisition/cohort/input contract and native parity tests or introduce another explicit storage observation protocol; no IDs, revisions, graph keys, native legacy audit wire, journal receipts or canonical artifacts need rewriting, and no live consumer is activated. The row budget is not an element-ID capacity limit and equal scans are not a storage lease.

Self-review: one cohesive task covers the source-to-actual-audit boundary, with exact new signature, failure phases, observed-cohort equation, native public compatibility and prohibited live effects. Dependencies are existing native APIs; new private helpers are explicitly implementation seams rather than undeclared external services. All six covering paths exist except the new scoped test. Shared scanner/retarget behavior stays unchanged but its covering tests guard extraction fallout. Whole-controller integration remains outstanding, not implicitly complete. Root will retain plan/brief/preflight before dispatch and record BASE.

Task1 dispatched fresh /root/captured_canonical_memory_audit Astra/high at BASE1cfbb98532d75a820b5469d35d97e9ac4c9e5e15 with exact74-line brief, no-subagents contract and explicit inactive/read-only boundary. First native-grounded RED notification, exact six-module once-only covering and full report/tree chronology required. Root owns administrative files excluded/dirty. Ruling35 selected; implementation/review outstanding. No subsequent implementation protocol selected.

First genuine RED notified before production edits: exact required pytest/workdir test_mempalace_captured_audit.py::test_captured_audit_reads_candidate_tree_and_actual_collection -q failed1 in0.60s with ModuleNotFoundError for the new module, after real transaction/source projection, native RequirementMemoryAdapter/SpecMemoryMiner planning of2 main/support rows, simulated read-only writer collection and native legacy fail/present_current_count=0 assertions all succeeded. No fixture corrections. Worker proceeding to scoped production implementation.

First post-implementation GREEN attempt exposed a new-fixture mismatch: spec_id demo passes native spec scope/loading, but inherited reconciliation only recognizes the existing three-digit-prefixed canonical directory pattern, so both candidate rows were non_canonical. Worker will use001-demo and retain failed GREEN/diagnostic chronology in the report. Root confirmed this is a fixture correction preserving native policy, not a change to ID width or a new ruling. The initial absent-API RED remains genuine; no successful GREEN/covering evidence claimed yet.

Root requested an explicit matching-old-row/native pass control so a bad prefix cannot accidentally satisfy the legacy fail assertion; this is the mandated unchanged native parity coverage, not a manufactured second RED. Worker focused module now103pass0.95s, including unchanged native/captured pass with2/2 rows and48 full-report parity combinations on corrected001-demo. Worker will place the old-row/native pass control locally in the first candidate test before installing candidate rows as well. Full failed-GREEN/diagnostic chronology and final covering/tree/report still outstanding; no code policy change or additional scope requested.

Worker DONE implementation36f15ddf3fa256926300e8135ad253dc1397fc98 and report-only2c471a4191a7f7d6233f9b0969df229fef57caae. Root read complete218-line report and directly verified implementation treea56f5865efc87afacaaf9bbc5e27d1b09291dc20 and report-only diff. Exact once-only six-module covering583pass2.30s pristine, new module135 cases; no post-cover production/test/storage-doc amendment or unchanged rerun. Full chronology retains firstRED1fail0.60s; failed firstGREEN1fail0.62, assertion-detail diagnostic1fail0.61, showlocals diagnostic1fail0.62; corrected001-demo firstGREEN1pass0.59. Expanded103pass0.95; graph fixture assumed stale presence instead of native invalid ->1fail133pass1.07, corrected native invalid/fail/stale issue assertion scoped3pass0.57. Final first test contains explicit native old-rows pass control before candidate-row mismatch. Physical/source/actual-native-planning and read-only storage boundary remain real/simulated as scoped; no live source/storage claims. Root now preparing fresh original-BASE review; progress remains dirty/excluded.

Fresh Astra/high /root/review_captured_canonical_memory_audit dispatched with exact74-line brief, complete218-line report and original-BASE1cfbb985..2c471a41 package69,072 bytes. Full constraints copied verbatim, bounded non-overlapping package reads and no unchanged reruns requested. Review pending. Root inspecting only newly established dependency signatures for subsequent planning, not conducting a duplicate broad task review or changing production.

Task 1: complete (commits 1cfbb985..2c471a41, review clean)

Fresh /root/review_captured_canonical_memory_audit returned spec compliant and quality Approved with no Critical, Important or Minor findings. Complete1,420-line package and218-line report read without truncation. Named-risk checks verified full-tree exact-type/bytes/hash/mode/membership validation and immutable byte reuse, existing exact spec component validation, complete scanner malformed/overflow/duplicate/wing/truncation/order/content rejection, and cut-off shared classification helpers retaining expected/extras/count/probe/failure policy. Historical execution Cannot Verify resolved by root's witnessed notifications, complete report and directly verified implementation/report-only trees. Physical selection/config provenance, long-lived storage consistency and identity/semantic/publication authority remain explicit non-claims, not silently supplied guarantees. No unchanged tests rerun. Ruling35 now implemented and independently approved; root checkpoint only administrative records. No subsequent implementation selected.

## Other-domain observations (read-only; no next protocol selected)

Spec evidence snapshots select CANONICAL_SPEC_EVIDENCE_ARTIFACTS at the spec root plus allowed direct files and manifest under evidence/. Native metadata includes spec-evidence scope/kind, canonical logical paths, active status, VERIFY phase, native filename-based room and spec_id. load_spec_evidence_artifact_snapshots defaults to requiring landed frontmatter; graph source loading opts allow_unlanded=True but its separate audit call keeps the default landed requirement. Preserve or explicitly adjudicate that distinction when wiring captured acquisition; do not silently turn optional unlanded evidence into passed verification.

RE descriptor snapshots select MINED_RE_ARTIFACT_KINDS with native (kind,scope) room mappings, exact descriptor sha256/path/source ID and reverse-engineering scope. Legacy RE loader separately selects curated files and native path-based kind/room rules; neither mode is interchangeable with an arbitrary list of byte strings. The current generic artifact audit already consumes supplied snapshot lists internally but acquires expected rows and a separate bounded extras scan. Future captured evidence/RE audit must share its existing classification and use actual observations, not a caller pass label or silently narrowed global RE scan. No changes to those modules or positive producer/retrieval/publication protocol were selected during this investigation.

Future legacy graph-writer admission has an existing spec-only ownership read: IdentityStore.managed_identity(spec_id=...) returns detached genesis or None in a query-only transaction. managed_store.read checks same-spec orphan registration operations, request/namespace/operation association and source context; no invented run identifier or new parallel lookup API is needed merely to inspect a spec. It is not a complete runtime admission or filesystem lease. Native build_spec_graph still builds legacy projections; graph_read.load_graph loads persisted bytes plus live audit. This task does not change either graph path, and no new graph exclusion/positive publisher protocol is selected here.
