# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-canonical-memory-audit.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / source capture and logical paths | Existing ProjectTreeSnapshot validation and spec-local graph structure already support explicit selected physical trees with canonical logical suffixes. | Validate the entire original tree before selecting main/direct support files; reuse exact spec component contract. Do not rewrite a physical manifest or claim source selection authority. |
| 1 / native metadata | Canonical and support snapshots repeat native metadata literals and read hashes separately. | Share private construction with exact native values/order; new path hashes its captured bytes, old loaders retain existing behavior. No separate metadata authority. |
| 1 / shared audit | Current audit combines planning, expected get, reconciliation, classification and bounded extras acquisition. | Extract concrete private planning/classification seams, preserve native public/mocking/error order, use captured reconciliation plus actual complete observations in new entry. No duplicated audit body or new report schema. |
| 1 / bounded versus complete | Native extras silently stop at1000; existing complete scanner rejects overflow and supports caller budget. | New API requires explicit positive operational maximum and uses scanner as-is. Overflow/unsupported/unstable observations are unavailable, never inferred completeness. This budget is not an ID-width cap. |
| 1 / expected versus wing read | Separate expected-ID and wing reads can observe different records. | Compare same-wing expected records to scan restricted to expected IDs before classification; unexpected response IDs reject; legitimate wrong-wing observations retain native failure classification. |
| 1 / backend ownership | Existing scanner stores references to parsed metadata; reused backend objects could alter both scans. | New private get-only detached-response wrapper, with ordinary copy failure unavailable. Do not change public scanner or retarget behavior. Two equal reads still are not a lease. |
| 1 / errors | Input damage is distinct from operational planner/reconciliation/backend failure. | Validate before acquisition with bounded no-chain input error. Native fail/unavailable report phases and SystemExit convention for operational seams; other process-control exceptions propagate. |
| 1 / tests | Authentic source/planner/classifier behavior must be exercised without global memory writes. | Real native projected tree and adapter/miner; simulate only storage boundary, preserve source bytes, test full report parity and new overflow/cohort differences. Six exact covering module paths verified to exist except intentionally new test. |
| 1 / graph boundary | Real audit reports can feed existing supplied-observation graph contribution, but don't authorize source/identity. | Test-only composition retains actual failure/unavailability. No production graph/controller/gate change or invented Phase A graph-audit guarantee. |
| 1 / scope | Five changed/new production/test/doc files plus root records, one cohesive actual-audit deliverable. | No live use, positive managed execution, creating/mining/deleting backend access, additional owner, wire or public scanner changes. Escalate scope before changes. |

Root read approved full design, native canonical/support snapshot loaders and their fields, native adapter/miner read-only paths, complete scan implementation and its existing retarget callers, native expected/extras audit classification, shared artifact audit domain paths, graph audit status policy and actual graph/Squad call sites. Captured reconciliation completed at30b3a1af with331 covering tests and independent review without findings; no duplicate dispatch or unchanged rerun.

Ruling: 35. The new inactive captured canonical memory audit requires an explicit complete-scan operational row budget, detached backend responses and agreement between expected-ID and complete same-wing observations; unsupported, truncated, overflowing or changing acquisition is unavailable instead of a pass or warning-based completeness claim. Derive canonical logical main/support snapshots from one explicitly selected fully validated physical tree without claiming that selection is authenticated — native bounded extras and mutable read objects cannot prove a coherent candidate-bound collection view, and the actual observation must be distinguishable from supplied pass labels — if wrong, revise the inactive acquisition/cohort/input contract and native parity tests or introduce another explicit storage observation protocol; no IDs, revisions, graph keys, native legacy audit wire, journal receipts or canonical artifacts need rewriting, and no live consumer is activated. The row budget is not an element-ID capacity limit and equal scans are not a storage lease.

Self-review: one cohesive task covers the source-to-actual-audit boundary, with exact new signature, failure phases, observed-cohort equation, native public compatibility and prohibited live effects. Dependencies are existing native APIs; new private helpers are explicitly implementation seams rather than undeclared external services. All six covering paths exist except the new scoped test. Shared scanner/retarget behavior stays unchanged but its covering tests guard extraction fallout. Whole-controller integration remains outstanding, not implicitly complete. Root will retain plan/brief/preflight before dispatch and record BASE.
