# SDD ledger — plan: docs/superpowers/plans/2026-09-12-durable-source-context-heads.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / source manifest decoder | Source contexts persist metadata claims, not original bytes. | Existing validated exact snapshot used for explicit registration/head; baseline codec retains actual original bytes for source-bound requests. No fake byte-bearing metadata or duplicated projection. Codec must pass independent gate before dispatch. |
| 1 / publication request | Existing frozen request has three fields and version1 wire, recovery string is opaque. | Add trailing default sources=None preserving positional/version1 bytes; explicit version2 closed source claim binds context/predecessor/baseline. No auto-detection or reinterpretation of opaque recovery contents. Root clarified exact five root keys before dispatch. |
| 1 / existing journal writer | Existing apply owns child lifecycle/binding writes and original application receipt in one caller TX. | Same apply adds source acceptance and head pointer atomically; source plan is parent-bound, not a new child method, independent lifecycle writer or second commit. Source-bound application version2 adds exact sources record with no circular self-hash. |
| 1 / current head | A derived maximum alone silently rewinds when the highest source row disappears. | Add independent nullable head_publication_id and verify against indexed highest accepted history. CAS pointer/application acceptance in same transaction. Missing/pointer-only drift blocks, never repairs; see Ruling24. |
| 1 / registration missingness | A surviving registration operation holds only method/spec/digest, not its deleted context name. | Conservative orphan-registration rejection within the spec, supported by partial source_registration_specs index. Do not invent lost context names or scan whole operation history on head reads. |
| 1 / selection and caller authority | Complete selected metadata can be structurally valid yet represent an incomplete or wrong caller-selected context. | Immutable selection within each registered context; expected predecessor must belong to that context. Correct context selection, completeness, managed run and namespace caller binding remain future owners. Root removed ambiguous cross-context authentication claim before dispatch. |
| 1 / empty selection | Draft test said all empty selections reject, but metadata registration accepts canonical empty selections. | Clarified that emptying a previously nonempty selection rejects; legitimately empty registered metadata remains valid and is not completeness proof. No new blanket restriction. |
| 1 / retries and later heads | Current-head CAS would incorrectly reject exact old retries after later publication. | Original registration/preparation/application/release receipts checked before today's-head comparisons; only first new operations compare current predecessor. Historical source rows validate recorded links without requiring latest status. |
| 1 / schema upgrades | Current full audit routes binding only3/4 and publication only4; current publication loader has no old-source-table distinction. | Freeze exact schema4, additive5, explicit source-state parameter on old audit paths; no missing-table suppression. Binding3/4/5 publication4/5 source5, full snapshot audit updated. Source-less old receipt wire preserved. |
| 1 / schema and performance | Two tables retain contexts/plans; histories must not be scanned/replayed for current reads. | Partial accepted-head numeric index, unique sequence index, context PK/registration uniqueness and partial orphan-registration index. Ordinary global operations preserve pending guards; unbounded sequence arithmetic uses existing decimal helpers. |
| 1 / faults and filesystem composition | SQLite cannot atomically commit arbitrary filesystem state; publish_sources only supplies a retained same-lock observation window. | Test-only callbacks compose prepare/apply, actual crash prefix/reopen/retry; source head is accepted observed state, not post-return freshness. Pending intent/recovery materials retained through later verification/release. Pre/post actual COMMIT error semantics distinguished. |
| 1 / tests and scope | Source/publication/schema/snapshot/admin/integration interfaces share state and must move together. | One independently reviewable task with exact eleven-module final covering run and one existing million-record capacity measurement for changed schema/guard; not a million source publications/assessed revisions. No provider/controller/graph/retarget activation. |

Ruling: Retain an independent accepted-source head pointer and require it to agree with the indexed highest accepted source history; do not derive the current head solely from surviving rows — deleting the highest row would otherwise silently rewind knowledge while a retained parent publication still records the accepted state — if wrong, revise the inactive schema5 pointer/consistency checks and deletion/CAS regressions or substitute another independently retained ownership proof; no existing element IDs, revisions, graph keys, canonical files or live data are rewritten by this unactivated library change. This is Ruling24 in the chronological final handoff.

Self-review: main read the complete 125-line draft, checked all public/request/receipt formats and existing publication/store/schema/snapshot audit call sites, and corrected the three preflight ambiguities above. One task because registration schema and source-bound prepare/apply/audit share durable invariants; registration alone is not the deliverable. Whole approved design still requires managed feature/namespace/run/context registration, authenticated historical/context/dependency transitions, provider proposal/reservation and isolation, exact candidate/semantic binding, graph/memory completion and bounded repair. A new source context is an explicit caller observation acceptance, not silent managed-spec initialization or historical reconciliation. No live activation authorized. Most-capable implementer/reviewer warranted for journal/schema/recovery/CAS integration.

Task 1: pending — source-manifest wire codec awaiting independent review. After clean gate, checkpoint/plan commit and exact BASE/brief, then fresh Astra/high implementer. Root owns plan/ledger, one implementer/no children. Preserve all artifacts through final whole-branch review and exhaustive rulings handoff.

Final interface check before dispatch corrected draft baseline-codec names to actual encode_initial_publication_sources / decode_initial_publication_sources (existing public definitions lines297/414). Source-manifest codec now independently approved at40ff5fbf. No helper aliases invented. Root preparing checkpoint/plan commit; no production source-head changes yet.

Task 1: dispatched /root/identity_durable_source_context_heads Astra/high, BASEcf5252c5582de05025bb629f8c3d88b639eb73a3, 102-line extracted brief plus full implementer/report template and Ruling24 clarification. Source codec/guard immutable dependencies, no activation, one implementer/no children. Exact eleven-module final covering run/capacity classification specified. Root independently reviews full original-base range.

Required first actual RED before production: real capture retained CRLF/binary bytes, seal/factory and IdentityStore.initialize succeeded; missing IdentityStore.register_source_context caused1failed0.21s in the exact required regression. Worker now implementing schema5/connection-owned source helpers then source-bound journal composition. No provider/controller effects.

Worker interim registration GREEN1passed. Schema5/explicit schema4 recognition and source-state audit routing in progress. Internal _namespace validation previously assumed latest schema; worker adjusting it to recognize frozen schemas for genuine schema4 journal auditing. Root confirmed this fits the planned old-schema audit contract, with ordinary public open/transaction boundaries still strictly requiring current schema and no writer access to unupgraded authority. Version2 source-only acceptance/closed receipt regression next; no final covering run or review yet.

Worker interim source-only version2 flow GREEN: registration plus three equal-content accepted heads, independent closed request/application/release expectations, stale original predecessor rejection, original retries after reopen and unchanged materialized identity snapshot. Reported implementation uses bounded immediate-predecessor validation and independent indexed head comparison, with source support explicitly disabled during schema4 audit. Adversarial/atomicity and real publication callback composition coverage still in progress; not final verification.

Worker interim76 focused source tests pass: mixed seven-family journal, real SQL denial at source-plan/child/head/acceptance/receipt/COMMIT, explicit uncertain outcome after actual commit, separate-process stale-predecessor competition, damaged pointer/history without fallback, bounded malformed claim validation and frozen schema4 prepared/applied/released upgrade/restore. Backup's old schema-only validation allowed17 observed corruption cases; worker added full authority audit before claiming the destination, with those cases now passing. This falls within the planned backup/restore rejection requirement; named review surface is audit completeness plus destination/transaction ordering. Real filesystem callback recovery and final eleven-module run remain; do not claim task complete or final-code suite results yet.

Worker real publish_sources callback recovery now passes: interrupted actual filesystem prefix, retained original binary/CRLF decoded from stored version2 request, writes/deletion/no-op/nested parents/modes, final accepted head and explicit release. Source drift blocks before prepare; after-callback failure retains pending recovery. Additional actual helper-input RED found altered supplied parent request escaping source-plan validation; exact retained-parent request binding now GREEN. Focused existing journal/admin/binding compatibility254passed. Exact eleven-module final covering run including one million imported-ID capacity case starting; wait for full report/commits before packaging independent review.

While that run is executing, worker self-review identified a missing-parent branch: deletion of both parent publication and ordinary operation with retained source row may returnNone. Root confirmed planned orphan/parent consistency scope, requested actual focused RED before fixing and only source/publication covering modules afterward. Do not repeat capacity/full covering run or attribute its result to subsequently amended production code. Worker also checking acceptance-helper argument revalidation before final packaging; no review dispatch until all amendments/report/commits finish.

Covering checkpointf8911aef:668passed74.12s. Capacity imported1,000,000 IDs14.496s; open0.000731s; nextallocation0.016208s; database168,914,944bytes. These are imported IDs, not source heads/assessed revisions. Afterward7actualREDs (3missing-parent state cases,4malformed acceptance-helper inputs) fixed; source/publication covering amendment203passed17.23s. Those later production amendments were not present in the eleven-module/capacity run.

Worker performance self-review found source-head read called journal_load(effects=True), whose historical child-effect validation can scan revisions. Root clarified the already explicit no-history-scan current-read contract includes identity history too. Merely switching effects=False is insufficient because _load still calls unconditional _absent_rows for the parent. Preserve existing full journal read/retry/audit and _ApplicationStore/common-guard behavior; source reads must use indexed source/registration/parent and immediate-predecessor checks while retaining exact v2 envelope/preparation/request/source-receipt/digest/state associations. No extra schema/index allowance, no whole-history replay. Worker to add real mixed-head scan regression and exact damaged-parent/application controls, then only source/publication covering tests. This is the original current-read/full-audit separation, not source-state semantic or child-effect certification. Wait for worker resolution/report before review.

Worker bounded-read correction: source_context uses indexed parent/claim ownership, shared pure journal plan/request/preparation validators, explicit closed application/state/completion checks, source plan and immediate predecessor validation; no full_load/_absent_rows. Actual mixed-head SQLite-authorizer regression failed on prohibited reservations access; four existing structural-damage controls passed. Full journal/common-guard paths unchanged. Source/publication amendment covering run now executing; await complete report/commits.

Task1 implementerDONE, commitsf8911aef and7e7a177a669efa4c5f6faaa5e80bc7dd31eea73c. Root read FULL231-line report; final amended source/publication208passed17.89s,92source+116journal, pristine. No production edits followed; no repeated capacity/full suite. Whole original-base diffcheck clean and workingtree clean. Package review-cf5252c5..7e7a177a.diff158,058bytes covers BOTHcommits. Fresh /root/review_identity_durable_source_context_heads Astra/high dispatched fullbrief/report/package with exact global constraints and named old-schema/backup/bounded-read/pointer interface risks. Readonly/nochildren/no reported-test reruns; full bounded nonoverlapping coverage required. Independent gate pending; not complete.

Independent /root/review_identity_durable_source_context_heads Astra/high approved spec and task quality; no Critical/Important/Minor findings. FULL brief1–102,report1–231,158,058-byte2535-line diff read in nonoverlapping1–300/301–580/581–880/881–1130/1131–1345/1346–1600/1601–1820/1821–2020/2021–2230/2231–2420/2421–2535; initial brief truncation repaired immediately, diff untruncated. Named unchanged checks: manifest detachment/canonicalization, baseline signatures, projection input, identifiers, strictJSON. No tests/git/writes/subagents. Only cannot-verify execution chronology/test outcomes resolved by root's witnessed initialRED and sequential worker checkpoints/full report; no new unsupported success claim. Root read complete final review. No fix loop, no deferred findings in this task.

Task 1: complete (commits cf5252c5..7e7a177a, review clean)

All library source-context task requirements handled; full approved integration still outstanding. Preserve workspace for final whole-branch review and exhaustive Ruling24 handoff. No rollout/main/install/stopped-run changes.
