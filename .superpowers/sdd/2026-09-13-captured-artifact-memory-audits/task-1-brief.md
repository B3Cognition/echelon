### Task 1: capture both native artifact memory audit domains

**Files:** Create `src/echelon/mempalace_captured_artifact_audit.py` and `tests/unit/test_mempalace_captured_artifact_audit.py`; modify `src/echelon/mempalace_memory_audit.py` for shared private classification, `src/echelon/mempalace_captured_audit.py` for shared existing acquisition, `src/echelon/mempalace_spec_evidence.py` and `src/echelon/mempalace_re.py` for shared private snapshot/report construction, `src/harness/spec_frontmatter.py` for a shared private pure frontmatter parser, and `docs/element-identity-storage.md`. No existing tests, public scanner, registry, graph, controller or other production changes without root escalation. Root owns plan/brief/progress; no subagents.

**Only new public APIs**, in the new focused module:

```python
def audit_captured_spec_evidence_memory(
    project_root: Path, *, spec_id: str, tree: ProjectTreeSnapshot,
    maximum_scan_rows: int, allow_unlanded: bool = False,
) -> SpecEvidenceMemoryAuditReport:
    # Native evidence selection over one captured spec tree.

def audit_captured_re_memory(
    project_root: Path, *, tree: ProjectTreeSnapshot,
    descriptors: tuple[ReArtifactDescriptor, ...] | None,
    maximum_scan_rows: int,
) -> ReMemoryAuditReport:
    # Explicit descriptor-catalog mode or explicit legacy curated mode.
```

Both require an absolute Path project_root and a positive exact-int maximum_scan_rows before acquisition. This is the same explicit operational budget as audit_captured_spec_memory. Validate every source value before adapter/config/planner/storage access. Invalid input raises `SpecMemoryError("invalid captured artifact memory input")` outside the exception handler, with no cause/context/source-bearing traceback; process-control BaseException propagates. Operational adapter/open/get/copy/cohort/scanner exceptions or SystemExit produce a native unavailable report, with artifact_count known from validated snapshots and expected_count0 before planning or exact planned count afterward. Planner failures/SystemExit produce a native fail report; KeyboardInterrupt and other uncaught process-control BaseException propagate. Native report constructors/converters must use their actual fields, not an invented label/root on a domain type.

**Private shared acquisition:** Extract the existing expected get, detached-response wrapper use, exact expected-ID response membership check, complete scan and same-wing cohort comparison from audit_captured_spec_memory into one concrete private helper in mempalace_captured_audit. Both canonical and new domain adapters call it. Preserve current canonical outcomes, call order, complete scanner defaults/retarget behavior and the existing `_DetachedReadOnlyCollection` implementation. No second scan protocol, new DTO or public backend-injection framework. Return the existing parsed rows plus complete wing row map; adapt between the existing canonical `_ParsedCollectionRows` and generic `_CollectionRows` carriers only where their native helper requires it. Use actual native adapter factories with run_id `audit` and their native planner/read-only opener. Do not infer another wing or open global memory in tests.

**Private shared artifact classification:** Factor existing audit_artifact_memory into acquisition/planning plus one shared private classification/report helper. Factor `_scan_extras` into its unchanged bounded acquisition and a shared already-parsed extras classifier. Preserve exact native metadata/hash/lifecycle rules, malformed expected handling, sorted/deduplicated native lists, counts, historical/duplicate warning semantics and all existing error behavior. New callers use the acquired complete wing rows for extras; no second bounded extras query. Do not duplicate the approximately160-line classification body. Existing native paths keep their original bounded policy and monkeypatch seams, including original behavior of failure branches; this task is not a redesign or unrelated fix of old audit exception constructors. New captured functions must themselves return valid existing domain report objects. Share normal native report conversion without accidentally changing legacy exceptional call paths.

**Evidence source selection:** Reuse the reviewed `_capture_logical_sources` validation/mapping with probe_retrieval=False for the selected spec tree (or extract its existing image mapping privately if needed without changing canonical behavior). Require the existing full original tree validation, exact spec component and present spec.md; do not mutate/rebase the original physical observation. Canonical, run-local and explicit staging physical roots map suffixes to `specs/<spec_id>/...` as in the existing captured canonical audit. Select exactly existing `CANONICAL_SPEC_EVIDENCE_ARTIFACTS` at the root and allowed direct names from `PUBLISHED_VERIFY_EVIDENCE_ARTIFACTS` plus manifest.json under existing `PUBLISHED_EVIDENCE_DIR`. Do not recursively include unrelated evidence files. Preserve final native Path component ordering using PurePosixPath ordering rather than raw path-string sorting.

Share private native evidence snapshot metadata construction with `_snapshot_for_artifact`, retaining exact scope/kind/canonical/source/hash/status/provenance/phase/room/spec_id values and native filename-to-room function. Native disk read/resolve/hash order stays unchanged; captured hashes come only from the same captured content bytes. All ignored original tree records still validate before filtering, and selected bytes/metadata must be detached before adapter callbacks. A valid selected spec with zero evidence files can produce an empty expected plan and still scans for extras, as native evidence audit does.

`allow_unlanded` must be exact bool. Default False must preserve the landed requirement using status from the captured spec.md YAML frontmatter: `str(value or "").strip().lower() == "landed"`. True is the existing explicit opt-in to unlanded evidence auditing, not an identity/quality waiver. Share a private `_parse_frontmatter_text(text)` with `_read_frontmatter_only` by extracting its current regex/YAML/mapping/error behavior; preserve every native caller. Decode captured UTF-8 and emulate Path.read_text universal newline translation before this parser, without normalizing the original bytes or their hashes. Thus LF, CRLF and CR frontmatter match native status behavior. Malformed/absent/nonmapping frontmatter yields the native empty metadata result and fails the default landed condition before acquisition. This captured status check does not read unrelated targets.yml or claim a full native filesystem frontmatter/target acquisition. Existing graph’s differing unlanded source-loading versus default-landed audit choice is not changed by this task.

**RE source selection:** Require a fully validated existing ProjectTreeSnapshot whose physical path is exactly `re`, detached into exact project-relative bytes. Do not resolve live roots or load a registry from disk. `descriptors=None` explicitly selects legacy curated mode and requires no captured file or directory at re/index.json; do not downgrade a present registry to legacy. Select native `_is_curated_re_artifact` paths from the whole tree and preserve native Path component ordering, classifications and rooms. This mode requires at least one native curated artifact, matching the native published-RE-root requirement.

An exact tuple selects descriptor-catalog mode; a captured regular re/index.json is required as a presence witness, not parsed/claimed registry authority in this API. Require the tuple to be nonempty, have unique exact descriptor paths in canonical native string-path order, and contain exact ReArtifactDescriptor records. For each descriptor, get its exact captured file bytes and validate using the existing pure GraphReArtifact / spec_graph_re._validate_artifact contract (not validate_re_artifact_descriptor, which rereads files). Validate every supplied descriptor and every original tree record before applying MINED_RE_ARTIFACT_KINDS/RE_ARTIFACT_ROOMS selection. Require at least one eligible snapshot; no empty-catalog or filtered-empty fallback to legacy. Preserve descriptor scope/source ID and exact kind-to-room behavior, including misleading filenames. The caller must authenticate catalog completeness/association to its captured index elsewhere; an index’s presence and a self-consistent supplied descriptor do not prove that association. No registry/index/schema parser changes or automatic descriptor inference.

Share native RE snapshot/metadata construction for both descriptor and curated loaders without changing their I/O/order. Descriptor metadata includes its native re_artifact_scope and optional re_source_id; legacy metadata does not invent these fields. Both retain existing source/hash/room/kind/status/provenance/phase values. The returned RE audit covers the supplied selected catalog/curated tree and actual wing observation; it does not certify a global registry or silently narrow an authenticated catalog. Existing graph report projection remains a separate consumer. Existing native ReMemoryAuditReport omits generic historical rows but retains their effect on native status; preserve that conversion.

**First RED, grounded in native sources/plans:** Use a real resolved temporary landed spec with evidence-grades.md and native source inspection/projection to capture changed evidence without promoting it. Use the actual SpecEvidenceMemoryAdapter and SpecMemoryMiner with only the writer/collection boundary simulated and palace location isolated. Show matching old rows pass the native audit first; then install matching candidate rows and show the native old-disk audit cannot certify them. The new captured evidence API is absent and should fail only after those controls succeed. Name `test_captured_evidence_audit_uses_candidate_bytes_and_native_storage_rows`.

```python
assert native_old_report.status == "pass"
assert native_old_report.present_current_count == len(old_rows)
assert native_candidate_rows_against_old_disk.status == "fail"
report = captured.audit_captured_spec_evidence_memory(
    root, spec_id="001-demo", tree=projected_tree, maximum_scan_rows=2000)
assert report.status == "pass"
assert report.present_current_count == len(candidate_rows)
assert evidence_path.read_bytes() == old_bytes
assert collection.writes == []
```

- [ ] Read the scoped implementations and native tests/fixtures; run the first test before production edits with `/Users/michalbachorik/work/echelon_r/echelon/.venv/bin/pytest tests/unit/test_mempalace_captured_artifact_audit.py::test_captured_evidence_audit_uses_candidate_bytes_and_native_storage_rows -q` from this worktree. Notify root of actual RED and fixture corrections. Real planner/classification/source/ownership code must remain real; only storage boundary is simulated.
- [ ] Implement the shared acquisition/classification and source factories plus explicit new adapters, then rerun the first test to GREEN. Keep root administrative files excluded. Escalate any unresolved native compatibility or additional owner/schema/API change.
- [ ] Add full native/captured report parity for both domains on unchanged sources and coherent collection responses: present/missing/stale content/hash, wrong wing/room/scope/path/source_file/kind/spec ID/schema/requirement identity, lifecycle, historical/duplicate/unrelated rows, malformed expected response behavior where complete observation agrees, and planner errors. Test actual descriptor-backed and legacy-curated RE native paths with independent expected drawer IDs/metadata. Do not fake a report or replace the shared native classifier/planners.
- [ ] Cover every evidence source-selection set and filename room family, allowed evidence manifest/direct files, nested/unknown exclusions, canonical/run-local/staging mapping and native Path component ordering. Default unlanded/missing/malformed status blocks before acquisition; explicit allow_unlanded True permits native auditing; exact bool rejects coercion. Exercise LF/CRLF/CR and invalid UTF-8 without hashing normalized bytes. Preserve all existing frontmatter tests unchanged.
- [ ] Cover descriptor mode with real registered/native catalog fixtures captured in full, misleading names, source/workspace scopes, unmined-but-valid descriptors validated before filtering, path/hash/owner/source-ID damage, missing/index-directory witness, duplicates/order/empty/all-unmined rejection and no fallback. Cover legacy curated roots/files/rooms, ignored caches/suffixes, component ordering, absent/empty/no-curated root and presence of index.json refusing legacy mode. Pure catalog validation is not actual registry association proof; test/report that boundary honestly.
- [ ] Reuse the common acquisition regressions rather than duplicating its entire matrix: for each new domain test coherent pass, overflow/unavailable, a changing expected/full cohort, reused nested backend metadata mutation, unexpected expected-response ID and a relevant extra after its native bounded window. Assert generic domain duplicate/historical semantics precisely (do not promote a native warning to failure). Keep the fixture bounded; do not run a million-row benchmark. Complete scan must feed extras without another bounded query.
- [ ] Test no source/registry rereads after capture with native loaders, path resolution/read, creating/mutating backend methods and live registry calls as tripwires, allowing only configuration acquisition and simulated read-only storage. Change/delete source files after capture and retain the captured result. Factory-time mutation of source/catalog objects cannot change detached plans/metadata. Validate invalid input before any acquisition, bounded no-chain errors, operational SystemExit handling and other process-control propagation. Record original source and collection state unchanged except deliberate drift fixtures.
- [ ] Compose actual returned missing/stale/unavailable reports with existing evidence/RE captured graph memory contributions in tests. Preserve origin `returned`, native invalid/present/missing semantics and inherited RE projection behavior; do not fabricate pass, current revision evidence, complete graph or publication authority. No production graph changes.
- [ ] Document explicit selection and landed/catalog-mode semantics, complete bounded observed cohort, native metadata/classification reuse and remaining physical/registry/config/lifecycle/publication authority. Highlight that these functions remain inactive, and the legacy exceptional paths and default graph choices are unchanged.
- [ ] Self-review and run once the exact covering set with the absolute pytest executable above: `tests/unit/test_mempalace_captured_artifact_audit.py tests/unit/test_mempalace_captured_audit.py tests/unit/test_mempalace_spec_evidence.py tests/unit/test_mempalace_re.py tests/unit/test_spec_frontmatter.py tests/unit/test_spec_graph_memory.py tests/unit/test_mempalace_retarget.py -q`. Confirm paths before running. No full unit/bare pytest, actual backend/provider, install, capacity or unchanged postcommit rerun. Later changes get named scoped checks with separately identified tested trees.
- [ ] Commit only scoped implementation/new tests/storage docs, then full report at the authorized path (force-add exact report path if ignored; report-only commit fine). Include every actual RED/GREEN/failure/fixture correction/diagnostic, exact command/results, staged tested tree and post-cover amendments. Root dispatches fresh original-BASE independent review. No worker subagents/reviewers.

## Remaining integration

All three memory domains will have captured-source observation paths after this task, not an authenticated aggregate acquisition or managed runtime. Complete source/catalog/config selection, identity-aware retrieval, source/graph publication and recovery, producer allocation and semantic checks, bounded repair, legacy writer exclusion and final rollout checkpoints still require their existing owners. No live activation or stopped-run migration is authorized here.
