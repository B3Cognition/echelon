# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-identity-graph-assembly.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / projected image carrier | Existing ProjectedPublicationSources contains exact selected trees/files plus metadata manifest, not a marker or observed capture. | Revalidate via existing manifest factory; require canonical selected spec tree; keep all hidden/binary/missing image semantics. Do not manufacture a captured baseline or acceptance proof. |
| 1 / local structure | Existing builder supports captured tree parsing and local final roles; full live policy also includes selected external artifacts. | Reuse structure first and add only missing explicit policy paths, checking existing input hashes and preserving local final overrides. Promise exact rendered records, not live tuple insertion order. |
| 1 / domain order | Native canonical memory always emits a receipt; evidence and RE return early without snapshots. | Require canonical observation, optional domains nonempty, execute fixed canonical/evidence/RE order regardless supplied tuple order. Existing builders keep all report/row/source rules. |
| 1 / shared byte table | Independently valid memory/RE DTOs can otherwise carry bytes differing from the proposed source images. | Every graph source/receipt content must equal the shared present bytes; semantic manifest path must be captured. No parsing/authentication of registry or storage claims invented. |
| 1 / selected RE descriptors | Lower-level contribution intentionally skips missing Artifact nodes for legacy compatibility. | Full assembly rejects an explicitly supplied descriptor without a preceding Artifact key rather than silently losing it; lower-level API unchanged. This is a new assembly consistency boundary, not physical selection proof. |
| 1 / history | Existing project_identity_history validates retained snapshot and emits stable typed history/evidence graph records. | Invoke last, preserve original snapshot/claims and unassessed legacy edges, no store read or revision/subject inference. |
| 1 / graph self-input | Complete spec source tree may retain old or future graph-file bytes; graph must not hash itself. | Keep bytes in selected-source manifest, forbid graph as policy/memory input and leave ordinary existing local selection unchanged. No exemption from later sealed publication. |
| 1 / values and errors | Existing nested builders validate exact values and own transformed records. | Narrow outer grouping/byte coherence validation plus existing owners; bounded outside-handler errors, BaseException propagation, no new codec/copier/graph model. |
| 1 / native comparison | Local extra artifacts can be inserted later than the live builder but final role/records must match. | Compare full existing render_spec_graph bytes after same retained-history projection; independent fixture expectations and native comparison cover every record/hash. Raw tuple order is not promised by the new API. |
| 1 / first RED | Existing real publication/image projection/native planner/history graph can succeed before new assembler exists. | Complete actual setup and old-evidence binding assertions before intended missing-module import; root notification precedes production. Physical fixture explicitly POSIX, pure tests portable. |
| 1 / covering | New module integrates projected images, all graph contributions and history. | Eight named modules once after focused cases, exact tree and post-amendment chronology, no unrelated capacity/full-unit/live work. |
| 1 / approved scope | Full source/authentication/semantic/runtime/producer/publication/repair integration is still required. | Only offline coherent graph assembly is implemented here; explicit remaining-integration section prevents false completion/activation. |

Root read approved design and current contributor interfaces, source projection/manifest factory, graph model/rendering and identity projection signature, native memory/history fixture, actual publication builder add_write/seal/inspection and projected-image fixture. Captured RE task is complete at checkpoint79c165e4 after681 covering and144 path-order fix tests; original full review and clean scoped review resolved its one Important. Source context fixed-selection and native registry/config/mining limitations remain recorded in the previous checkpoint; this task introduces none of their pending protocols.

Self-review: no new product-policy ruling or live authority decision. This is a strict opt-in supplied-value composition API under the approved graph preservation design. It uses canonical-path projected images, requires every declared graph source to be present/coherent, and leaves complete selection/authentication with the future controller owner. Source/path aliases, registry acquisition, graph sealing, accepted-source selection, managed run transitions and repair protocol remain unselected here. No new fields on existing graph wire, no native interpretation changes.

Fresh Astra/high implementer/reviewer chosen for multi-domain byte coherence and source-versus-authority boundary judgment. One implementer, no child agents; root plan/ledger ownership and retained task artifacts through final whole-branch review/exhaustive rulings remain. User approved automatic continuation; no new approval requested for this inactive worktree step. No subsequent implementation selected.

Task 1 dispatched fresh /root/captured_identity_graph_assembly Astra/high at BASE60f5f8bb483cf37521fb5267f8ae3edf56e81808,128-line brief with concrete first native/publication/history setup and complete implementation/report discipline. First actual RED notification required before production; no existing production owners changed, only new assembly module/tests/docs. Exact parent pytest and once-only eight-module covering specified. Root ledger intentionally dirty/excluded; no next implementation selected.

Required actual RED witnessed before production: exact parent absolute pytest and this worktree, tests/unit/test_spec_graph_captured.py::test_captured_assembly_retains_projected_source_and_old_evidence -q =>1failed0.69s solely at absent echelon.spec_graph_captured import. Actual IdentityStore reserve/create/evidence/revise/history, real sealed source inspection/projection, native canonical planning, existing local/memory and history projection all completed first. Independent assertions confirmed stable key/current revision2 and retained evidence targeting revision1. Worker starts new module after root notification.

## Exploratory integration observations (not a selected follow-up API)

Root inspected IdentityStore check_identity_candidate, validate_projected_bindings and preview_identity_history ownership while this task runs. They each currently open their own query-only transaction. A future managed owner must not assume separately obtained candidate diagnostics and proposed-history hashes describe the same authority snapshot; v3 publication binds the preview's history but does not retroactively authenticate an earlier candidate check. Shared underlying candidate_store/check_identity and snapshot_preview/preview already accept caller connections. Any unified preflight must also bind the exact decoded journal lifecycle/reference/occurrence operations to candidate scopes/report contexts and source-claim checks; no such method/result shape is selected in this task.

Root read actual IssueReportContext before/after occurrence mapping, candidate reference-state result fields and validate_reference_claim_sources. The source checker validates supplied proposed claims, not completeness of every reference as a new stored claim. Preserve this boundary; do not infer semantic assessment, synthesize evidence bindings or silently consider source consistency a complete candidate/history/publication gate. Graph assembly remains a pure derived view and its code scope is unchanged.

Worker progress: required first GREEN1pass; independent complete rendered fixtures for missing/empty canonical-only and all three memory domains plus source/workspace RE decisions/topology3pass; new strict outer/nested/damaged/subclass/recursive/source-table/self-input/process-control cases101pass. Full native comparison, actual guarded source publication, purity/ownership and unavailable-observation cases still pending; covering eight-module run reserved. This is partial test evidence, not completion or authority activation.

Further read-only integration inspection: publication_store.operation_children already decodes the exact canonical ordered operations; planned_effects owns lifecycle planning plus projected binding validation; snapshot_preview._overlay owns exact proposed materialized snapshot construction from an already captured retained snapshot. Reusing these in one query-only transaction can avoid both a second authority observation and an independent lifecycle batch. Existing candidate diagnostics report retained reference assessments, so proposed claims must not silently rewrite those observations. A clean candidate result alone does not ensure each newly supplied issue occurrence is a journal child, or each proposed journal occurrence is represented by a candidate report; that association needs an explicit future integration check using full native occurrence payloads and retained provenance, not display IDs alone. No public DTO/signature or occurrence policy is selected in this active graph task.

Read-only test planning: native typed requirement parsing plus exact existing candidate check and proposed-history preview can establish a first RED before any new integration method. A real rollback-journal second writer held at commit while a single reader performs candidate/history work can exercise snapshot coherence; existing v3 preparation must reject the resulting stale proposed history after that writer commits. Test synchronization must use explicit events, no sleeps or mocked authority. New unclaimed-operation preview remains pre-intent, not a recovery API or lock spanning provider work.

Worker integration checkpoint: full native rendered comparison passes for both '.' and 'sources/api' roots with real typed catalogs/workspace/topology, native planners, rich local task/traceability/deferral/amendment/verified records and imported history; external memory adapters/audits remain deterministic fixtures. Initial policy fixture omitted linked uncataloged contracts.md and was corrected without production changes; initial exception-audit fixture was corrected to native shape. Amended focused run4pass. Actual guarded non-graph source publication passed with final captured manifest and graph rendering equal to retained projected sources; graph itself remains unwritten/unsealed. Latest purity/ownership/sanitization/import-order batch7pass. Docs/self-review/once-only eight-module covering and independent review remain outstanding.

Task 1 implementer DONE at7a2c2d4b66b6ecbe6d943a53dbc7bced4600f615. Root read all286 report lines. Actual first RED1fail0.69s -> GREEN1pass0.67; complete3pass0.24; boundary101pass0.33; new native/unavailable/guarded batch3fail3pass0.78 (fixture audit shape and two omitted linked contracts.md selections). Diagnostic standalone first failed on /var symlinked ancestor, resolved temporary path rerun isolated exactly omitted policy records/digest; neither was a verification pass. Scoped fixture corrections4pass0.76; ownership/purity/import batch7pass0.89. Final additions covered once across exact eight modules:712pass9.15s, pristine. Tested staged tree5ea945dced266fce4c68cb87406c54bca5d4013a; final commit tree967d73ad421974cf81178042fa361ac167026597 differs only by report (root read-only diff-stat confirms). No production amendment after first GREEN or executable/test/doc amendment after covering. No unchanged test rerun.

Original-BASE review package60f5f8bb..7a2c2d4b.diff84030bytes generated. Fresh /root/review_captured_identity_graph_assembly Astra/high dispatched with128-line brief286-line report, full global constraints and read-only/full bounded non-overlap diff discipline. No remaining integration was claimed complete; graph sealing/publication and live activation remain absent. Review pending; do not dispatch next implementation before the gate.

Fresh review complete: spec compliant, task quality Approved; no Critical/Important/Minor findings. Reviewer read full1430-line package once in bounded nonoverlap chunks, with no truncation/changed-file rereads/git/test reruns or mutations. Named unchanged checks: shared scope validator, identity projection deep ownership, manifest carrier/selection entry point. Historical RED/tested-tree chronology cannot-verify resolved by root's witnessed pre-production RED notification, recorded partial/final test updates, complete286-line report and independent report-only diff-stat between tested staged tree and final commit. No missing evidence or implementation gap; no repeat verification required.

Task 1: complete (commits60f5f8bb..7a2c2d4b, review clean). Exact captured-source graph assembly remains inactive and does not publish itself. No new product ruling in this task. All ledgers retained through whole-branch review and exhaustive final ruling report. Next planned integration is coherent candidate preview against one authority snapshot; separate plan/ledger owns its occurrence-association ruling and future implementation.
