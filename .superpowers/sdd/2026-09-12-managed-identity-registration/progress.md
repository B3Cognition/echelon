# SDD ledger — plan: docs/superpowers/plans/2026-09-12-managed-identity-registration.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / approved migration design | Newly initialized managed specs versus historical reconciliation. | Explicit fresh-only genesis enrollment; no existing/imported/allocated-spec activation or stopped-run conversion. This immutable first-run record is not a mutable active-run pointer; subsequent transitions remain required. Ruling25 below records that boundary. |
| 1 / source context | Current source receipts change after acceptance, but original registration remains. | Bind both original source operation and original manifest SHA; validate original empty selected spec root, never require today's head/root to remain initial. Root added independent original SHA before dispatch to prevent changed context/hash alone from rebinding genesis. |
| 1 / namespace | Codec request includes namespace while store has retained workspace/epoch. | Exact canonical strings must match actual authority; no caller value replacement or inferred initialization. Wrong/copy namespace fails. |
| 1 / operation ownership and audit | Common operation gate inserts new operation; full audit rejects orphan operations. | Full first-enrollment audit precedes new operation insertion in the SAME transaction. Exact retries use bounded original-record checks before fresh-only conditions. No half-assembled audit or transaction split. |
| 1 / fresh-only validation | First enrollment rejects every allocated/materialized spec, including unused gaps; source registrations allowed. | Tests retain allocated/terminal/import/publication history and assert no reset. Other specs' valid histories remain allowed. Present empty selected root required; unrelated dependency content and root modes preserved. |
| 1 / schema and frozen compatibility | New method/table requires schema-aware full audits across store/snapshot/backup/upgrade. | FrozenSCHEMA_V5, schema6 additive one table+one partial index; marker/user_version1. Managed audit explicit parameter; source5/6,publication4/5/6,binding3/4/5/6. No source/journal/lifecycle edits or old missing-table fallback. |
| 1 / current read and bootstrap costs | Explicit enrollment requires full integrity audit, ordinary registry lookup must not scan identity history. | First bootstrap audit is intentional; existing exact retry/current read uses indexed registration/source/parent checks. EXPLAIN and SQLite-authorizer coverage required. History snapshot stays source/registry-wire independent but full-audited. |
| 1 / test boundaries | First regression needs real capture/source registration before missing new API, codec import would otherwise fail too soon. | Test reads missing method before importing new request module. Pure/SQLite cases not POSIX-skipped. SQL faults/process collision/after-COMMIT uncertainty and frozen real journals included. |
| 1 / covering tests | Additive registry/schema routing does not change allocator/import/common guard behavior. | Ten named modules, deselect only existing inspected million-record test; prior schema5 capacity result remains labeled schema5. No new schema6 capacity assertion. No full/live/install/repeated tests. |
| 1 / live integration | Persisted genesis is required input for future runtime checks, not enforcement itself. | No state/provider/controller/CLI/retarget/graph/memory edits or activation. Future owner must consult registry and prevent stripped metadata downgrade, bind subsequent run transitions, and validate complete source/semantic scope. No producer proposal or graph API selected. |

Ruling: Initial managed enrollment is an immutable genesis record for a fresh, unallocated spec and its first run, bound to the original accepted source operation and manifest hash; it cannot relabel an existing spec or serve as a mutable current-run pointer — the approved migration starts with newly initialized managed specs and requires explicit historical reconciliation, while later source heads must not invalidate the initial binding — if wrong, extend the explicit enrollment/retained run-transition protocol and its compatibility tests rather than overwriting genesis, element IDs, revisions, graph keys, canonical files or existing live runs. This is Ruling25 in the chronological final handoff.

Main fully read the108-line completed plan and approved broad design, reviewed all interfaces/receipt fields and the table/index/audit/fresh-only/retry test relationships above. Last audit-order clarification reviewed directly in patch. One task because request, registry, schema and frozen audit routing share durable invariants. Initial source contexts and legacy records are not retroactively reinterpreted as managed. Most-capable implementation/review warranted for namespace/enrollment/schema/atomic retry semantics. Existing isolated worktree verified; no new worktree or external action.

Task1 pending dispatch after plan commit/brief extraction. Root owns this plan and ledger. One implementer/no children, independent full original-base review, no full-suite reruns on unchanged code. Preserve all artifacts through final broad review and exhaustive ruling handoff.
