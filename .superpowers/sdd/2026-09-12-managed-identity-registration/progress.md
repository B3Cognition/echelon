# SDD ledger — plan: docs/superpowers/plans/2026-09-12-managed-identity-registration.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / approved migration design | Newly initialized managed specs versus historical reconciliation. | Explicit fresh-only genesis enrollment; no existing/imported/allocated-spec activation or stopped-run conversion. This immutable first-run record is not a mutable active-run pointer; subsequent transitions remain required. Ruling25 below records that boundary. |
| 1 / source context | Current source receipts change after acceptance, but original registration remains. | Bind both original source operation and original manifest SHA; validate original empty selected spec root, never require today's head/root to remain initial. Root added independent original SHA before dispatch to prevent changed context/hash alone from rebinding genesis. |
| 1 / namespace | Codec request includes namespace while store has retained workspace/epoch. | Exact canonical strings must match actual authority; no caller value replacement or inferred initialization. Wrong/copy namespace fails. |
| 1 / operation ownership and audit | Common operation gate inserts new operation; full audit rejects orphan operations. | Full first-enrollment audit precedes new operation insertion in the SAME transaction. Exact retries use bounded original-record checks before fresh-only conditions. No half-assembled audit or transaction split. |
| 1 / fresh-only validation | First enrollment rejects every allocated/materialized spec, including unused gaps; source registrations allowed. | Tests retain allocated/terminal/import/publication history and assert no reset. Other specs' valid histories remain allowed. Present empty selected root required; unrelated dependency content and root modes preserved. |
| 1 / schema and frozen compatibility | New method/table requires schema-aware full audits across store/snapshot/backup/upgrade. | FrozenSCHEMA_V5, schema6 additive one table+one partial index; marker/user_version1. Managed audit explicit parameter; source5/6,publication4/5/6,binding3/4/5/6. No source/journal/lifecycle edits or old missing-table fallback. |
| 1 / current read and bootstrap costs | Explicit enrollment requires full integrity audit, ordinary registry lookup must not scan identity history. | First bootstrap audit is intentional; existing exact retry/current read uses indexed registration/source/parent checks. EXPLAIN and SQLite-authorizer coverage required. History snapshot stays source/registry-wire independent but full-audited. |
| 1 / test boundaries | First regression needs real capture/source registration before missing new API, codec import would otherwise fail too soon. | Test reads missing method before importing new request module. Pure/SQLite cases not POSIX-skipped. SQL faults/process collision/after-COMMIT uncertainty and frozen real journals included. |
| 1 / covering tests | Additive registry/schema routing does not change allocator/import/common guard behavior. | Ten named modules, deselect only existing inspected million-record test; prior schema5 capacity result remains labeled schema5. No new schema6 capacity assertion. No full/live/install/repeated tests. |
| 1 / live integration | Persisted genesis is required input for future runtime checks, not enforcement itself. | No state/provider/controller/CLI/retarget/graph/memory edits or activation. Future owner must consult registry and prevent stripped metadata downgrade, bind subsequent run transitions, and validate complete source/semantic scope. No producer proposal or graph API selected. |

Ruling: Initial managed enrollment is an immutable genesis record for a fresh, unallocated spec and its first run, bound to the original accepted source operation and manifest hash; it cannot relabel an existing spec or serve as a mutable current-run pointer — the approved migration starts with newly initialized managed specs and requires explicit historical reconciliation, while later source heads must not invalidate the initial binding — if wrong, extend the explicit enrollment/retained run-transition protocol and its compatibility tests rather than overwriting genesis, element IDs, revisions, graph keys, canonical files or existing live runs. This is Ruling25 in the chronological final handoff.

Main fully read the108-line completed plan and approved broad design, reviewed all interfaces/receipt fields and the table/index/audit/fresh-only/retry test relationships above. Last audit-order clarification reviewed directly in patch. One task because request, registry, schema and frozen audit routing share durable invariants. Initial source contexts and legacy records are not retroactively reinterpreted as managed. Most-capable implementation/review warranted for namespace/enrollment/schema/atomic retry semantics. Existing isolated worktree verified; no new worktree or external action.

Task1 pending dispatch after plan commit/brief extraction. Root owns this plan and ledger. One implementer/no children, independent full original-base review, no full-suite reruns on unchanged code. Preserve all artifacts through final broad review and exhaustive ruling handoff.

Task1 dispatched /root/identity_managed_registration Astra/high, BASEe637706429a3b0a3b3180736c2a44cc0ce54920a,85-line extracted brief plus full implementer template/report contract. Root clarified original-source SHA/genesis versus current-head semantics and full-audit-before-new-operation order; no source helper or live integration edits. Required real missing-method RED notification before production, ten-module final covering run excluding only inspected unchanged million-record case, fresh independent review after all commits/report. Active worker only; prior source-context phase complete at7e7a177a and checkpoint39f87e96, do not repeat it.

Required actualRED witnessed from worker before production: pytest-q tests/unit/test_element_identity_managed.py reached missing store.register_managed_identity after successful real secure-POSIX capture and sequence0 source registration; AttributeError,1failed0.26s. Initial fixture-discovery failure was setup-only and is not claimed as behaviorRED; worker added the same scoped secure-POSIX fixture pattern used by source tests before the genuine regression. Production untouched at notification; implementation now proceeds.

Interim focusedGREEN1passed0.25s: real enrollment/reopen test. Worker added focused request/store modules and schema6/audit routing. Full first-enrollment audit before insertion in sameTX; original source manifest/hash binding plus existing source-read current integrity checks without identity child-history scans. Codec, corruption, later-history retries, transaction/process and independent frozen-schema coverage expanding. No final verification/review yet.

Interim expanded suite114passing+4frozen-fixture import setup failures; package import corrected, focused frozen checks46passed72deselected2.00s. Real source file publication then seven-family creation/revision/pending exact retry passes. SQL insert/COMMIT rollback, separately labeled after-actual-COMMIT recovery,2spawned-process race,21corruption variants through read/audit/snapshot/backup/restore and no-child-history authorizers pass. Audit count omission had observed productionRED1failed80passed, corrected routing81passed. Fixture-import failures are setup-only, not productionREDs. Worker reports no scope/source-module changes. Final ten-module run not yet started; await exact full report before review.

Managed/admin/source focusedGREEN249passed18.84s, pristine; additional real schema6 backup/restore/upgrade genesis preservation1passed0.33s. Existing current-schema assertions updated after observed5expectedfailures/123passes. Worker self-review complete before final covering run; no production fixes since audit-count routing. Single required ten-module run starting with only inspected test_million_identity_capacity_uses_indexed_lookup_and_counter deselected. No schema6 million-capacity claim; wait for full report/commits before review.

During final covering run against stagedtreee0f5e8dc1f5bbf510a96586476a6b842c131598f, worker self-review found retained import operations can have no child rows (valid definitions=() or removed opaque import rows), so child-table-only emptiness could enroll historical spec. Root confirmed original fresh-only requirement: first-enrollment-only retained non-source operation check after full audit/before insertion, exact retries unchanged; preserve legacy import behavior. Actual focusedRED then affected managed-module coverage only, no repeated source/journal/capacity suite. Report ten-module code point separately from this production amendment. No broader scope or new policy ruling.

Final ten-module result at stagedtreee0f5e8dc1f5bbf510a96586476a6b842c131598f:759passed1deselected63.29s, pristine. Actual post-run fresh-onlyRED2failed0.37s (empty import and removed legacy rows incorrectly enrolled). First-enrollment retained non-source operation guard added after audit/before insertion; affected managed moduleGREEN124passed6.05s, pristine. No broad/capacity repeat. Worker writing final report/committing task-onlyfiles, root ledger remains unstaged. Do not label759run as final amendedcode; await DONE and full report before review.

ImplementerDONE commits0daa82c1 implementation andf66cc81a195589bf04681e84935a23adccc153bb report. Root read FULL208-line report; whole original-base diffcheck clean. Only root-owned progress ledger dirty. Report identifies initial fixture setup failure and immutable-subject/frozen-package fixture corrections separately from real productionREDs; final124-test code has no later production edits. Full review package105,982bytes covers BOTHcommits. Fresh /root/review_identity_managed_registration Astra/high dispatched brief/report/package plus exact global constraints and named source-genesis/namespace/first-audit-order/frozen-routing/bounded-read/backup risks. Readonly/nochildren/no reported-test reruns; exact full bounded coverage required. Independent gate pending, not complete.

Independent /root/review_identity_managed_registration Astra/high approved spec and task quality; no Critical/Important/Minor findings. FULL85-linebrief,208-linereport,105,982-byte1792-linediff in nonoverlapping1–360/361–730/731–1070/1071–1330/1331–1590/1591–1792, no truncation. Named unchanged checks: source original/current head validation, globaloperation/pending/child ownership, TX/namespace and reused text/JSON/path validators. No tests/writes/git/subagents. Root read complete review. Only cannot-verify historical execution resolved by root's initialRED/checkpoint observations and full report; no schema6capacity or liveenforcement claim. No fix loop, no deferred findings for this task.

Task 1: complete (commits e6377064..f66cc81a, review clean)

Preserve workspace and Ruling25 for exhaustive final handoff. Next approved integration is managed state metadata preservation/ownership; no live enrollment, runtime authority classification, provider proposal protocol, current-run transition or graph staging API has yet been implemented or selected by this task.
