# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-memory-graph-contribution.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / graph models | Existing graph classes stay in spec_graph; native plan records have the same seven string fields in two established types. | Reuse class identities, accept exact supported rows at new boundary, local imports in legacy adapters if needed. No generic graph engine or new drawer identity. |
| 1 / structural scope and artifacts | Previous pure builder validates spec component/lifecycle inline and exposes pure artifact record/required helpers. | Factor only that existing validation into _validate_scope and reuse it unchanged. Reuse artifact roles/required construction; no fake missing capture or duplicated scope policy. |
| 1 / local versus memory ownership | Local structure precedes canonical support overrides, evidence and RE contributions. | Return contributed/replaced records only, with explicit known endpoint IDs. Canonical main spec is not re-added; preserve supporting/task overrides and legacy domain ordering. |
| 1 / source-set and artifacts | Legacy source-set digest comes from snapshots' metadata, while legacy artifact nodes use actual Path reads. | Retain legacy acquisition/coercion behavior, share pure record digestion. New public sources supply exact bytes and enforce row hash coherence; do not route legacy through stricter input validation. |
| 1 / audit normalization and drawers | Legacy normalization coerces values, but drawer issue handling accepts lists and strings only. | Share transformations without moving normalization before legacy drawer interpretation. New strict tuple audit can convert to ordinary list-valued report internally. |
| 1 / RE report | Existing RE report is global, then projected to selected planned drawers before normalized digest/status. | Retain projection sequence and exact count/error/default rules. No new audit status policy or count-equality requirement. |
| 1 / unavailable partial rows | Existing exception branches can retain partial planned rows but use a zero-count unavailable report. | Preserve this observation; input count shape checks do not pretend counts or reports prove planner/storage completeness. |
| 1 / endpoint projection | Drawer source depends on known requirement key and artifact kind, otherwise Artifact key. | Shared drawer-record function consumes IDs, not dummy GraphNodes; missing endpoint rejects. Contribution does not copy unrelated nodes or certify full graph. |
| 1 / public validation | New supplied values must be type/path/hash coherent, while legacy helpers use existing duck typing. | Exact public DTO/container/native-row types, unique sources/drawers/endpoints, byte hashes, bounded outside-handler errors. Legacy helper signatures/defaults/coercions remain wrappers over shared construction. |
| 1 / memory context | Native planners already accept supplied bytes and existing explicit wing; live adapters own context/storage. | Function consumes planned rows and captured audit only. No context/wing/palace derivation, planner rerun, collection lookup, writer or fabricated audit. |
| 1 / first RED | Native canonical planner plus existing actual drawer projection can succeed without new module. | Assert complete existing drawer properties/edge before intended missing-module import. Pure first regression needs no POSIX skip; root notification precedes production. |
| 1 / verification | Shared memory helpers affect all three legacy domains, graphs/audits/identity. | Eight exact discovered modules once after focused native-planner/legacy/identity composition and purity checks; no actual memory store mutation or unchanged repeats. |
| 1 / broader design | Complete captured source selection, memory acquisition/reconciliation and RE/topology composition are not yet integrated. | Explicit observation contribution only; current storage/semantic/accepted source claims remain prohibited until complete owner and rollout. |

Root read full current graph memory helpers earlier, native plan record/functions, canonical/support/evidence/RE snapshot shapes and metadata, actual memory audit/acquisition/reconciliation call paths, public artifact helpers and spec scope validation, approved design and relevant skills. The existing ordinary canonical audit still rereads filesystem through reconcile_drawers; the new function must never call it or falsely claim to fix acquisition. Existing artifact memory auditing also has its own policies; none are changed here. These are integration prerequisites, not new findings against changed code or selected follow-up APIs.

No new product-policy ruling: this extracts existing observational graph semantics and adds a strict supplied-value boundary without changing acceptance. Captured local structure completed f429a965/82f97091,310 covering plus final test-only1pass, clean independent review and checkpoint2873d6e5. Root owns plan/ledger; retain all task artifacts until final whole-branch review and exhaustive rulings handoff. Astra/high implementation/review selected for multi-domain compatibility and value/authority boundary judgment. One implementer, no child agents; no subsequent task selected.

Task 1 dispatched fresh /root/captured_memory_graph_contribution Astra/high at BASE3552e442f523de925b884998d0f98cfaab3354c5 with116-line brief, full implementer/report contract and exact environment/covering constraints. Required native planner and existing drawer full-properties/edge success before missing-module RED, root notification before production. Strict public values versus unchanged legacy duck-typed observations emphasized. Root ledger intentionally dirty/excluded. No next task selected.

Required actual RED witnessed before production: exact parent absolute pytest and this worktree, tests/unit/test_spec_graph_memory.py::test_captured_memory_retains_native_plan_after_input_damage -q =>1failed0.57s. Real native planner returned FR-1000000; all existing helper drawer property and STORED_AS edge assertions passed before ModuleNotFoundError for echelon.spec_graph_memory. Worker starts shared extraction after root notification.

Named GREEN same exact command1pass0.55s. Expanded new module checkpoints37pass0.57s then212pass0.66s after validation, ownership and pure tripwires. Worker reports no compatibility ambiguity; shared drawer generator yields node then edge to preserve legacy incremental dictionary updates on missing-endpoint failure. Native domain parity, legacy coercion/partial continuation, import-order and real identity composition remain before once-only covering.

Native parity exposed a plan defect: legacy RE projects a successfully returned audit (even unavailable), but exception-created _UnavailableMemoryReport bypasses projection and retains zero counts/global exception error. Initial public DTO lacked origin and plan always projected, making exact replay of both outcomes impossible. Root verified the existing try/except placement previously read and paused the not-yet-run covering suite for a regression-first plan amendment. Worker separately reported6fail221pass fixture-only checkpoint because supplied lifecycle=build had no legacy build marker; adding run-history.json to fixture yielded named six-case canonical continuation6pass0.59s, no production policy change.

Ruling: 29. Retain explicit returned-versus-exception acquisition origin in the new GraphMemoryAudit carrier, and project RE only for returned observations — equal status strings cannot encode the existing distinct error/count meaning, and captured replay must preserve knowledge rather than silently rewrite it — reversal cost: revise this inactive DTO/adapter and exact exception/returned parity tests; no existing graph wire, normalized payload, drawer IDs, identity revisions, receipts, canonical files or live deployment are rewritten. Origin is a caller observation, not proof of acquisition. Exception origin requires the exact existing fallback shape so a pass report cannot opt out of projection by claiming an exception. Root updated plan/brief with mandatory origin and targeted regression; no new authority or audit status policy.

Ruling29 regression RED witnessed before production amendment: exact parent absolute pytest, this worktree, tests/unit/test_spec_graph_memory.py::test_returned_unavailable_and_exception_origin_match_real_legacy_re -q =>1failed0.62s. Both actual legacy RE branches first passed independent full-record/hash assertions with native rows retained (exception zero counts/class error; returned-unavailable selected counts/filtered errors), then TypeError for missing GraphMemoryAudit origin keyword. Worker proceeds to mandatory origin/fallback validation. Once-only covering has still not run. Unrelated identity-composition fixture assertion for nonexistent spec_input_hash was corrected to existing verified provenance contract, without production change; full chronology due in report.

Worker DONE 2303891f67d0b8628a7962306cbe1b8ccfa2a261 implementation and f1a6972a report-only. Root read full report through EOF in bounded chunks (initial combined truncation repaired). Origin parity/fallback/invalid-origin GREEN18pass0.59s; expanded new module251pass1.10s. Identity fixture also corrected lowercase implemented expectation to existing parser-normalized IMPLEMENTED; final named composition1pass0.70s. Once-only eight-module covering542pass8.21s, pristine, staged tree711ef5f1a666d330a4358ecea4048161c83fac8f. After covering, only extra EOF blank line removed; named original/native-origin regressions2pass0.56s, clean diff-check; final implementation treee4467f10210ba6bd528198c69445c0c7031ba547. Report records exact unchanged blobs and EOF-only module difference. No later production/test/public-doc change and no unchanged rerun. Root plan/ledger excluded.

Fresh Astra/high /root/review_captured_memory_contribution dispatched with amended brief, complete report, original BASE3552e442..f1a6972a two-commit133185-byte review package, copied binding constraints and full task-review template. Read-only both-verdict gate, bounded nonoverlapping full reads, named unchanged-code risks only, no already-passing test repeats or child agents. No following implementation selected or dispatched.

Independent review returned spec-compliant and quality Approved, no Critical/Important/Minor findings. Reviewer read complete119-line brief,464-line report and2537-line package, recovering initially truncated combined-output portions; no changed-file rereads, git re-derivation, test reruns, mutations or subagents. Named unchanged check: source path validator rejects string subclasses and invalid UTF-8 at squad_source_snapshot.py:51. Cannot-verify RED timing resolved by root's actual pre-production notifications recorded above for both cycles. Cannot-verify complete acquisition/selection/sealing/activation is an explicit remaining integration, not claimed by this partial contribution. Ruling29 implementation complete; retain for exhaustive final handoff.

Task 1: complete (commits 3552e442..f1a6972a, review clean)
