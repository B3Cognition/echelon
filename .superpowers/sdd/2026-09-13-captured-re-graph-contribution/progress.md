# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-re-graph-contribution.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / existing graph owner | _add_re_topology interleaves reads and mutations; moving all acquisition ahead of projection would change failure state. | Extract at existing per-artifact/per-source transformation boundaries; preserve read order, early returns and partial updates. |
| 1 / descriptors | Existing physical validator uses typed taxonomy, ownership prefix and byte hash. | Reuse existing native record and pure taxonomy/prefix/path helpers; new strict byte boundary does not replace physical registry admission or select descriptors. |
| 1 / source observations | Legacy independently compares workspace, semantic and topology physical paths. | New DTO retains all compared path values and source IDs, validates only their supplied equality; actual physical validation stays legacy-owned. No inferred existence or accepted source claim. |
| 1 / topology receipts | Physical registry uses source_storage_key, not necessarily literal source_id, and validates provider payloads. | Reuse exact native receipt and pure storage-key ownership, supplied content/hash equality; do not reimplement provider receipt validation or assume display ID is storage key. |
| 1 / artifact records | Legacy updates existing artifacts and skips missing ones but groups source descriptors before node checks. | Explicit preceding Artifact records, missing-node skip and group semantics preserved; output contains replacements only. Preceding STORED_AS source IDs are observations, not current storage proof. |
| 1 / nested graph properties | Existing identity copier supports Mapping proxies, tuples/lists and finite scalar values. | Move only common copier to a neutral narrow module and retain original identity wrapper/error behavior; use same detachment policy in new builder, no second copy algorithm. |
| 1 / ADR title | Legacy strict text read catches OSError/UnicodeError and falls back to stem. | Share text transformation; actual legacy read/catch remain in place, pure supplied invalid bytes use same fallback. No earlier or extra decision reads. |
| 1 / composition | Local, canonical/evidence/RE memory, RE topology then identity is existing order. | Tests compose real contributions and history without claiming full source discovery or publication. No complete graph wrapper or live consumer introduced. |
| 1 / validation versus compatibility | Strict exact public DTOs coexist with duck-typed legacy test/acquisition values. | New public validation only; legacy wrappers use shared pure records without strict DTO construction. |
| 1 / required RED | Native physical descriptor validation and existing workspace-decision graph owner can run before new module exists. | Assert complete nodes/edges and actual title first; intended absent-module error then notify root before production. |
| 1 / covering | Shared copier affects identity, shared RE helpers affect old graph. | Eight named modules once after focused tests, with actual-tree/post-amendment accounting; no repeated unchanged verification or real collection. |
| 1 / approved scope | Approved design requires preserved graph keys/history and eventual captured publication. | This is the next offline graph contribution, not activation or completion. Complete selection/acquisition/sealing and managed/runtime/producer/repair integration remain explicit. |

Root read complete approved design across bounded chunks (recovered truncated checkpoint middle), complete existing _add_re_topology, source/path/title functions, build ordering, descriptor type/pure and physical validators, native source/index and topology receipt/index shapes, topology physical receipt ownership and existing identity property copier. Existing typed graph/topology fixtures were inspected for native versus supplied boundaries. Earlier captured-memory phase is complete at 8053967f,542 covering plus2 EOF-cleanup scoped tests, independent clean spec/quality review. No following task is selected.

Self-review found no new product-policy ruling: strict observation DTOs and source consistency checks do not supply physical or semantic authority, and all legacy interpretation/acquisition remains owned by existing code. Native topology_generation is the observed topology index generation, not a replacement derived from a per-source record. Scope/source paths use existing pure rules rather than new path interpretation. Shared copier relocation is limited to its current value semantics. The plan is one independently testable extraction gate; remaining approved integrations are enumerated rather than asserted complete.

Use fresh Astra/high implementation and review for interleaved registry/graph compatibility, source ownership distinction and native type/value boundaries. One implementer, no child agents. Root owns plan/ledger and retains all artifacts through final whole-branch review and exhaustive rulings handoff. User approved automatic continuation; no new approval needed for this inactive in-worktree step.

Task 1 dispatched fresh /root/captured_re_graph_contribution Astra/high at BASEb6f0fb8dd7cdd943a629c3132c7258b51c13b20d. Worker receives117-line brief, full implementer/report/TDD/verification contract and explicit native topology index generation observation; no subagents, no whole-plan read, no implementation outside named scope. Actual RED and root notification precede production; once-only eight-module covering and complete failure/amendment chronology required. Root ledger intentionally dirty and excluded. No following implementation selected.

Actual required RED witnessed before production: exact parent absolute pytest and this worktree, tests/unit/test_spec_graph_re.py::test_captured_re_retains_actual_workspace_decision_after_file_change -q =>1failed0.23s. Native physical descriptor validation succeeded; existing _add_re_topology full node/edge assertions and real ADR title passed, then ModuleNotFoundError echelon.spec_graph_re. Worker starts scoped production after this root notification.

Ruling: 30. Preserve exact `.` in captured source-root path observations, without admitting it as an artifact/receipt path or changing RE source-ID policy — the blanket non-dot path requirement would reject an existing valid monorepo root: _canonical_source_path yields `.`, RE index parsing explicitly allows_dot, and topology_model.normalize_source_root_path accepts it — reversal cost: revise this inactive supplied source-root validation and compatibility tests; no graph keys, native registry policy, identities, revisions, receipts, canonical files or live deployment change. Physical existence/authentication remains outside the pure function. Root amended plan/brief and notified worker before covering; regression must exercise actual legacy root-source behavior before the corrected public call. A RE source ID such as root is valid; source_id=`.` remains rejected by the existing typed descriptor policy. Do not fake an otherwise unreachable storage-key variant in tests.

Worker reports root-path regression reached decisive RED after real typed RE catalog, workspace config and real topology registry passed actual legacy node/title/receipt-edge assertions. Topology fixture index generation9 differs from source receipt generation3, covering that required distinction. Two earlier fixture-only failures (test import path and unsorted native catalog) were corrected before decisive RED; exact count/command/time and complete chronology requested for ledger/report. Worker amends source-root normalization only; covering not yet run.

Exact root-path regression used parent absolute pytest 'tests/unit/test_spec_graph_re.py::test_native_registry_and_legacy_source_root_match_captured_values[.]' -q from this worktree: fixture import1failed0.22s, fixture catalog sorting1failed0.25s, decisive bounded SpecGraphError1failed0.24s. After source-root-only amendment, whole named parameterized regression2pass0.24s. Native index/receipt generation distinction remains asserted. Covering still not run.

Worker focused expansion94pass0.31s; added native-acquisition tripwires, two import orders, five legacy early-return cases and semantic/receipt partial-mutation order tests10pass. Self-review found extracted annotation used a damaged node.id rather than the original dictionary lookup artifact key; regression-first1failed10passed0.48s, then explicitly passed artifact_id to shared helper, scoped5pass0.23s. This restores existing behavior, not a new policy ruling. Offline structure+memory+RE+identity composition remains before once-only covering.

Composition now1pass0.71s: actual native structure/canonical and RE memory/RE/retained history. Fixture corrections were native RE planner metadata canonical=True and scope=reverse-engineering, and existing identity behavior marking requirement STORED_AS unassessed while leaving RE-artifact STORED_AS unchanged. Self-review caught moved decision-key derivation after title reads; bounded legacy ordering regression1failed0.25s, restored shared _decision_id before read, scoped7pass0.23s. Docs and exact native-record subclass rejection cases added. Worker ready for once-only eight-module covering; complete fixture and self-review chronology due in report. No policy change or additional scope.

Once-only eight-module covering681pass8.50s exit0 pristine, staged tree96ac1f1bbfb23ad7ffbe61c503e10045788fbd4f with six production/test/public-doc files. No later code/test amendment reported. Worker writing full chronology/report and commits. Root requested the exact amended brief be force-added as requirements-only after covering (plan/progress remain excluded), with tested-tree versus final-commit accounting and no unchanged test repeat.

Worker DONE b70dfa9c6a42f9a421d111b2808bf60d4a4da26f implementation (exact covering tree96ac1f1bbfb23ad7ffbe61c503e10045788fbd4f), 7cfaf0a8 amended brief/report-only. Root read full318-line report in two nonoverlapping chunks through EOF. Initial original-regression GREEN1pass0.21s; all later fixture/self-review/covering chronology is retained. No production/test/public-doc changes after covering, no warnings/skips, clean staged/working diff-check. Actual typed RE/catalog/config/topology acquisition is distinguished from first-RED/order fixtures and deterministic memory audit observations. No real memory collection or live writer.

Fresh Astra/high /root/review_captured_re_contribution dispatched with amended brief, full report and original BASEb6f0fb8d..7cfaf0a8 two-commit107803-byte package, copied binding constraints and complete reviewer-template requirement. Read-only both-verdict gate; full bounded nonoverlapping input reads, named unchanged checks only, no repeated passing tests or child agents. No next implementation selected or dispatched.

Review returned spec noncompliant / quality Needs fixes: one Important, no Critical/Minor. New public builder sorts descriptor strings, but legacy sorts Path components: valid re/workspace/a/decision.md must precede re/workspace/a-b.md, and source-group relationship order has the same issue. Reviewer confirmed with one new in-memory exact-environment check, not repeated passing tests. Full117-line brief,318-line report and1918-line package read in bounded nonoverlapping chunks; no truncation, changed-file rereads, unchanged-source inspections, git re-derivation, mutations or subagents. Cannot-verify historical chronology/tree is resolved by root's witnessed messages and exact implementation tree/report accounting; no rerun warranted. Root read receiving-review skill and verifies finding matches the explicit same-legacy-order requirement, not a policy conflict.

Task 1: fix round 1/5 dispatched to original /root/captured_re_graph_contribution via followup_task at FIX_BASE7cfaf0a8033430a78b25443f282c3b830193410d (0 addressed,1 open — component-wise path ordering). Actual legacy plus independent workspace/source ordered-record regression RED first, minimal pure path-component sort, scoped module and legacy spec_graph covering only, full appended report/tested-tree accounting. No next task dispatch until fixed and fresh scoped re-review passes.

Root additionally confirmed b70dfa9c implementation commit has reported covering tree96ac1f1bbfb23ad7ffbe61c503e10045788fbd4f by read-only git show, no test repeat. Fix-round actual RED witnessed: named test_captured_path_component_order_matches_actual_legacy_relationships workspace/source cases, exact parent pytest/worktree,2failed0.28s. Both native physical descriptor and actual legacy ordered-node/edge assertions succeeded first; captured workspace edge0 and source edge1 incorrectly picked a-b.md before a/decision.md. Source case includes intervening a/summary.md DESCRIBED_BY. Worker applies one-line PurePosixPath sort-key correction after notification.

Fix round1 worker DONE7cf1b3139a032f6f9491f99bba8af006e142f649 code/test and c9689f36 report-only. Root read full appended report319–EOF. Same named GREEN2pass0.22s; requested two modules144pass1.13s pristine; tested/fix tree6ff30cedf21c51efa40a9b3387e5aa3081e35d7e, clean staged diff-check, no later code/test changes. Independent expected full ordered nodes/edges/rendered contribution bytes cover workspace/source and interleaved nondecision. No eight-module/full-unit rerun. Fresh Sol/high /root/rereview_re_component_order receives full brief/report and FIX_BASE7cfaf0a8..c9689f36 two-commit13814-byte scoped package and exact finding, read-only scoped-review template. No next implementation until finding verdict.

Fresh scoped re-review: component-wise ordering ADDRESSED, no new breakage or out-of-scope observations. Full115-line template,117-line brief,398-line report and240-line fix package read; no changed-file rereads/git re-derivation or tests rerun. Named unchanged check spec_graph.py:387–470 confirmed actual Path sorting and retained order within source groups. Fix is one pure sort key, independent ordered workspace/source regression covers intervening nondecision. Root has both original review verdicts plus resolved sole Important; historical execution/tree item reconciled above. Ruling30 implemented; retain for final exhaustive handoff.

Task 1: fix round 1/5 (1 addressed,0 open — component-wise path ordering; commits 7cfaf0a8..c9689f36)
Task 1: complete (commits b6f0fb8d..c9689f36, review clean)

## Exploratory integration observations (not a selected next task or protocol)

Root read current source-context prepare/validate_plan and existing completion/mining/graph call sites while worker implements. Accepted source contexts require exact retained predecessor manifest and unchanged selected tree/file roots. Therefore full per-publication external graph dependencies cannot simply be added to an existing context or silently rebased when external RE/config changes. Candidate design must distinguish canonical ownership from read dependencies explicitly before wiring; no subset/rebase/new context policy is selected here.

Existing squad _publish_and_finalize still calls prepared.publish after existing quality/product-input authentication; mining occurs after durable external-marker clearance. Existing standalone graph/refresh/retarget CLI paths use live build_spec_graph and independent writes; they also need a managed publication owner before activation, not just the squad path. spec_graph_audit compares live memory receipts and treats required unavailable as an error; caller-supplied observations cannot silently become fresh audit proof. No memory timing or audit-acceptance change is selected.

Root also read topology_registry _parse_index/_parse_source: configured-source values can be supplied, but provider artifacts and source receipt are still physically hash-read; receipt generation may be older than index generation. RE registry typed descriptor enumeration similarly reads manifest/catalog files and validates physical descriptors. workspace_model.load_workspace_source_declarations parses only config after authenticated read, while discover_workspace may inspect source markers/git/directories/content counts for implicit or empty declarations. Full capture must account for the actual selected inputs and physical source validation rather than treating declaration-only parsing as equivalent discovery. No captured registry parser, directory-observation API, filesystem mirror, implicit-mode restriction or generic resolver callback is selected in this task.
