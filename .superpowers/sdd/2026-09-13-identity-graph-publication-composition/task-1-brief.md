### Task 1: characterize real combined source, graph and identity recovery

**Files:** Create `tests/unit/test_identity_graph_publication_composition.py`; document measured composition behavior and remaining boundaries in `docs/element-identity-storage.md`. Root owns plan/brief/ledger. Do not modify existing production or tests. If a required real primitive fails, report the exact gap to root before choosing a production fix or changing the expected contract.

This is a test-only integration/characterization task, not a new API implementation. Do not invent a missing API or manufacture a RED failure. Add the first actual end-to-end fixture/test and report its genuine result; then add the specified negative and interruption cases. A failing assertion must expose a real fixture error or production gap, not be silently weakened. Follow normal native-fixture and verification discipline; no live provider/storage service.

**Native interfaces:** Existing IdentityStore initialize/reserve/apply_lifecycle/record_reference_claims/source-context registration and reads, preview_identity_candidate, prepare/apply/release/read/pending identity publication; CandidateSourceBinding/assemble_candidate_sources; SquadPublicationTransaction begin/build_path/add_write/seal; PreparedSquadPublication inspect_sources/publish_sources; load_prepared_publication; source baseline encode/decode, snapshot_source_manifest, project_publication_source_images; PublicationOperation/PublicationSourceClaim/PublicationIntentRequest and their existing codecs; native canonical planner; CapturedGraphMemory/GraphMemorySource/GraphMemoryAudit/build_captured_identity_graph/render_spec_graph. Reuse exact model identities and existing wire formats. Existing tests in test_element_identity_candidate_preview, test_spec_graph_captured, test_element_identity_source_store and test_squad_source_guard supply native setup patterns, not expected results as an oracle.

**Initial fixture:** Resolve tmp_path; use `specs/demo` and `runs/spec-test`, actual on-disk IdentityStore, and an allocated `FR-000001`. Materialize revision1 using exact typed requirement content with immutable subject `Movement`. Actual old spec bytes are `- **FR-000001**: Move using WASD.\n`; proposed bytes are `- **FR-000001**: Move using arrow keys.\n`. Keep an actual read-only evidence source with an exact native parsed reference and a stored revision1 evidence claim. Include a hidden binary file and an empty directory in the selected spec tree. Start without a graph file, so creating it is a real new sealed operation. The source context is explicitly registered from a real captured initial manifest for this existing assessed fixture; this does not claim fresh managed-run enrollment or historical migration.

**Stage the graph without modifying a seal:** A fixture may use a never-promoted provisional source-only transaction with a distinct literal transaction ID to obtain projected source bytes. Its capture, source assembly, real coherent candidate preview and native graph derivation run before creating the final transaction. Native canonical planning uses the actual projected spec bytes, explicit fixture wing and correct artifact hash; its supplied GraphMemoryAudit is explicitly a deterministic observation, not a real collection audit or prospective storage-pass proof. Policy includes the retained evidence artifact. Independently assert the stable requirement key, current revision2 and old evidence edge to revision1 in the graph.

Create a separate final transaction with the proposed spec bytes and exact rendered graph bytes as ordinary staged writes. Never mutate/unseal/reuse the provisional prepared transaction as mutable, publish it, or register an identity intent for it. Do not create/delete anything outside the isolated temporary fixture. Keeping both staging directories until test cleanup is acceptable. This two-transaction fixture is a way to exercise existing immutable APIs, not a selected production staging protocol.

Capture the final seal over the same canonical tree/file selection. Assemble candidate sources with the graph path explicitly in the physical opaque-write set and normal typed spec/evidence bindings; the graph is not an identity definition artifact. Re-run the real coherent candidate preview for the same new unclaimed child operations and derive the graph from final projected images. Compare complete rendered bytes to the actual sealed graph postimage, not just an asserted digest. The comparison is a test expectation, not a new production acceptance function. Prove the source-only and final derived graphs match despite graph bytes now appearing in the selected manifest, while the final projected manifest includes those graph bytes.

Register the source context from the real initial source manifest (before preparation) and construct the existing v3 request, preserving the exact final seal and history association:

```python
request = PublicationIntentRequest(
    prepared.marker.manifest_sha256,
    "fixture-only recovery claim; not controller completion authority",
    operations,
    PublicationSourceClaim(
        "test-source", "source-registration",
        encode_initial_publication_sources(initial),
    ),
    proposed_history_sha256=preview.history.sha256,
)
store.prepare_identity_publication(
    spec_id="demo", operation_id="publish-revision", request=request)

def apply_after_sources(final):
    # Fixture owns the hook; real journal and publisher own all mutations.
    applications.append(store.apply_identity_publication(
        spec_id="demo", operation_id="publish-revision"))

final = prepared.publish_sources(initial, after_publish=apply_after_sources)
```

Keep providers/planners/graph construction out of hooks. The graph itself is promoted by the publisher's existing sorted operation loop; do not assume spec-before-graph ordering. Use the existing fault positions (including position0 before promotion) and native promoted_prefix rather than fabricated operation order. Actual journal pending state must exist before any promotion, and its existing whole-spec guards must reject conflicting allocation/lifecycle writes throughout incomplete cases. Readers/controller consumers are not activated or proven to enforce those guards by this task.

**First actual test:** `test_sealed_graph_and_sources_match_applied_identity_history` exercises the whole chain. Independently assert final physical spec/graph bytes, unchanged evidence/binary bytes and empty directory, complete final source manifest equality with projection, source-head advancement from registration to publication sequence1, complete history payload equality after real apply and reopen, stable keys/original evidence revision binding, exact v3 application receipt history hash and source association. No state change is attributed to preview. Release only after the guarded call returns successfully and all final assertions hold, using an explicitly fixture-only completion string. Test exact application/release retries with no additional revisions/claims/source sequence, not a second allocation. This uses the low-level release API; it does not prove squad completion authenticates the string.

- [ ] Add/run the first real composition case using `/Users/michalbachorik/work/echelon_r/echelon/.venv/bin/pytest tests/unit/test_identity_graph_publication_composition.py::test_sealed_graph_and_sources_match_applied_identity_history -q` from this worktree. Notify root of the real outcome before expanding. All filesystem cases explicitly request the existing secure-POSIX capability fixture; any new purely value-based cases remain portable.
- [ ] Add before-first-promotion, after-first-operation and after-all-operations-before-identity-apply fault cases using real publish_sources. Check the exact promoted prefix/files, preserved sealed stage and original retained request/source baseline, old identity/source head until apply, pending guard and no release. Reload prepared publication and IdentityStore using existing persisted marker/request/baseline codecs, then finish the exact same operation. Complete final bytes/history/source receipt must equal uninterrupted expectations; no duplicate reservation/revision/claim/source publication rows.
- [ ] Add failure immediately after successful identity application inside the final hook. The journal is applied but pending, physical postimages are complete, source head is advanced exactly once and the guarded call fails rather than reports success. Reopen/reload and use idempotent actual apply during retry, then successful guarded exit and exact release. Do not attempt pre-intent preview against the pending journal or replace original snapshots with partial current captures.
- [ ] Use one real spawned child process that exits with a deliberate distinct code at the first completed promotion boundary, bypassing Python cleanup. Parent opens actual persisted state, verifies the exact partial prefix and pending journal, and completes from retained request/source baseline and sealed stages without the child's objects. Use bounded joins/events, no sleeps/high-count loops or provider/network/real user state. This tests process interruption, not an assertion that exception injection is equivalent to every crash.
- [ ] Add actual native failures for changed read-only evidence/hidden binary/mode/empty-directory membership, changed canonical preimage and damaged sealed graph stage bytes/mode. Run these after identity preparation but before promotion; native source/seal checks must fail before the apply hook, leave ledger/source head old and pending, and retain recovery material. Never fix drift, rewrite graph bytes or discard pending material as part of failure handling.
- [ ] Add a post-apply hook that deliberately changes a guarded source before returning. Existing post-hook source checks must reject the call; actual applied journal remains pending and conflicting writes stay blocked. Subsequent recovery against drift must fail rather than release or silently accept new bytes. This explicitly demonstrates that a committed DB effect does not by itself certify successful final guarded publication.
- [ ] Observe actual stale-history and stale-source-context request rejection before intent preparation in separate native cases, without using fixture assertions as a substitute for a production rejection. Wrong but freshly sealed semantic graph bytes are not expected to be caught by a low-level publisher: do not claim such a gate exists or add a test-only validator that pretends to implement it. Final graph rederivation and semantic approval still belong to the future production owner.
- [ ] Count actual SQL materialized rows and accepted source sequence across retries/reopen; compare complete retained history, request and receipt payloads, not just counts. Verify known old evidence remains revision1 rather than rebinding to2. Record deterministic audit/provider boundaries, lack of managed enrollment/runtime completion, and fixture-only staging/hook orchestration in storage docs.
- [ ] Run once six covering modules: `tests/unit/test_identity_graph_publication_composition.py`, `tests/unit/test_squad_source_guard.py`, `tests/unit/test_element_identity_publication_history.py`, `tests/unit/test_element_identity_source_store.py`, `tests/unit/test_element_identity_candidate_preview.py`, `tests/unit/test_spec_graph_captured.py`. No full-unit/capacity/live/global-install or unchanged postcommit reruns. Later test/doc amendments receive named scoped checks and exact tested-tree chronology.
- [ ] Self-review that real native functions—not assertions or mock coordinators—own every effect/rejection being claimed. Diff-check and commit only new tests/docs; write full report with all actual runs/failures/fixture corrections, commands/output, staged tested tree and later amendments. Root performs original-BASE independent review. Escalate any actual production gap; do not modify production or silently recast a failing contract as supported.

## Remaining integration

Passing tests prove the named primitives cooperate under explicit fixture orchestration. They do not implement production staging, closed approval/recovery admission, authenticated complete dependency/memory acquisition, semantic review, run ownership, pending-read enforcement, squad completion, producer allocation, manual/CLI integration or bounded repair. In particular, low-level publication APIs still rely on their trusted caller for graph derivation and semantic authorization; opaque recovery/completion strings remain unverified by those APIs. No rollout is authorized.
