# SDD ledger — plan: docs/superpowers/plans/2026-09-13-identity-graph-publication-composition.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / test-only scope | Native primitives now exist; no missing production API is requested. | Honest characterization outcome, no fabricated RED or test-only validator pretending to enforce a production gate. Actual gaps escalate to root. |
| 1 / immutable staging | Graph must be derived before final seal; prepared transactions are immutable. | Separate never-promoted provisional transaction is permitted only as fixture setup; final source+graph seal is independently captured and rederived. No unseal or runtime staging protocol selected. |
| 1 / capture and candidate | Graph is a write but not an identity definition artifact. | Explicit opaque physical graph path plus typed spec/evidence mappings and normal coherent preview; all final sealed operations remain guarded. |
| 1 / source ownership | Context registration can capture an existing assessed fixture without enrolling a managed first run. | Use actual source manifest/registration; make no fresh enrollment or historical migration claim. Source selection remains fixed. |
| 1 / graph and evidence | Final projected manifest contains new graph bytes; graph excludes self-input. | Compare complete rederived graph to sealed postimage; independently assert stable key/current revision and old evidence revision1. No self-hash exemption from source guard. |
| 1 / memory acquisition | Test needs native rows but no real user memory writes or prospective fabricated pass authority. | Native planner and explicitly deterministic supplied audit; no service/miner/provider call, no claim of acquisition completeness/semantic proof. |
| 1 / mutation ownership | Publisher owns all file writes and journal owns all DB effects. | Short final hook invokes only actual apply; graph never written in hook. Pending intent exists before first promotion; no DB transaction wraps publisher call. |
| 1 / recovery | Initial baseline must survive partial promotion and DB apply. | Reload exact stored request/source baseline and immutable stage via existing codecs; no preview against pending journal, no partial-baseline replacement, no recovery material deletion. |
| 1 / completion | Low-level release retains caller completion strings, not squad proof. | Release only in successful fixture path after guarded exit/assertions; describe fixture-only completion, no runtime consumer/pending-read/semantic enforcement claim. |
| 1 / fault positions | Actual sorted promotion can create graph before spec; fault hook includes0. | Inspect native operation order/promoted prefix; test before/partial/all-source/after-DB boundaries, one real child process exit. No sleeps/repeated stress loops. |
| 1 / post-apply drift | DB commit may precede guard's final callback-exit verification. | Actual guard failure leaves applied journal pending; recovery against drift must fail. A DB receipt alone is not successful publication. |
| 1 / six-module covering | New tests compose graph/candidate/source guard/v3 source-history owners. | Six named modules once, exact tree/report chronology, no capacity/full-unit/live reruns or production changes. |

Root inspected approved design, actual candidate/graph/native fixture interfaces, source-context registration and baseline codecs, sorted immutable seal and load_prepared_publication, actual guarded callback/fault semantics, and native partial-recovery test patterns. Coherent candidate phase is independently approved after590 covering tests; captured graph phase independently approved after712.

No new product ruling. Provisional/final staging and hook ordering are explicitly test fixture orchestration, not production protocols. This phase characterizes the supported composition and exposes any real primitive gap before a controller owner is selected. It does not cure the documented missing graph/semantic admission or completion authority by assertion. Existing31 rulings remain tracked in their owning ledgers.

Self-review: all task effects are real existing native operations inside temporary fixtures; assertion-based comparisons claim only expectations, not an implemented validator. Remaining source/memory acquisition, managed run/runtime ownership, graph admission, semantic proof, completion, producer and bounded-repair integrations remain explicit. Placeholder scan returned no matches. No implementation dispatched yet; root will commit the plan/brief/ledger and select BASE after the preceding checkpoint.

Preceding coherent preview checkpoint505ec471 complete; this plan/brief/preflight committed at BASE88e5af8122067fb75c7b0207fc2aa2f77eb881a3. Task1 dispatched fresh /root/identity_graph_publication_composition Astra/high for cross-owner interruption/recovery judgment, with58-line exact brief and full constraints. First genuine characterization result required before expansion (no invented RED); no production modifications allowed. Six covering modules once, one bounded real child exit, complete report/tree chronology and root independent review. Root ledger intentionally dirty/excluded. No next implementation selected.

First actual composition test passed on its first run, exact parent pytest/workdir tests/unit/test_identity_graph_publication_composition.py::test_sealed_graph_and_sources_match_applied_identity_history -q ->1pass1.15s. Real final sealed spec+graph writes, coherent preview/native graph derivation, v3 prepare/apply/source-head receipt and release retries; no fixture failure or production change. This is a genuine characterization GREEN, not fabricated TDD RED. Worker now adds interruption/native rejection cases; covering and independent review outstanding.

Expanded native composition module passed first run17pass7.59s: fault positions0/1/2, hook failure after actual apply, one spawned child os._exit(73) at first completed promotion, eight actual source/seal drift variants, post-apply drift blocking recovery, and native stale-history/source-head preparation rejection. No fixture failure or production gap. Worker self-review/docs then six-module covering once; no production changes.

Worker DONE: implementation dcebb085cbb88e4557e14a0944a5614e2e1dad51, administrative report2aeb506e8b1e7f0a1cef529b726f5d796823938f. Root read complete123-line report: three actual test runs, no fixture failures or production amendments, self-review assertion/doc improvements precede the sole covering run451pass31.80s. Exact tested implementation tree aea329a8e2bdc415f1743bfc7a08e05c9f3f11c1 verified by git show; subsequent commit differs only by report. Report staging initially encountered ignored-directory refusal then explicitly force-added its exact authorized path; not a test/source change. Root progress excluded. Fresh Astra/high /root/review_identity_graph_publication_composition reviewing full51505-byte original-BASE package, exact brief/report and constraints; no unchanged suite reruns. Review pending.

Fresh reviewer read complete brief/report/diff in four bounded non-overlapping chunks: spec compliant, quality Approved, no Critical/Important. Named unchanged-code check confirmed the conflicting ElementRevision content is valid and reserve/lifecycle writers reach the shared pending guard. Execution Cannot verify item resolved through full report, root-witnessed first/expanded notifications and exact tested-tree/report-only git comparison; no reruns. No new product ruling.

Task 1: minor (deferred): test_identity_graph_publication_composition.py:232–236 conflict assertions accept any IdentityStoreError; match pending-publication error text so future unrelated validation failures cannot satisfy the guard test. Reviewer confirmed no current false-positive path. Carry to final whole-branch triage, not the task fix loop.

Task 1: complete (commits 88e5af81..2aeb506e, review approved; one deferred Minor). Combined graph/source/identity native characterization is complete; no production gate or live workflow was activated. Whole-branch review/full-unit suite remain outstanding after the remaining integrations. Keep this workspace for final rulings and deferred-minor review.

## Read-only later integration observations (not selected protocols)

Root inspected harness/squad.py::_prepare_controller_completion and _drain_pending_controller_completion. Existing completion stage binds publication marker plus ordered effect plan before the authorizing state save. Recovery has its own staged_publication.publish call (in addition to _publish_and_finalize), then handoff_external_publication, durable state confirmation and immediate staged_publication.discard before remaining completion effects. Managed integration must cover both publication owners and prevent premature discard of identity recovery material. It must not prepare an orphan identity intent before an authorizing state boundary without a recovery admission path, or clear completion while identity remains unreconciled. No ordering/schema extension is selected here; existing opaque completion/recovery strings do not provide that admission by themselves.

Root confirmed physical/logical scope distinction: echelon/phase_a_start.py::_write_prepared_state creates runs/<run>/specs/<id>; Squad._active_phase_a_spec_dir follows state spec_dir or that run-local path, while _published_phase_a_spec_dir selects workspace specs/<id> and legacy slug fallbacks. Finalization builds a virtual tree under the publication transaction then copies to canonical publication targets. New pure captured graph assembly requires canonical-path specs/<id> images and deliberately has no alias mapping. Source contexts retain fixed selected roots. Future managed integration therefore needs explicit authenticated active/canonical physical-view ownership and promotion/copy semantics, not duplicate IDs or treating canonical mirroring as a second entity creation. No alias adapter, dual-root selection, run-transition, legacy slug-policy change or promotion protocol is selected by this test-only task.

Potential follow-up considered, not yet selected: a narrow explicit run-local source-view option on the existing graph assembler could keep logical canonical artifact/requirement keys while consuming an authenticated selected runs/<run>/specs/<id> tree. Any such view must validate the entire original raw projected capture, retain its unchanged physical manifest for source publication, distinguish ignored old canonical bytes from active logical graph inputs, and never present rebased metadata as an accepted physical source claim. A parallel generic alias carrier/codec is not automatically needed. Default canonical behavior and all legacy graph routes must stay unchanged. Exact API/path admissibility and runtime admission remain unselected pending this composition gate.
