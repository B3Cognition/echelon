# SDD ledger — plan: docs/superpowers/plans/2026-09-13-coherent-identity-candidate-preview.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / authority observation | Separate existing public candidate and history readers open separate transactions. | New thin reader owns one query-only transaction; existing caller-connection helpers share it, no nested public reads or writer transition. |
| 1 / operation authority | Candidate changes supplied independently of journal children could diverge. | Derive all lifecycle/binding batches exclusively from detached validated canonical children; no parallel changes/claims/occurrences parameters. |
| 1 / retained history | Existing snapshot capture audits full retained authority; overlay and journal planner already exist. | Capture once before candidate diagnostics, share planned_effects and _overlay; no new snapshot codec or independent plan logic. |
| 1 / candidate semantics | Current candidate helpers own definitions, scope, active references, issue provenance and supplemental checks. | Compose unchanged helpers; retain existing diagnostic/reference meaning and no semantic equivalence inference. |
| 1 / claim source consistency | Reference source helper checks supplied claims, not completeness of every parsed reference. | Call with exact decoded claims, merge its diagnostics; no manufactured assessment or implicit new claim. |
| 1 / occurrence association | Candidate report contexts and proposed occurrence children are otherwise independent. | Exact proposed occurrence must appear in after context; each after occurrence must be proposed or exactly retained. Existing report/revision checks still apply. See Ruling31. |
| 1 / output | Failed candidate must not provide passing projected history. | Frozen values-only result has history=None exactly for diagnostic results; invalid authority/request raises bounded error. No acceptance/semantic flag or wire schema. |
| 1 / errors | Stale lifecycle proposals are existing candidate diagnostics; child-ID ownership/authority/binding failures are exceptions. | Clarified exact distinction in plan instead of ambiguously labeling all stale operations exceptions. Full audit precedes candidate rejection, no source-bearing exception chains. |
| 1 / ownership | Existing normalizers copy some descriptors but retain original frozen artifacts/occurrences. | Copy native input records before opening authority; codec owns detached lifecycle/binding entries; no generic deep-copy framework. |
| 1 / concurrency test | Rollback-journal readers prevent second writer commit, but allow its in-transaction changes. | Use explicit events and real connections/writers; prove one old observation plus later v3 stale-history rejection. No timing sleeps or mocked authority. |
| 1 / graph integration | Captured graph consumes supplied proposed history but does not seal/publish itself. | Actual fixture composes capture/candidate/preview/graph plus identity journal only; does not claim atomic graph publication or semantic authorization. |
| 1 / scope and tests | Public store method added without changing existing owners or activating callers. | New module/tests/docs plus thin method/type annotation only; nine named covering modules once, first native RED, fresh original-BASE review. |

Alternatives considered: independently calling existing readers leaves a transaction gap; adding a second baseline-hash protocol to every reader duplicates binding state; one caller-owned read transaction composes existing helpers directly and is the smallest coherent integration. No source/publication/semantic authority is added by the result.

Ruling: 31. For the new candidate-preview integration only, every proposed issue occurrence must equal a full captured after-context occurrence, and every after occurrence must be proposed or exactly retained — otherwise a clean report check and clean journal plan could describe different occurrence histories. Existing unchanged authenticated history needs no duplicate operation; existing report/revision/duplicate semantics remain owned by their helpers — reversal costs changing this inactive association adapter and its diagnostic/tests or supplying another explicit occurrence association protocol; no existing IDs, revisions, fingerprints, graph keys, journal/snapshot wire, canonical artifacts or live behavior are rewritten.

Spec self-review: the task implements one coherent structural/history observation within approved identity/reference publication validation. Source acquisition/acceptance, semantic review, graph sealing, managed runtime/producers, repair, final offline tests and rollout remain explicitly outside this reader and still required overall. Exact names/types were checked against existing candidate, bundle, issue context, journal decoder/planner, snapshot capture/overlay and graph/candidate-source APIs. Placeholder scan returned no matches. No final unit suite or capacity work is repeated.

Drafted while captured graph assembly finishes its independent gate; no implementation dispatched, no BASE selected yet. Previous task worker must exclude this root plan/ledger from its scoped commits. User's approved phased automatic continuation remains authority; all implementation stays in the existing isolated worktree, with explicit later rollout checkpoint. Root retains ledgers and exhaustive ruling list through whole-branch review.
