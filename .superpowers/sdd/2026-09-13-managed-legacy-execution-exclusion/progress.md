# SDD ledger — plan: docs/superpowers/plans/2026-09-13-managed-legacy-execution-exclusion.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / managed versus legacy | Existing managed primitives are explicit, but ordinary runtime publishers remain legacy. | Refuse already managed declarations/ownership from those paths; do not enable or initialize managed execution. Positive managed admission is not claimed. |
| 1 / removed state metadata | A state-only check could silently become legacy after removal. | Check independent physical run name, declared run and claimed spec against retained managed rows. Changed state IDs cannot hide a retained matching physical run. |
| 1 / damaged authority | Missing row may leave a managed registration operation; dangling authority symlink is not absence. | Native open for any lstat-present authority; indexed managed-operation orphan check in query-only transaction. No auto-init, repair or document maxima. |
| 1 / authority query | Full history audit per admission would add unrelated million-row scans. | Inspect managed registrations using existing indexes; presence rejects without certifying damaged payloads. No identity child-history reads; absence is not a full audit or enrollment lease. |
| 1 / execution ordering | Lease owner drains completion before _run_locked. | Place exclusion after existing PhaseA/SpecRun leases and before drain/retarget/cleanup/execute; return an unpersisted blocked result. Busy behavior remains first. |
| 1 / human-input ordering | Four public methods can write decisions/drain completion outside run callback. | Check at existing state loads before effects, preserving preceding read-only validation. Existing HumanInputPolicyError communicates refusal. |
| 1 / CLI response | CLI ignores resume_with_human_input False and prints submitted. | Raise the already handled HumanInputPolicyError instead of returning False from excluded human-input entry. No CLI edit or false submission result. |
| 1 / rejected state | Saving a blocker could mutate the very state whose authority was rejected. | Return/raise only, preserve exact state/SQL/stages/counters; no provider, cleanup, publication or memory writes. |
| 1 / modes | guided/semi/banzai differ in routing, not identity integrity. | Same exclusion across modes; unmanaged existing paths retain normal behavior and existing integration tests. |
| 1 / concurrent enrollment | Short query cannot serialize a future explicit registration through long provider execution. | State this limitation; no long DB transaction or inferred registration lease. Enrollment remains explicit/inactive; future source/candidate/publication owner still required. |
| 1 / tests | New first RED must show existing unsafe legacy entry, not an absent invented API. | Actual registered authority/state plus public run and narrow ordering spies; native stage-preservation, query-only and unmanaged controls follow. Seven exact covering modules once, no capacity/live/full suite. |
| 1 / scope | Four human-input boundaries plus public run/manual use existing owners. | One authority method, one small adapter, narrow Squad entry calls; no new controller, schema, codec, feature snapshot or producer. |

Root inspected approved design, actual managed schema and partial indexes, managed context APIs and read validation, Squad constructor/state load/telemetry constructor, execution lease ordering, public run/manual paths, all four human-input state-load/effect boundaries, and actual CLI resume exception/banner handling. Existing test helpers provide real authority/state/stages and simulated providers. Captured active graph phase is independently approved after290 covering tests at checkpoint1eb7b9e8; its unused-import Minor remains in its own final-review ledger. No next implementation has been dispatched yet.

Ruling: 33. Until the complete managed runtime protocol is integrated, explicitly refuse existing legacy controller execution when managed state is declared or retained ownership matches the physical/declared run or claimed spec; also refuse malformed present authority and orphan managed registrations instead of guessing legacy. Keep rejection non-mutating and raise the already handled human-input error so the CLI cannot report a false submission — current legacy recovery/publishers cannot safely consume an explicitly enrolled identity authority, and removal of a state field must not bypass the boundary — if wrong, revise the scoped inactive exclusion/entry checks and tests or replace refusal with a fully authenticated managed owner; no IDs, revisions, graph keys, schema/journal wire or canonical artifacts need rewriting, and no installed/live behavior has changed. This is an instantaneous negative check, not a concurrent-enrollment lease or complete authority audit; unrelated valid legacy ownership still proceeds.

Self-review: all named interfaces/files exist except the explicitly introduced method/adapter/new tests; exact errors and matching semantics are specified; no unsupported positives or live activation. The shape-validation fast path preserves no-authority legacy state behavior. Placeholder scan returned no matches. Scope excludes producer, publication, source selection/enrollment and repair support rather than pretending this negative gate completes them. Root will commit brief/plan/preflight before dispatch and record BASE.

Task1 dispatched fresh /root/managed_legacy_execution_exclusion Astra/high at BASE6eb07d8c5f8169880bac7068ffd033789bfc9a8b with exact71-line brief, full constraints and no-subagents contract. First real existing-behavior RED required before production edit; seven exact covering modules once (including simulated human-input integration), full report/tree chronology, independent original-BASE review. Root progress intentionally dirty/excluded. Ruling33 selected but not yet implemented/tested. No next implementation selected.

First genuine RED before production edit: mandated exact parent pytest/workdir test_squad_identity_exclusion.py::test_managed_run_refuses_legacy_recovery_before_any_effect -q failed1/1 in0.40s, actual public run returned completed rather than blocked. Native empty run-local source inspection, source/genesis registration and managed StateStore initialization succeeded; narrow legacy recovery/retarget/cleanup/execute spies exposed current ordering. No fixture failure. Worker proceeding to bounded authority query/adapter/entry changes.

Root clarified genuine absence in plan and exact brief during implementation: leaf lstat can also raise FileNotFoundError through a dangling .echelon parent. Require parent lstat first, permit absent parent, refuse present symlink/non-directory, then leaf/native-open handling; no broad root-ancestor validation on no-authority legacy path. This is the stated malformed-present-authority refusal of Ruling33, not a new positive admission or full filesystem-pinning protocol. Worker notified to expose a genuine focused RED if current implementation misses it and retain test chronology; root plan/brief amendments remain administrative and must be excluded from worker staging, then retained with the review package.

Worker confirmed actual parent-edge RED before correction: dangling .echelon and symlink .echelon with absent identity both incorrectly returned; regular-file parent already refused. Focused result2failed1passed0.22s. Worker implementing precisely amended parent lstat/refusal and will retain this separate RED/GREEN chronology. Root additionally reminded worker to exclude all dirty plan/brief/progress administrative files, not just progress.

Expanded worker checkpoint: guard suite first pass73/73; amended parent case focused GREEN3/3 after2RED+1existing control. Controller expansion45passed5fixture failures (same-thread run-lock inversion2; guessed nonexistent snapshot method3); fixtures corrected to external thread owner and native capture_routing_snapshot, focused GREEN7/7. Native staged publication plus terminal completion uses begin_terminal_controller_completion; raw removed managed metadata exists only in fixture, and exact stage/state/SQL retained on refusal. Four human-input entries across modes and retained/removed metadata pass. No extra owner/API/CLI scope needed. Worker self-review then seven-module covering once; report/exact tree and review still outstanding.

Once-only seven-module covering result reported758pass65.56s, no skips/warnings; staged implementation tree c86b086bdd51a9192a39417f8177871915678bec. Worker self-review complete and proceeding with six scoped files then report-only commit; root administrative plan/brief/progress remain excluded. A later provider-versus-setter fixture correction is explicitly retained in forthcoming full report; native manual-claim test now passes. No unchanged rerun requested. Root still must read complete report, retain amended brief/plan and dispatch independent original-BASE review.

Worker DONE9076350a9322ae1477bb7a517c9a7c3bccf4a21b; report-only4c57a576b10d7abbf0656dec30ac211b5f430ef5. Root read complete208-line report and directly verified implementation tree c86b086bdd51a9192a39417f8177871915678bec and report-only diff. Full chronology includes firstGREEN1pass0.43s; guard73pass2.48s; parentGREEN3pass0.18s; controller fixture5fail45pass3.48s corrected7pass0.76s; addedfocused5pass0.29s; manual-replay fixture provider_escalation used unsupported direct setter source1fail0.46s, corrected to native controller_safeguard/agent_blocked1pass0.39s. No production/test/storage-doc amendment after758pass65.56s.

Root retained exact previously communicated parent-presence amendment in plan/brief-only e8ace2e6c1de5338e78fff0ed2ac7eb2f49f8129. This is administrative after the tested implementation tree, not a claim that the changed documentation tree itself was rerun. Fresh Astra/high /root/review_managed_legacy_execution_exclusion reviewing full79722-byte original-BASE6eb07d8c..e8ace2e6 package, amended brief, report and full constraints; no unchanged reruns. Review pending. Root progress remains dirty/excluded.

Task 1: complete (commits 6eb07d8c..e8ace2e6, review clean)

Fresh /root/review_managed_legacy_execution_exclusion returned spec compliant and quality Approved, with no Critical, Important or Minor findings. The full 79,722-byte package was read in bounded non-overlapping chunks without truncation. Focused unchanged-code checks confirmed exact lifecycle text validation, established-authority opening and query-only schema checks, read-only state loading, public execution delegates/busy handling, and the existing CLI HumanInputPolicyError catch before its submission banner. Historical execution/tree evidence cannot be independently replayed from a diff; root resolves this from the witnessed RED/GREEN/covering notifications, complete 208-line report and directly verified implementation tree/report-only and brief-only commits. No unchanged tests were rerun. Ruling33 is now implemented and independently approved; concurrent enrollment serialization and positive managed execution remain explicitly outside this completed exclusion. Root checkpoint changes only administrative records.

## Read-only future integration observations (not selected protocols)

Existing graph memory acquisition is not yet captured-input acquisition: echelon/spec_graph.py::_add_canonical_memory loads current canonical/support snapshots, builds an adapter and plans rows, then mempalace_audit.audit_spec_memory loads canonical/support files again and reads collection rows; its reconciliation also takes project_root. The other graph domains acquire adapters/audits through _add_artifact_memory_domain and preserve native partial/unavailable semantics. New pure graph APIs accept coherent supplied rows/observations, not authentic current collection acquisition. spec_graph_audit compares memory_state_digest and still treats required memory unavailability as a gate failure. Future managed staging must preserve actual acquisition/provenance and existing quality gates; do not fabricate prospective pass receipts to break graph-sealing order. No acquisition, memory mutation, graph staging or completion ordering protocol has been selected during this exclusion task.

Further exact acquisition dependency: context_reconciliation.reconcile_drawers resolves project/canonical paths and hashes current files. Its canonical directory policy is specs/<three-digit-prefix-slug>/..., which is the existing spec-directory policy, not an AC/FR/NFR element-width guard; do not change it as an ID-counter fix. audit_spec_memory plans canonical/support rows, fetches collection data, invokes that live reconciliation, compares exact wing/room/scope/artifact/source/content hashes and lifecycle flags, scans extras, then creates the native report. A captured audit needs a truthful captured-input reconciliation path and actual collection observation, not a supplied pass label. No new reconciliation API, source-view extraction, audit policy or memory/graph completion sequencing has been selected.
