# SDD ledger — plan: docs/superpowers/plans/2026-09-13-managed-retarget-rewind-exclusion.md

## Scope and recovery

Prepared while preceding captured artifact audit is awaiting independent review. Do not dispatch until that phase is complete. Only this root owns this plan, brief and ledger. No implementation BASE yet. Offline existing worktree only, no installation, main mutation or stopped smoke. Retain until final whole-branch review and exhaustive rulings.

Spec: docs/superpowers/specs/2026-09-12-durable-element-identities-design.md, approved and fully read in this execution. Task is a bounded negative admission step, not all-writer integration or positive managed recovery.

## Preflight

| Task/interface pair | Producer versus consumer | Finding |
| --- | --- | --- |
| Task 1 self-consistency | Six named production owners, one new test module and storage documentation versus seven exact covering modules | No new controller/schema/publication protocol; existing tests unchanged. Native first-effect test establishes defect before implementation. |
| Task 1 spec query / prior execution guard | Empty run tuple with real selected spec versus existing nonempty run-only calls | Only spec-present empty tuple becomes valid; None plus empty still rejects; exact validation, indexed owner/orphan query and one read transaction unchanged. |
| Task 1 presence helper / old guard | Shared lstat/open versus existing metadata-first execution checks | Preserve truly absent authority compatibility and malformed-parent rejection; no inferred run IDs or initialization. |
| Task 1 retarget / locks and errors | Actual baseline/active state plus separate canonical selector versus three locked mutation owners | Place after existing rechecks but before first durable effect or early successful resume return; outside mutation exception handlers. Busy precedence remains. |
| Task 1 recovery / native hidden mutation | `_require_recovery_revision` identity checks versus its captured-receipt history advance | Gate before raw_graph reconciliation, not merely before outer restoration. Optional baseline state read must not create directories or treat a present broken entry as absent. |
| Task 1 CLI rewind / lower prepare | Actual locked runtime state versus native same-head applied=True and later state effects | Guard entire CLI including nominal preview; lower confirm=False stays unchanged and nonmutating. Confirmed library query precedes same-head success and all Git/file effects. |
| Task 1 scope / managed design | Negative guarded owners versus future managed transition/publication | No managed retarget/rewind enablement, full writer perimeter or enrollment lease claimed; native unrelated legacy paths retained. |

Ruling: 37 — Allow the existing query-only negative ownership method to accept an empty run tuple only with a real selected spec, and use it together with actual runtime witnesses to exclude managed retarget/rewind/recovery at their native effect owners. Guard the whole rewind CLI including nominal preview because its same-head native result can proceed to state effects; preserve the nonmutating lower-level preview and unrelated legacy behavior. — Those owners can otherwise bypass the delivery-loop exclusion and rewrite accepted source/history/graph/memory or claim completion without a managed transition; a spec-only command must not invent a run ID. — If wrong, narrow the inactive negative-admission APIs/placements and compatibility tests or integrate an explicit authenticated managed source/run transition; no IDs, revisions, graph keys, schema, journal receipts, canonical sources or live deployment need rewriting.

Task 1: prior captured artifact audit complete (implementation 0768a2ac, report 41c6fcdb, 821 tests, independent clean spec/quality review). Pending dispatch after committing this plan/brief/ledger; Ruling 37 selected, not implemented or reviewed. Placeholder scan found no matches; all six existing covering-test paths verified, seventh is the intentional new module.

Task 1: dispatched to /root/managed_retarget_rewind_exclusion (Astra/high), BASE 5591bc696d8c3d9ef27667961760c593a8954bd7. Prior phase administrative checkpoint 53532707. Root-only ledger excluded from worker commits. Await actual native-grounded RED.

Task 1: initial fixture attempts: import collection error corrected; then native enrollment rejected a nonempty genesis source. Worker corrected fixture through actual empty-tree capture/source registration and only afterward materialized native retarget fixture content. Root confirmed no weakening of genesis; this is deliberate post-genesis source state for negative ownership testing, not accepted-source/positive managed-retarget authority.

Task 1: actual required first RED observed, 1 failed in 0.64s at append_prepared_revision_from_preview effect tripwire after native preview, selectors and all leases. No production changes before this RED. Await exact failed attempt commands/timings in full report.

Ruling: 38 — Preserve `_cmd_rewind`'s existing pre-lease `_failed_gate_rewind_authority` evaluation; the new admission precedes the fresh under-lock evaluation and all consuming/effect paths. — Root inspected the function and immediate policy helpers: the early call reads/validates decision authority but does not consume claims or mutate state. Removing it would unnecessarily alter legacy validation/error precedence; an earlier native rejection is safe and is not bypass into mutation. — If wrong, relocate the preflight explicitly and revise precedence tests, with no identity/revision/graph-key/schema/receipt/source or live-deployment rewrite.

Task 1: worker raised ambiguous pre-lease gate placement before proceeding. Root ruled above, amended exact plan and worker brief, and instructed preservation plus native precedence/consuming-path tests. These root administrative amendments must be carried in the original-BASE review package but excluded from worker implementation/tested tree.

Task 1: first GREEN received, exact required test 1 passed in 0.66s; named native owner placements implemented, preserving pre-lease gate per Ruling 38. Comprehensive coverage underway.

Ruling: 39 — Translate only managed_identity-validator StateAdvanceError at both existing rewind CLI state loads into the bounded handled identity refusal; keep unrelated read errors and existing native state-read/lease bookkeeping unchanged. Optional recovery baseline admission must use direct regular-file/strict-JSON reading without SquadStateStore construction. — Native load validates malformed managed declarations before later admission, and its constructor/load can create staging/state.lock; silently treating those APIs as pure or leaking an uncaught managed-state error would misstate the boundary. Cold-state tests must expose the exact native bookkeeping allowance, while source/state/history/identity/Git/graph/memory effects remain forbidden. — If wrong, revise the narrow error translation or add an explicitly side-effect-free native state-read owner and its tests; no IDs, revisions, graph keys, schemas, receipts, canonical sources or live deployment need rewriting.

Task 1: root inspected StateAdvanceError, _managed_identity_from_state, SquadStateStore ctor/load and CLI imports; approved the worker's narrow translation and read-only optional baseline approach, amended plan/brief, and flagged early-handler local-import shadowing risk. Root administrative amendments remain excluded from worker implementation/tested tree and must travel in final original-BASE package.

Task 1: root reminded worker that native restore_or_recreate_baseline_state rejects symlink/non-directory runs and baseline-run ancestors before reading state; optional read must preserve those native pre-read semantics, not validate only state.json leaf. This is the existing-semantics requirement, not new scope or authority.

Task 1: expanded new-module run reported 22 failed, 188 passed in 61.88s. Worker identified test matrix expectation errors: verified/resume committed-recovery entry points return None before _require_recovery_revision for unrecovered history, with no effects. Correcting captured-receipt cases to actual mutating validator/recover owners, and using separate actual committed-recovery fixtures for verified/resume assertions; unmanaged verified-commit control already passed. No production expansion proposed.

Task 1: worker reports corrected recovery cases passed in scoped run that finished 2 failed / 30 passed / 175 deselected in 11.11s: test patch accidentally placed standalone-preview assertions in failed-gate test, producing NameError after intended assertions. Lines moved back; scoped rerun pending for recovery, native subprocess busy leases, failed-gate precedence/claim nonconsumption and standalone cases. No production fix attributed to these test errors.

Task 1: corrected scoped follow-ups passed: 36 passed / 171 deselected in 12.18s; then 87 passed / 128 deselected in 29.75s. Worker added narrow baseline ancestor/missing-run, CLI independent-witness and exact subtype/query equality cases plus docs, completed self-review and confirmed all covering paths. Once-only seven-module cover started on staged implementation tree 46f9bedca263e566ea7b1cea3d3d9b517b88e7fa, eight scoped files; root admin amendments remain unstaged. No result claimed yet.

Task 1: original seven-module cover finished 8 failed / 441 passed in 83.70s on tree 46f9bedca263e566ea7b1cea3d3d9b517b88e7fa. All eight failures are three existing synthetic CLI-retarget fixture bodies (ordering, six destructive-failure cases, bootstrap failure) that mock preflight/locks/effects and lack baseline state.json; fresh read correctly refuses before their target assertions. Other modules/new native tests passed. Root read the three fixture bodies before deciding.

Ruling: 40 — Permit only actual ordinary legacy baseline state.json setup in those three existing test_cli_spec_retarget fixtures, preserving their assertions/mocks and production fresh-state admission; verify new exclusion plus CLI-retarget modules on a separately recorded amended tree without repeating the whole covering set. — Synthetic previews previously omitted a required native precondition; allowing absent state or stubbing admission to satisfy them would weaken the new boundary. — If wrong, revise the three fixture setups/compatibility tests, not the mandatory fresh-state read; no identity, revision, graph-key, schema, receipt, canonical-source or live deployment changes are involved.

Task 1: approved scope amendment sent to worker and applied to exact plan/brief. Initial eight-path test tree and later nine-path fixture-amended tree must be distinguished in report/review; no fresh passing full-cover claim.

## Root read-only graph integration reconnaissance (not selected protocol)

- Three completed captured memory APIs each acquire their own native adapters/row cohort. They do not establish one aggregate cross-domain storage/configuration observation. Native adapter factories each read wing config and create MemPalaceContext; future aggregate ownership must bind those observations rather than assume separate passing audits are one snapshot.
- `spec_graph_captured.build_captured_identity_graph` consumes caller-selected CapturedGraphMemory records with source bytes, native planned rows and explicit GraphMemoryAudit, plus supplied RE descriptors/source topology and complete projected history. It validates byte association but deliberately does not acquire or authenticate catalogs/config/storage.
- Actual RE registry owner is `harness/re_registry.py`; typed descriptors come from index-owned manifest descriptors plus each manifest catalog, and legacy registry descriptors include path-based curated discovery. `_parse_index`, `_existing_registry_path` and descriptor validation still use native disk paths. Captured self-consistent descriptor tuples plus index presence do not prove that association; no aggregate or captured-registry protocol chosen here.

Task 1: root read the complete 120-line report and confirmed implementation d57c3c59f372f732bce784d7ed66787e8aef0024 has the amended tested tree 8e456289d9908655d91290d6f82409fdfae04067; 93a8598512518bc61b4310099b47632755ac5a9b is report-only. Final scoped new-exclusion plus CLI-retarget run passed 280 tests in 70.44s, with only 12 fixture setup lines added after the original 8-failed/441-passed seven-module cover. No fresh passing seven-module run claimed. Root plan/brief/ledger amendments will be committed before original-BASE review packaging; administrative commit is not a retested implementation tree.

Task 1: root administrative commit 0d0f7cec carries amended contract. Initial git add refused ignored SDD paths; exact authorized files were explicitly force-added, no broader add. An earlier package ending at93a85985 was superseded and not dispatched. Fresh Astra/high /root/review_managed_retarget_rewind_exclusion is reviewing full120940-byte original-BASE package ending0d0f7cec. Next memory/evidence plan is drafted only, not dispatched.

Ruling: 41 — Add a shared read-only recovery identity inspector and invoke its public negative-admission wrapper in the rewind CLI before committed-recovery probing and every Git/ledger effect; leave captured-receipt reconciliation at its existing later recovery owner. — Independent review reproduced baseline-only managed metadata reaching rewind's first file/Git effect because the non-recovered committed probe harmlessly skips admission and the CLI guards only selected/current ownership. The plan's gate composition was incomplete; moving full reconciliation earlier would introduce a different mutation-before-admission defect. — If wrong, revise the scoped inspector/CLI placement and baseline-only integration tests or provide another explicitly nonmutating admission protocol; no IDs, revisions, graph keys, schema, receipts, accepted sources or live deployment need rewriting.

Task 1: root inspected exact _require_recovery_revision and CLI control flow, confirmed Important baseline-only gap, amended plan/brief above. Await complete first review then resume original worker for fix round1/5; no next implementation dispatch until scoped re-review clean.

Task 1: complete first review received and read: spec noncompliant, quality Needs fixes, one Important baseline-only CLI admission defect, no Critical/Minor. Fresh reviewer recovered full package and cut-off owner context and ran one new native temporary-repository diagnostic: actual legacy retarget applied, baseline-only managed_identity=False, confirmed CLI entered first rewind effect. No reported suite rerun. Historical execution/tested-tree CannotVerify resolved by root's original test notifications, full report and implementation tree check; broader writer/enrollment/positive-transition items are explicit scope nonclaims, not completed requirements. Original worker resumed via followup_task for fix round1/5, with finding verbatim, Ruling41 amendment and exact three-module scoped verification. Next memory/evidence plan remains drafted only.

Task 1: fix round1 actual first RED received: test_cli_rewind_rejects_baseline_only_metadata_before_first_effect failed in1.97s at native create_backup_ref after genuine successful legacy retarget and baseline-only managed_identity=False. No production fix existed yet; no fixture correction was required. Await extraction, GREEN and scoped cover.

Task 1: fix round1 first GREEN received: exact baseline-only CLI regression passed in1.79s. Worker extracted shared nonmutating identity/admission inspector and wrapper, CLI calls before probe/resume/dirty planning, original later recovery owner retains reconciliation. Expanded baseline-witness/preview/no-op coverage underway; no final scoped-cover result yet.

Task 1: worker found three existing synthetic CLI routing tests have no history before final fix cover. Scoped diagnostic test_cli_rewind.py -k 'retarget_checkpoint or unconfirmed_committed_recovery' -q --tb=short returned3 failed/31 deselected in1.28s, all unavailable revision. Root read all three full bodies before approving fixture-only history setup. Ruling42 belongs to the separately drafted memory/evidence plan and was made before the following decision; preserve chronological order in final handoff.

Ruling: 43 — Add real native prepared-revision/recovery-projection fixture history and bind its actual revision ID into the three named CLI rewind fixtures, sharing setup if useful, while preserving every effect mock and assertion and mandatory production inspection. — Those synthetic routing fixtures never supplied history because old mocked paths did not read it; missing-history admission or patched guards would weaken the baseline fix. — If wrong, revise only the three fixture setups and compatibility tests, not the new pre-effect history check; no IDs, revisions, graph keys, schema, receipts, accepted sources or live deployment need rewriting.

Task 1: fix round1 three amended existing CLI fixtures passed3/31deselected in0.53s. New focused matrix returned16 failed/37 passed/228deselected in68.99s: all clean-noop fixtures restored whole spec to checkpoint (prepared history) but left rebuilding runtime, so native dirty-path validation correctly refused drift before the target. Worker replacing only that setup with genuine prepared-checkpoint plus replacement bootstrap before invalidation, maintaining naturally matching history/runtime/HEAD. Root confirmed no production relaxation; same-head dirty/moving baseline witnesses and read-only wrapper controls passed. Original first RED unchanged; final cover not yet run.

Task 1: corrected clean-noop fixtures passed16/265deselected in18.29s using native prepared revision/checkpoint/bootstrap under leases, with lower-level preview verifying Already at checkpoint. CLI new admission precedes later dirty-plan validation. Worker self-review found no further production change and started exact three-module fix cover on staged tree9d72db31394a0065c491778110c4487579cdbf98, five scoped code/test/doc files only; root admin and next draft excluded. No result claimed yet.

Task 1: fix round1 exact three-module cover passed332 tests in138.50s. Worker confirmed staged tested tree remained9d72db31394a0065c491778110c4487579cdbf98, exactly five scoped paths, no unstaged changes to those paths and no post-cover implementation amendment. Await implementation/report commits and full fix report before root provenance verification, administrative contract commit and scoped re-review.

Task 1: root read the full59-line fix report append through EOF and verified72af5b164eea4fa38b73afb930949b83d36a3cbb tree is9d72db31394a0065c491778110c4487579cdbf98;86d87b5a0c33f32a449b7129620ab98c0b29457c changes only the report append. All exact commands/results and fixture corrections are present. No post-cover code change claimed. Root now commits only current amended plan/brief/ledger before fresh scoped re-review; next memory draft remains excluded. Administrative HEAD is not another tested code tree.

Task 1: administrative contract commitd37845257368255934015c084cc3add91e9eb4cb completed. Fresh Sol/high /root/rereview_baseline_rewind_admission dispatched with amended brief/full report and67513-byte package0d0f7cec..d3784525, the one Important finding verbatim and no unchanged-suite rerun. Scope is finding plus fix-diff new breakage. Await verdict; root ledger-only update is excluded from reviewed implementation.

Task 1: fix round1/5 (1 addressed,0 open; baseline-only CLI pre-effect admission; commits0d0f7cec..d3784525). Fresh Sol re-review read the full716-line package, confirmed shared inspector/wrapper and CLI placement, retained later raw_graph reconciliation, and the332-pass report. No new C/I/Minor, no out-of-scope observations, no reruns. Root read full verdict; initial spec/quality blocker is resolved. Historical execution provenance was already independently reconciled by root; no remaining CannotVerify gap in scoped requirements.

Task 1: complete (commits5591bc69..d3784525, review clean after fix round1). Rulings37–41 and43 are implemented and reviewed; Ruling42 belongs to next memory/evidence draft and remains unimplemented. Final exact code tree9d72db31394a0065c491778110c4487579cdbf98 passed332 focused tests; initial cover and fixture-amended280-pass run retain their distinct chronology above. Root completion/docs checkpoint is administrative, not a new tested tree. Retain workspace for final whole-branch review and exhaustive Rulings I made; no merge/install/smoke activation. Proceed automatically to next scoped plan.
