# SDD ledger — plan: docs/superpowers/plans/2026-09-12-proposed-identity-history.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / graph and source publication | Existing graph file is inside the completely selected spec tree; mutating it after guarded promotion would contradict that source manifest. | Preview exact proposed materialized history before sealing, allowing later graph rendering with existing pure projector. No actual graph/source owner activated here. Ruling27 below. |
| 1 / operation validation | PublicationOperation and request v1/v2 enforce exact canonical payloads and ordered unique child methods/IDs. | Share narrowly extracted validation/decoding if needed; no fake parent/hash/recovery request in production, unchanged existing wire bytes. |
| 1 / ownership and preview phase | Journal preparation accepts only new globally unclaimed children and blocks pending spec writes; historical read is separate. | Preview is explicitly pre-intent/new-operations-only, rejects existing/global child IDs and pending same-spec state including empty tuple. No retry/receipt/lease claim or mutation. |
| 1 / lifecycle and bindings | Existing planner produces revision8-tuples and lineage7-tuples; projected binding view validates current/projected target semantics. | Reuse plan_changes and projected validator, not a new lifecycle switch. Corrected draft active-dependency language: historical and None reference semantics remain existing policy; candidate-only active-dependency validation is not introduced here. |
| 1 / retained and proposed snapshots | Existing capture full-audits and returns canonical version1 complete materialized history, excluding allocation/publication records. | Preserve wire/root fields and all retained rows; new child operation IDs belong only to the new rows. No receipt/namespace/source metadata invented. |
| 1 / shared row construction | Sole lifecycle writer constructs exact revision hash/lineage and binding writer uses _entry plus payload digest. | Extract only small pure row constructors where needed; keep SQL owners, operation retry, claims and receipts unchanged. Exact real-journal output comparison plus independent expected rows prevents mutually agreeing bad projections. |
| 1 / ordering | Snapshot SQL sorts entity kind/ordinal presence/length/value/label, revisions numerically by string length/value, lineage by predecessor/successor ordering and bindings by op/index. | Overlay must reproduce exact total ordering, including opaque None ordinals and entry indexes9/10. No bounded integer conversions or padding relabeling. |
| 1 / transaction and cost | Existing snapshot capture is deliberately full-audit/full-history, not indexed per-ID lookup. | One query-only transaction, no savepoint/write simulation/in-memory database copies, no state/publication locks or provider/source I/O. No scan added to ordinary allocator/context checks. |
| 1 / graph evidence | Existing projector preserves stable keys and old revision-bound evidence with assessment metadata. | Same base graph plus preview must render byte-identically to real journal-applied history; no canonical write or claim that base source rows are authenticated. |
| 1 / first RED | Real reserved FR and existing canonical PublicationOperation precede new method call. | Exact AttributeError required before production; later real v1 journal apply is test evidence, not physical publication proof. Root must witness RED. |
| 1 / covering tests | Shared codecs/row constructors affect existing publication and history code. | Eight named modules plus actual source-store module when shared publication logic touched; real v2/frozen compatibility, no capacity/full-unit/controller/live repeat. All existing module names verified. |
| 1 / broad approved design | Runtime mode selection, physical isolation, semantic judgment, producers, complete graph/publication recovery and bounded repair remain. | This task closes a graph-before-seal data dependency only; no deferred legacy adoption, run transition or producer protocol silently selected. |

Ruling: Compute proposed materialized history through the existing read-only planners before graph sealing, as a pre-intent preview for new unclaimed child operations with no pending publication — the complete source guard includes the graph artifact, so graph bytes must be knowable before promotion, while temporary ledger writes or treating a projection as a recovery receipt would blur the existing owners — if wrong, replace the planning adapter or add an explicit retry-preview protocol and adjust focused equality/ownership tests, not rewrite element IDs, accepted revisions, graph keys, stored snapshot wire data, canonical sources or live runs. This is Ruling27 in the chronological final handoff.

Main fully read the approved design and complete87-line plan after preflight correction, existing history capture, lifecycle plan/apply, binding row/receipt construction and projected view, publication DTO/validator/prepare/guard/apply/read, pure graph test constructors and renderer usage. Verified actual request-codec/source-store/snapshot/graph module names. New preview ownership/canonical-byte equality spans multiple retained data shapes, warranting fresh Astra/high implementer and independent same-risk reviewer. One task because all rows must describe one coherent planned publication; no parallel implementation.

Managed-context authentication completed at5dae1a1d/checkpointd98c07ef, one report-wording minor deferred in its own ledger. Current plan pending commit/brief/dispatch. Root owns plan/ledger. Retain artifacts through final whole-branch review and exhaustive rulings handoff.

Task1 dispatched fresh /root/identity_proposed_history Astra/high, BASE5b3442aaeb702d09731712252fe432f0617e5a15, full64-line brief plus complete implementer/report contract. Exact first AttributeError RED after real allocation/operation setup required before production; one query-only transaction/shared planners/full byte equality and graph evidence checks emphasized. Historical/None binding compatibility correction and Ruling27 carried. Eight named covering modules plus source-store if shared publication helper changed, no broad/capacity/live repetitions. Root ledger intentionally dirty and excluded from worker commits. Pending actualRED checkpoint. No next plan selected.

Required actualRED witnessed before production: existing pytest -q tests/unit/test_element_identity_snapshot_preview.py,1failed0.21s exactly AttributeError IdentityStore has no preview_identity_history. Actual initialize/reserve/ElementCreate/encode_request/canonical PublicationOperation all succeeded before the new entry call. Worker notified root and is proceeding to shared planner/overlay implementation. No scope expansion or live change.

Worker focused checkpoint: firstGREEN1passed0.26s; expanded preview module57passed4.36s. Self-review identified existing source-store caller of publication _children and preserved original _children(request,spec_id) wrapper while extracting operation_children(operations,spec_id); no source-store production edit. Root confirmed changed-file inventory stays within allowed shared-helper/preview files and required source-store v2/frozen compatibility in final nine-module run. Graph/rendering, no-lease, prohibited-writer tripwires/docs/report and once-only covering run remain, not task complete.

Worker once-only nine-module covering checkpoint656passed49.56s, exit0/pristine, staged treef6af35e69c2fa5f62ab9382e32afb65020cb7092. Includes60preview cases and retained/lifecycle/binding/projected-binding/publication/codec/graph/source-store modules. No code amendment after covering reported; full self-review/report/scoped commits pending before independent review. Root progress remains unstaged. Not task completion.

Worker DONE34f4616b23860d30071c40c5ab685d03c46c1379 plus report79ba8f55159e5d520567be1fcf63e7f4168f630c. Main read FULL211-line report. It clarifies source _children compatibility was an actual first-implementation failure1failed0.29s (PublicationIntentRequest not iterable), corrected by preserving old wrapper before1passed0.26s. Expanded tests5passed1.82s→57passed4.36s→60passed4.76s; final SQLite no-backup/savepoint/attach/vacuum+single-connection tripwire refinement1passed0.24s before nine-module656covering. No production/test/docs after covering treef6af35e6; only report added. 60preview cases/17functions include independent complete expected rows, all seven families, lineage/nullable/history cases, 5000-digit fixture values, query-only and graph render equality. Fixtures are not a capacity measurement or billions of real revision writes.

Fresh Astra/high /root/review_identity_proposed_history dispatched full64-line brief/211-line report and full originalBASE5b3442aa..HEAD79ba8f55 two-commit81033byte diff. Complete bounded nonoverlapping coverage required, no unchanged656repeat; both spec and quality verdicts pending. Intermediate reviewer has read1–900diff without truncation, continuing remainder and named boundary checks. No next plan selected.

Fresh review completed: spec compliant and task quality approved, no Critical/Important/Minor findings. Full64-line brief,211-line report and both commits'1,333-line diff read in nonoverlapping chunks1–300/301–600/601–900/901–1130/1131–1333 without truncation. Reviewer repaired a cut-off SQL ordering window at snapshot.py46–91 and checked projected binding fallback and the source-store _children caller; no tests rerun or files changed. Cannot-verify chronology/provenance resolved by root's witnessed actual firstRED and recorded once-only656-test pristine covering checkpoint at treef6af35e69c2fa5f62ab9382e32afb65020cb7092; subsequent report-only commit did not amend production/test/docs. No external rollout performed or authorized. Ruling27 complete; later source/semantic/runtime integration remains required.

Task 1: complete (commits 5b3442aa..79ba8f55, review clean)
