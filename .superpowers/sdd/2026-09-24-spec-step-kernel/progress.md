# SDD ledger — plan: docs/superpowers/plans/2026-09-24-spec-step-kernel.md
Implementation base: b24259611015ca9904c97e448acc823ce49bb95f
Pre-flight: Tasks 2→3 — sealed `PreparedSpecStep`/marker documents feed atomic state transitions; names and cursor contract agree.
Pre-flight: Tasks 2+3→4 — document loaders/receipts and store transitions feed the recovery kernel; interfaces agree on marker advancement and final commit.
Pre-flight: Tasks 2+4→5 — prepared steps and bound receipts feed the effect adapter; canonical effect names agree.
Pre-flight: Tasks 3+4+5→6–8 — store, drain kernel, and adapters feed routed/manual, terminal, and resolution cutovers; all use one `step_id` and no dual-write.
Pre-flight: Tasks 6–8→9 — every active origin cuts over before retired protocols are deleted; ordering is consistent.
Pre-flight: Task 9→10 — ownership cleanup precedes controller flattening; the shape guard can enforce the final boundary.
Pre-flight: Tasks 1–10→11 — current-only behavior and all cutovers feed documentation and repository verification; no interface conflict found.
Task 1: Ruling: use the plan's 1,014-test state/controller partition as the per-task regression suite and reserve the 9,000+ test repository gate for Task 11 — the approved plan explicitly schedules one Git-bound full gate after all eleven tightly coupled slices, while repeating it per slice would add roughly six hours — cost if wrong: a cross-partition regression may be discovered at the final gate instead of this task.
Task 3: Ruling: export `validate_spec_step_marker` from Task 2's schema module and modify `spec_step.py` in this task — the state store must consume the one canonical exact-schema parser rather than duplicate marker validation — cost if wrong: this adds one small public schema helper beyond Task 2's listed interface.
Task 1: complete (commits b242596..5b51a6a, tests: ../../.venv/bin/python -m pytest -q tests/unit/test_phase_a_state_version.py tests/unit/test_spec_service_boundary.py tests/kernel/test_squad_state.py tests/integration/test_squad_controller.py tests/integration/test_human_input_routing.py -x → 1015 passed in 445.04s (0:07:25))
Task 2: complete (commits 5b51a6a..c7ab858, tests: ../../.venv/bin/python -m pytest -q tests/unit/test_spec_step.py tests/unit/test_squad_completion.py tests/unit/test_squad_publication.py tests/unit/test_squad_publication_inspection.py -x → 414 passed in 15.20s)
Task 3: complete (commits c7ab858..ef13154, tests: ../../.venv/bin/python -m pytest -q tests/kernel/test_spec_step_state.py tests/kernel/test_squad_state.py tests/kernel/test_prepared_phase_result.py -x → 698 passed in 12.66s)
Task 4: complete (commits ef13154..380cfd6, tests: ../../.venv/bin/python -m pytest -q tests/unit/test_spec_step_kernel.py tests/unit/test_spec_step.py tests/kernel/test_spec_step_state.py -x → 38 passed in 10.39s)
Task 5: Ruling: land the explicit adapter and hardened publication implementation before origin cutover, while the seven completion-effect entry points remain fail-closed until Tasks 6–8 supply each origin's sealed input shape — this preserves the old production path and prevents an adapter from accepting incomplete authority — cost if wrong: the first cutover task must finish more adapter wiring than the original task partition implied.
Task 5: complete (commits 380cfd6..b2fb23d, tests: ../../.venv/bin/python -m pytest -q tests/unit/test_spec_step_effects.py tests/unit/test_squad_completion.py tests/unit/test_squad_publication.py tests/unit/test_squad_publication_inspection.py tests/integration/test_squad_controller.py -k 'completion or publication or journal or timing or checkpoint or context or mining or retarget' -x → 575 passed, 336 deselected in 90.28s (0:01:30))
Task 6: Ruling: route new Phase A advances through one state-authoritative `pending_spec_step`, while temporarily retaining the sealed completion outbox only as a non-authoritative effect-compatibility companion until Task 9 deletes the retired implementation — this removes split state ownership now without rewriting every effect in the same slice — cost if wrong: the temporary companion adds staging and marker-translation code that must be deleted on schedule.
Task 6: Ruling: preserve the established checkpoint-before-quality execution order in the canonical spec-step order — quality restoration consumes the preceding checkpoint receipt and changing production ordering during lifecycle cutover would create behavioral drift — cost if wrong: the design's earlier illustrative ordering and one kernel fixture required correction.
Task 6: Ruling: validate and discard unreferenced spec-step stages on restart before sweeping companion completion stages — a crash before the begin-step CAS otherwise leaves durable but authority-free staging — cost if wrong: cleanup now contains one additional bounded directory scan that Task 9 may simplify.
Task 6: complete (commits 9a01e02..5c914f6, tests: ../../.venv/bin/python -m pytest -q tests/integration/test_squad_controller.py tests/kernel/test_squad_state.py tests/unit/test_squad_phase_checkpoints.py tests/unit/test_phase_a_readiness.py -x → 803 passed in 330.41s (0:05:30))
Task 7: Ruling: seal the terminal done-state skeleton before effects, then project only receipt-derived terminal provenance, inventory digests, and retarget finalization fields from authenticated durable spec-step receipts during `commit` — those values do not exist before mining/retarget effects, while the common marker still owns the only state transition and completed effects never replay — cost if wrong: `complete_spec_step` temporarily retains terminal-specific compatibility projection until Task 9 removes the companion completion protocol.
Task 7: complete (commits 821d26b..eaab4a4, tests: ../../.venv/bin/python -m pytest -q tests/integration/test_squad_controller.py tests/unit/test_phase_a_readiness.py -k 'terminal or readiness or spec_step' -x → 47 passed, 480 deselected in 23.78s)
Task 8: Ruling: retain the old prepared completion only as a sealed, non-authoritative compatibility companion beneath `pending_spec_step` until Task 9 deletes its adapters — managed publication, context, quality, and release keep their existing authentication while the common step owns the only durable state transition — cost if wrong: Task 9 must remove more translation code than the plan's nominal file list suggests.
Task 8: Ruling: bind managed publication transactions to the common `step_id` at their final sealing sites — the publication transaction and state transition must share one replay identity, while earlier draft publication builders remain unchanged — cost if wrong: a producer-specific staging path could still derive an older transaction identity and fail closed during publication.
Task 8: Ruling: verify the shared managed path with one complete provider route per distinct lifecycle plus focused replay/security coverage, and defer the duplicate provider matrix to Task 11 — individual nested managed fixtures take 20–30 minutes and exercise the same sealed adapters — cost if wrong: a provider-specific fixture mismatch may be discovered only at the final repository gate.
Task 8: complete (commits eaab4a4..WORKTREE, tests: core controller/completion/restoration batch → 592 passed in 585.17s; managed alignment question recovery → 1 passed in 1765.32s; managed feasibility partial-publication recovery → 1 passed in 647.72s; managed alignment handoff recovery → 1 passed in 1675.57s; final debt-resolution smoke → 1 passed, 504 deselected in 1.78s; compileall and diff check passed)
