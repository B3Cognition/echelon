# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-graph-active-source-view.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / existing physical layout | phase_a_start creates runs/<run>/specs/<id>; captured assembly currently selects specs/<id>. | Explicit optional source-tree selector bridges only the pure graph view; no runtime ownership or state transition is inferred. |
| 1 / local structure | Existing structure uses relative captured tree files and emits canonical paths. | Reuse original selected tree with existing owner; no duplicate parser, rebased carrier/manifest or new graph builder. |
| 1 / physical manifest | All original selected bytes/modes/membership must validate before graph filtering. | Preserve complete existing raw fingerprint equality; ignored canonical/other-run/binary images do not escape validation. |
| 1 / logical table | Active bytes and stale canonical bytes may coexist physically. | Active selected tree exclusively owns canonical logical spec subtree, including absence; remove old subtree with component-aware ancestry, no per-file fallback. |
| 1 / selector syntax | Only canonical or exact runs/<one component>/specs/<same id>. | Existing normalized UTF-8 physical-path grammar plus layout check, no guessed active run or namespace/run-admission claim. |
| 1 / existing domains | Policy/planner/memory names are canonical; RE paths stay unchanged. | All domain byte comparisons use one private logical table; supplied stale canonical content or physical run-local spellings do not acquire a second naming policy. |
| 1 / graph identity | Canonical keys and retained old evidence are invariant. | Full rendered comparison against existing canonical builder plus independent revision1/current2 assertions; native memory planning, explicitly fixture-only audit. |
| 1 / missing and empty | Selected root omission differs from explicitly selected missing or empty trees. | Omission fails; selected missing/empty do not use canonical leftovers; original physical manifests remain distinct. |
| 1 / mutation and runtime | Adapter returns graph only; publication still consumes original physical capture. | Input ownership and purity tests; one actual guarded active-root promotion confirms physical projection while canonical bytes remain unchanged. No graph/completion callback. |
| 1 / test cycle | New API requires a genuine first RED; existing APIs must work before it. | First native fixture validates complete expected graph/old evidence then fails on missing keyword. Five covering modules once, all runs and trees retained. |
| 1 / boundaries | Approved design's run ownership, graph admission and producer gates are not implemented by this selector. | Explicit remaining-integration section, no live caller or activation. Scope is independently testable without pretending runtime integration exists. |

Root read approved durable identity design and current assembler, local structure scope/relative-tree implementation, native source snapshots/normalization and existing graph fixtures. Previous combined composition phase independently approved after451 covering tests at checkpoint14a988bb, with its one deferred Minor retained in its owning ledger for final review. The root checkpoint's git add returned an ignored-directory warning but staged both already-tracked exact administrative paths; resulting commit contains only design/progress and status confirmed only the next plan remains untracked. No test/source amendment.

Ruling: 32. Add one explicit inactive spec_source_path selector limited to canonical or runs/<one normalized component>/specs/<same spec>; the selected active tree exclusively supplies canonical logical spec bytes, while the complete original physical observation is validated and retained unchanged — existing run-local authoring layout needs stable canonical graph keys without stale canonical fallback or inventing another source authority — if wrong, revise the inactive selector/path contract and logical-view tests or replace it with an explicitly selected adapter; no published IDs, revisions, graph keys, source/journal wire, physical artifacts or live installation require rewriting. This is not managed run authentication, canonical mirroring or a run-transition protocol.

Self-review: one task, scoped existing module plus new tests/docs; interface keeps old defaults and delegates local parsing/domain rules; no public rebased carrier or generic alias framework; raw-source authority is not relabeled. Placeholder scan returned no matches. All remaining approved integrations are listed rather than falsely marked complete. No implementation dispatched yet. Root will commit plan/brief/preflight and record exact BASE before fresh implementation.

Task1 dispatched fresh /root/captured_graph_active_source_view Sol/high at BASEc38c66f26ae7e0f403ce929afea17a6b868f8002 with exact52-line brief, complete constraints, real RED-before-code, five covering modules once, full report/tree chronology and no-subagents contract. Narrow existing-owner integration fits standard model. Root progress now intentionally dirty/excluded. No next implementation selected.

First genuine RED reported before any production edit: exact parent pytest/workdir tests/unit/test_spec_graph_active_view.py::test_run_local_view_matches_canonical_graph_and_retains_history -q ->1failed, TypeError unexpected keyword spec_source_path. Real filesystem/IdentityStore/native planner fixture and independent expected graph assertions passed before new call; no fixture correction. Worker proceeding to minimal implementation and focused GREEN.

First focused GREEN after minimal selector/private-byte-table change: same exact command1pass0.65s. Expanded boundary/purity/domain/physical-publication coverage and the five-module covering run remain outstanding; no runtime activation.

Worker checkpoint after all coverage groups: selector/default/malformed/absent; missing-empty/no fallback; no blending/byte ownership/logical names; all domains/RE; graph self-exclusion; full raw validation; coherent ignored data; ownership/purity/BaseException; actual run-local guarded publication. Expanded23-case run had2 identity-history fixture failures (corrected by importing required fixture labels); later38-case run had1 sorted-tree index assertion failure (corrected to exact-path selection). The once-only five-module covering run passed290tests9.05s, staged tested tree b15dc1c7ca2265d279b3ebe9b7e646c18a43daef. Root progress excluded. Worker proceeding with commits/report only; full report and review outstanding.

Worker DONE at0e2f7d37551f16cc3e5167f7a47daa4b4bffa31f plus report-only cee2dc7f1c21e67d79c8709585505f0c8a476797. Root read complete198-line report. Additional honest chronology: diagnostic wildcard import omitted underscore helper and failed NameError, corrected module import exposed expected empty-history fixture error; module runs23pass0.67s,37pass0.76s, then1fail37pass0.79s corrected38pass0.78s. Self-review simplified four-component layout check and exact-path detachment fixture before sole covering290pass9.05s. Root git show verified tested implementation tree b15dc1c7ca2265d279b3ebe9b7e646c18a43daef, later diff only report. Fresh Sol/high /root/review_captured_graph_active_source_view reviewing complete49521-byte original-BASE package, exact brief/report/constraints; no unchanged reruns. Review pending.

Fresh review: spec compliant, quality Approved, no Critical/Important or Cannot verify items. Reviewer checked exact source-path string/UTF-8 validation in unchanged owner and necessarily read the assembler's cut-off trailing hunk to confirm selected original tree/native structure and private table consumers. No tests rerun or mutations. One Minor below. Ruling32 is implemented; no new ruling from review.

Task 1: minor (deferred): tests/unit/test_spec_graph_active_view.py:23,32 imports PublicationImageDescriptor and _three_domains are unused; remove in final whole-branch cleanup triage. No task fix loop for this harmless import polish.

Task 1: complete (commits c38c66f2..cee2dc7f, review approved; one deferred Minor). Five-module final coverage290pass9.05s, implementation tree b15dc1c7ca2265d279b3ebe9b7e646c18a43daef; later report/checkpoint administrative only. Keep workspace for final deferred-minor/rulings review. Full runtime/producer/repair integration and final full-unit suite remain outstanding; no installation or live trial.

## Read-only next runtime observations (not selected protocols)

The execution lease is an earlier owner than _run_locked: Squad._run_with_execution_lease acquires PhaseA and SpecRun locks, then drains pending completion, emits retarget comparison and cleans completion orphans before invoking the phase callback. An eventual managed gate must therefore precede this recovery/cleanup, not merely agent dispatch or _run_locked. _run_locked itself handles human-input, budget and phase recovery state writes. _run_single_phase_locked claims failed automatic decisions before applying initial_state_updates and can isolate/copy a spec tree. resume_pending_human_input can drain completion and rearm decisions. No gate placement or runtime change is selected in this graph task.

Squad constructor first loads state, reads existing telemetry manifest and builds TelemetryStore; its constructor retains fields/lock only and does not itself create telemetry files. SquadStateStore.load uses a shared state lock and validates any present managed_identity shape, but absence is currently legacy-shaped and the read does not authenticate the ledger. IdentityStore.check_managed_context compares independently selected spec/run and full supplied record to immutable retained genesis plus current source context in one query-only transaction. It does not classify missing metadata or select a run. The genesis table has an independent unique run_id, so future runtime admission must consider retained run ownership as well as untrusted state spec IDs; no new lookup API or classification policy is selected here. Runtime source selection/managed enrollment and late-run transition remain unimplemented.

Confirmed completion wire owner is harness/squad_completion.py: _validate_publication is closed kind=none or kind=external with exact marker; schema1 intent limit4MiB, ordered effects journal/timing/checkpoint/quality/context/mining/retarget. Existing publication_binding_sha256 binds that value, not an identity request. _drain_pending_controller_completion authenticates quality/product stage, calls staged_publication.publish, then hands off state and discards stage. A future closed identity bundle cannot be slipped into the existing publication value as an unvalidated extra field, and the opaque identity completion string is not proof of these effects. No schema/bundle/release-order policy selected here.

Confirmed executor owner harness/squad_executors.py: _phase_prompt_metadata grants exclusive product writes only for SAGE phase1-why2/phase3-consensus and otherwise returns metadata; missing spec_dir also returns unchanged. _exec_agent_with_contract conditionally passes metadata based on provider capabilities/signature and runs with project_root cwd. Managed author/reviewer isolation must use an explicit enforced capability and selected candidate paths, not silently reuse these legacy fallbacks. Workflow actual producer IDs include phase1-discover, phase1-synthesizer (not phase1-synthesize), phase1-why1/why2, phase3-consensus, phase4-document. No producer reservation/proposal protocol was selected while this task runs.
