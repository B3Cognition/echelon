# SDD ledger — plan: docs/superpowers/plans/2026-09-13-captured-graph-active-source-view.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / existing physical layout | phase_a_start creates runs/<run>/specs/<id>; captured assembly currently selects specs/<id>. | Explicit optional source-tree selector bridges only the pure graph view; no runtime ownership or state transition is inferred. |
| 1 / local structure | Existing structure uses relative captured tree files and emits canonical paths. | Reuse original selected tree with existing owner; no duplicate parser, rebased carrier/manifest or new graph builder. |
| 1 / physical manifest | All original selected bytes/modes/membership must validate before graph filtering. | Preserve complete existing raw fingerprint equality; ignored canonical/other-run/binary images do not escape validation. |
| 1 / logical table | Active bytes and stale canonical bytes may coexist physically. | Active selected tree exclusively owns canonical logical spec subtree, including absence; remove old subtree with component-aware ancestry, no per-file fallback. |
| 1 / selector syntax | Only canonical or exact runs/<one component>/specs/<same id>. | Existing normalized UTF-8 physical-path grammar plus layout check, no guessed active run or namespace/run-admission claim. |
| 1 / existing domains | Policy/planner/memory names are canonical; RE paths stay unchanged. | All domain byte comparisons use one private logical table; supplied stale canonical content or physical run-local spellings do not acquire a second naming policy. |
| 1 / graph identity | Canonical keys and retained old evidence are invariant. | Full rendered comparison against existing canonical builder plus independent revision1/current2 assertions; native memory planning, explicitly fixture-only audit. |
| 1 / missing and empty | Selected root omission differs from explicitly selected missing or empty trees. | Omission fails; selected missing/empty do not use canonical leftovers; original physical manifests remain distinct. |
| 1 / mutation and runtime | Adapter returns graph only; publication still consumes original physical capture. | Input ownership and purity tests; one actual guarded active-root promotion confirms physical projection while canonical bytes remain unchanged. No graph/completion callback. |
| 1 / test cycle | New API requires a genuine first RED; existing APIs must work before it. | First native fixture validates complete expected graph/old evidence then fails on missing keyword. Five covering modules once, all runs and trees retained. |
| 1 / boundaries | Approved design's run ownership, graph admission and producer gates are not implemented by this selector. | Explicit remaining-integration section, no live caller or activation. Scope is independently testable without pretending runtime integration exists. |

Root read approved durable identity design and current assembler, local structure scope/relative-tree implementation, native source snapshots/normalization and existing graph fixtures. Previous combined composition phase independently approved after451 covering tests at checkpoint14a988bb, with its one deferred Minor retained in its owning ledger for final review. The root checkpoint's git add returned an ignored-directory warning but staged both already-tracked exact administrative paths; resulting commit contains only design/progress and status confirmed only the next plan remains untracked. No test/source amendment.

Ruling: 32. Add one explicit inactive spec_source_path selector limited to canonical or runs/<one normalized component>/specs/<same spec>; the selected active tree exclusively supplies canonical logical spec bytes, while the complete original physical observation is validated and retained unchanged — existing run-local authoring layout needs stable canonical graph keys without stale canonical fallback or inventing another source authority — if wrong, revise the inactive selector/path contract and logical-view tests or replace it with an explicitly selected adapter; no published IDs, revisions, graph keys, source/journal wire, physical artifacts or live installation require rewriting. This is not managed run authentication, canonical mirroring or a run-transition protocol.

Self-review: one task, scoped existing module plus new tests/docs; interface keeps old defaults and delegates local parsing/domain rules; no public rebased carrier or generic alias framework; raw-source authority is not relabeled. Placeholder scan returned no matches. All remaining approved integrations are listed rather than falsely marked complete. No implementation dispatched yet. Root will commit plan/brief/preflight and record exact BASE before fresh implementation.
