# SDD ledger — plan: docs/superpowers/plans/2026-09-12-publication-history-binding.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / previous history preview | Preview observes a complete proposed graph history but leases no baseline; ordinary journal planning checks only touched effects. | Add opt-in complete proposed-history fingerprint checked inside prepare/apply, not a graph writer or second lifecycle authority. |
| 1 / source CAS | Accepted source head covers selected physical bytes, not every unrelated direct identity-ledger mutation before pending ownership. | The materialized-history digest independently closes the complete graph-history race. Source and history claims remain distinct. |
| 1 / request compatibility | Existing request fields are manifest, recovery, operations, sources; v1/v2 canonical payloads retained. | Append optional digest; new closed v3 with optional complete sources. Old field order and bytes unchanged. |
| 1 / storage compatibility | No new persistent column or table is needed because request/receipt already retain canonical versioned JSON and digests. | Keep schema6 and marker1; old code rejects unsupported v3 when interpreting it. No implicit migration or old-binary live rollout claim. |
| 1 / shared overlay and pending owner | Public pre-intent preview rejects pending; prepared application has authenticated retained parent/children. | Extract minimal pure overlay only; validated apply combines current captured history and existing plan without weakening public preview. |
| 1 / recursive capture | capture full-audits publication _load; source reads and child guards use bounded envelope checks. | Do not recapture in _load/_application/source reads. Prepare and still-prepared apply are the full-history owners; final check follows complete applied-state updates inside same transaction. |
| 1 / exact retry and retained evidence | Released old snapshots legitimately differ from current history after later publication. | Original request/receipt association checked, old exact retry returns retained receipt before fresh-history check. No relabeling or comparison to today's head. |
| 1 / source application envelope | Source _parent, _bound_row and accept currently assume v1/v2 and reconstruct parent receipts. | Extend exact closed v3 association through existing owners with a narrow shared pure metadata helper if needed, without child-history scans. |
| 1 / postcheck rollback | Extra unrelated valid rows could escape child-specific receipt checks. | Actual full-history fingerprint checked after all child/source/parent updates and before commit; any failure rolls back everything. Fault fixtures must be identified as faults, not allowed public writes. |
| 1 / evidence and scope | First RED uses real existing allocator/preview before new keyword; source case uses actual capture and guarded publication. | Journal-only fake manifest is not physical proof; independent wire/receipt/graph assertions plus actual source integration required. |
| 1 / tests and activation | Eight named modules are existing except the new history module; no live caller changed. | Cover once, focused later amendments only; all producer/runtime/semantic/graph/completion/repair integration remains outstanding. |

Ruling: Bind the exact proposed complete materialized-history SHA to an opt-in version3 journal request and application receipt, checking it before new preparation, before prepared application and before commit — touched-entity revision checks and source-head CAS alone do not bind a graph containing unrelated retained history, while an after-the-fact graph rewrite violates sealed source ownership — if wrong, revise the opt-in payload/enforcement adapter and equality/concurrency compatibility tests, with an explicit payload migration if any v3 records ever exist outside this inactive worktree; do not renumber IDs, rewrite accepted revisions/evidence/graph keys, silently downgrade old receipts or activate live consumers. This is Ruling28 in the chronological final handoff.

Root read the complete approved design, previous preview plan and new task interfaces; publication DTO/store and snapshot preview in full; source envelope/current-head checks and history capture structure directly. New plan has one coherent transaction/wire compatibility task; no parallel implementer. Original preview completed at34f4616b/79ba8f55 with clean review and656 covering tests; checkpoint53cd4ce2. Root-only spec checkpoint has no test rerun requirement. Task pending brief/BASE/dispatch. Plan artifacts retained until final whole-branch review and exhaustive rulings handoff.

Task1 dispatched fresh /root/identity_publication_history_binding Astra/high, BASE6879b17c583f38c0b83a8b94d3408d7df2be66ec,58-line brief and complete implementer/report contract. Ruling28 and no-recursion/indexed-read boundaries carried. Existing real allocator/preview must succeed before first unexpected-keyword RED; root witness required before production. Eight named covering modules once, no broad/capacity/live repetition. Root ledger intentionally unstaged and excluded from worker commits. No next plan selected.

Required actual RED witnessed before production: existing pytest -q tests/unit/test_element_identity_publication_history.py,1failed0.23s exactly TypeError PublicationIntentRequest.__init__ unexpected keyword proposed_history_sha256 at testline11. Real initialize/reserve/canonical child encoding/preview all succeeded first. Worker confirms only new test edited before notification, and is proceeding with shared request/overlay/journal integration. No live change.

Worker minimal implementation GREEN on same focused module1passed0.26s. Reported optional request field/v3 codec, pure overlay extraction, prepare/pre-apply/post-effect whole-history checks and shared application metadata for source envelopes. Full adversarial/compatibility/real-source tests and once-only covering run remain; no task completion claim. Root's prospective graph-builder inspection found existing base builder reads mutable files and live memory audit adapters; no next API/policy or plan selected from that exploratory read.

Focused worker checkpoint:40passed3.96s, then expanded56passed/1fixture-failure6.17s (missing restore destination directory). Actual source prefix interruption/reopen, source/application hashes, graph equality/old evidence, retained retries after later history and no-capture/indexed-read cases passed before that final fixture step. Worker fixing setup only; no production integration issue reported, covering eight-module run not yet performed. The57-case run is not all-pass evidence.

Worker DONE implementation554f83c94176115fc98040dde883ee9e62b42dd0 and report563665a38713eb7575c4b2ee1b92aa82c98070b2. Main fully read226-line report. First restore destination setup fixed→1passed0.78s. Later18rehashed association cases passed but second real guarded source setup failed missing runs/second (1failed18passed2.51s); test-only directory fix→1passed1.06s. Final stale-prepare authorizer before-any-insert test1passed0.34s. No production amendment after firstGREEN, all tests/docs additions before final once-only eight-module run576passed42.31s, exit0/no skips/no reported noise. Staged and implementation commit tree4425f8caaf6ebe870bc150abcbf7c8f83dd26f65; only report added afterward. No unchanged repeats. Source/graph/retry/restore/corruption/transaction-boundary/indexed-read evidence and exact limitations recorded; live integrations remain off. Independent full original-BASE review pending, no next plan selected.

Fresh Astra/high /root/review_identity_publication_history_binding reviewing full58-line brief/226-line report and originalBASE6879b17c..HEAD563665a3 two-commit83398byte package. Interim: complete brief/report and diff1–960 read without truncation; remaining fault/recovery tests and named capture/audit/source-parent checks pending. Both verdicts required before task completion. No next plan selected.

Fresh review complete: spec compliant and task quality approved; no Critical/Important/Minor requiring action. FULL58-line brief,226-line report and both commits'1,249-line diff in bounded nonoverlapping chunks with no truncation. Cut-off overlay/application/loader/source-parent windows repaired and named request-detachment/sole-transaction/capture/guard/source-acceptance interfaces checked. No tests rerun or probes. Cannot-verify execution chronology resolved by root's witnessed firstRED and once-only576-pass covering evidence at4425f8caaf6ebe870bc150abcbf7c8f83dd26f65 with report-only afterward. External untouched-workspace claim is artifact/process scope evidence, not a global filesystem attestation: root and worker performed no external rollout and have no authority for it. Future integration explicitly remains outstanding, so no missing task requirement inferred. Ruling28 complete.

Task 1: complete (commits 6879b17c..563665a3, review clean)
