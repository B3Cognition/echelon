# SDD ledger — plan: docs/superpowers/plans/2026-09-12-managed-identity-state-preservation.md

## Preflight

| Tasks | Shared interface or self-check | Result |
| --- | --- | --- |
| 1 / genesis record | Existing managed receipt is a flat ten-field string record; request codec validates seven fields. | Reuse request validation and add exact version/spec/operation envelope, no new wire schema or provenance claim. State field is named managed_identity; absent differs from present None/false/empty. |
| 1 / transaction namespace | PHASE_A_IDENTITY_KEYS is both store-owned and implicitly routing-mutable/removable. | Add separate MANAGED_IDENTITY_KEYS to STORE_OWNED only, not PHASE_A or routing/control sets. Existing prepared ownership/attestation checks must enforce it, tested through real APIs. |
| 1 / initializer and common writer | Existing initializer resets the state; common writer supplies durable CAS/backup/replace semantics. | Explicit initial metadata admission only through initializer; same-run reinitialization preserves retained record under same lock, ordinary removal/introduction/replacement fails. No database reads under state lock, no second writer. Ruling26 below. |
| 1 / first versus later runs | Genesis records the first run, not an active physical source pointer. | This state format is initial-run-only, validates spec/run association, but does not freeze mutable spec_dir to genesis root. Later run/source transitions remain explicit integration, not a genesis rewrite. |
| 1 / controller fresh initialization | Existing whitelist omits new metadata and calls initialize without the new optional keyword. | Same-run preservation inside existing initializer keeps metadata through this path without modifying controller production or default call shapes. Exact named real-controller/simulated-provider regression required; concrete extra owner requirements escalate before scope change. |
| 1 / malformed prior state | Existing legacy initialization can ignore malformed JSON; missing managed metadata needs durable registry evidence to classify. | Present malformed managed records reject; explicit managed initialization of malformed old JSON rejects. Unknown legacy malformed/deleted files retain old policy; no substring authority or false whole-system downgrade claim. Runtime registry gate remains required. |
| 1 / write rejection ordering | Existing backup write happens early in _save_unlocked. | New metadata validation must precede backup/temp/state writes; preserve unrelated legacy/durability/attestation/human-input behavior. Exact state+backup bytes asserted on rejection. |
| 1 / first regression | New initializer keyword must be the failure after real existing genesis succeeds. | Actual source capture/store/source/genesis registration, explicit scoped secure-POSIX fixture, then missing managed_identity keyword. Pure record tests remain platform-independent. |
| 1 / test scope and modes | Shared state writer plus preparation namespaces affect normal and automatic paths. | New module+kernel state/prepared+namespace+managed tests, only named newly added controller integration cases; guided/semi/banzai same integrity behavior. No entire unchanged huge controller suite/capacity/live/provider/full-unit repeat. |
| 1 / approved overall design | Runtime lookup, provider isolation/reservations, source/graph recovery and repair remain. | Current phase is state protocol preservation only; no live caller starts supplying metadata. Physical isolation/manual relocation authority and registry freshness are not certified by preserving a field. |

Ruling: Admit managed genesis state metadata only through the explicit initializer, preserve its exact record on same-run reinitialization, and exclude it from all generic provider/routing update and removal authority — silently dropping or replacing the field during normal state initialization would make later enforcement optional, while genesis must remain distinct from future run/source transitions — if wrong, add a narrowly authenticated state/run-transition interface or adjust initializer preservation tests rather than weakening generic ownership, rewriting genesis/element IDs/revisions/graph keys, or altering existing live runs. This is Ruling26 in the chronological final handoff.

Main fully read the complete87-line plan, existing SquadStateStore constructor/load/save/initialize/common writer, prepared ownership/removal checks, state namespace sets, both controller initialization/manual replay paths and actual test module names/fixture headers. The immutable record format matches completed schema6 registration. No collision with an existing managed state field. No new active-run/relocation/proposal/graph API is selected. One task because shared writer and namespace must protect the same field consistently. Existing worktree verified. Most-capable implementation/review warranted for durable writer/initialization/attestation compatibility.

Task1 pending plan commit/brief then fresh implementer. Root owns plan/ledger; one implementer/no children. Preserve artifacts through final whole-branch review and exhaustive rulings handoff.

Task1 dispatched /root/identity_managed_state_preservation Astra/high, BASEb70afc3c8d57870253af4c9d63f49513f1bb651c,65-line brief plus full implementer/report contract. Root clarified separate reserved namespace (not PHASE_A routing-mutable set), exact genesis versus current physical scope, same-lock initializer preservation, validation-before-backup and unchanged unknown-legacy malformed-file policy. Actual missing-keyword RED after genuine source/genesis setup required before production. Five named complete modules plus only new named controller tests, no broad controller/capacity/live repeats. No root ledger/observations in worker commits. Prior managed registration complete atf66cc81a/checkpoint6749d219, do not redispatch it.

Required actualRED before production: real POSIX capture, source registration and managed genesis succeeded; exact test_initialize_accepts_real_managed_genesis failed at initialize TypeError unexpected keyword managed_identity,1failed0.36s. Module-local secure_posix fixture defined up front. Worker adding focused rejection tests then validator/existing-owner implementation. No live activation.

Worker interim checkpoint: first66-case validator/state pass GREEN. Additional focused routing/recovery/race/fault coverage passed after test expectations were aligned to existing exact-write error semantics. Required actual fresh-controller test passes guided/semi/banzai without controller production changes; actual simulated-provider manual replay preserves metadata and injection/replacement rejects before dispatch,14 focused kernel/controller cases passed. Docs/self-review and once-only final five-module plus named-controller covering run pending. These are interim claims, not independent review or phase completion.

Worker DONE91a4c48afd0b4c0f6cda18dd95f78b5e2c178d2d. Main read FULL139-line report. Final five modules plus exactly three new named controller tests passed912 in12.69s, exit0/pristine/no skips or warnings, staged implementation/test/docs tree63ce01175c7f285de1ad6e216e32ddba20c4dfa7. Only report added afterward, no production/test amendment. Actual first RED witnessed. Additional self-review actualRED showed fromNone suppressed display but retained hostile exception context; pure validator/state helper now raise after handler,7 focused tests passed before covering run. Interim fault tests corrected expected existing StateAdvanceError rather than OSError; envelope tests corrected read-only property targets to actual frozen private fields, neither represents production failure. Actual fresh controller dispatch (one simulated call then bounded interruption) and manual replay tested; ten named-controller parameter cases. Root ledger intentionally dirty and excluded from worker commit.

Fresh Astra/high task review dispatched /root/review_identity_managed_state_preservation with FULL65-line brief,139-line report and full original BASEb70afc3c..HEAD91a4c48a package63494bytes, one commit. Complete non-overlapping bounded coverage required, no re-running passing tests or broad branch review. Pending independent verdict, not task complete.

Fresh review completed FULLbrief1–65/report1–139 and63494byte1315line diff in non-overlapping1–220/221–440/441–660/661–880/881–1100/1101–1315 chunks, no truncation/cut-off repairs. No changed/unchanged file rereads, git or filesystem mutations. Novel in-memory initialize probe (three setup repairs before target reached) verified malformed prior JSON retained in JSONDecodeError.__context__.doc despite suppressed display. Spec NOT compliant, quality Needs fixes: one Important at squad_state.py4367/2388, bounded managed errors raised inside except handlers retain prior JSON. No Critical/minors. Review Cannot Verify chronology resolved by main actualRED witness; future registry/deletion/source-scope limits match explicit task boundary and remain outstanding overall, not completed claims.

Task1 fix round1/5 dispatched original /root/identity_managed_state_preservation, FIX_BASE91a4c48afd0b4c0f6cda18dd95f78b5e2c178d2d. Verbatim finding carried, actual real-owner RED then narrow post-handler raising correction, original legacy JSON policy and state/backup ordering preserved. Cover amended identity-state and kernel-state modules only; no repeat912/controller/full/capacity. Full fix report required. Root's next draft plan/workspace and dirty ledgers excluded. Pending correction and scoped fresh review.

Fix1 actualRED checkpoint: real initializer and locked writer on malformed on-disk JSON,7failed3passed0.31s. Both parse wrappers retained JSONDecodeError context; initializer record-validation wrapper also retained its bounded ValueError. Worker fixing all three new wrapper sites; BaseException controls already passed and backup/temp tripwires confirmed no writes on rejection. No blanket legacy error-policy rewrite.

Fix1 worker DONEe5130edc9b0bf4bae0a7aa8c48d9adb0b4a6148e; main read entire appended140–208 report lines (original1–139 already fully read). Boolean parse-failure and None validation-failure sentinels move three bounded raises outside handlers. Final amended identity-state/kernel-state run354passed2.49s exit0/pristine/no skips or warnings, stagedf81c3347c1217c3ba521cd4c2186134c7b30ec97; only report appended afterward. No production/test changes after covering. Prior912explicitly pre-fix. Fresh scoped Sol/high /root/review_identity_managed_state_fix1 dispatched full65-line brief,208-line report and fullFIX_BASE91a4c48a..e5130edc16065byte one-commit diff, no repeats, finding and fix-only breakage. Pending verdict.

Scoped fresh Sol review completed FULLbrief1–65/report1–208/diff1–302 in bounded non-overlapping chunks. Both parse-owner finding and same-mechanism supplied-record wrapper ADDRESSED, no new breakage or out-of-scope observations. Actual real-owner no-context/cause, unchanged state/backup/temp, legacy behavior and BaseException assertions verified; same initializer lock and validation-before-backup order retained. All findings addressed/no new Critical or Important. No tests rerun. Main chronology already witnessed; no unresolved cannot-verify item.

Task 1: fix round 1/5 (1 reviewed finding plus same-mechanism wrapper addressed, 0 open; commits91a4c48a..e5130edc).
Task 1: complete (commits b70afc3c..e5130edc, review clean).

Ruling26 implemented and independently reviewed. No live caller supplies or authenticates this metadata yet; registry runtime check, missing-field classification, physical scope, producer/publication/graph and repair integration remain. Retain this workspace through final whole-branch review and exhaustive rulings handoff.
