# Coverage Map — Spec 015: CA Outcomes Validation
**Agent**: SENTINEL | **Run**: squad-1775154996 | **Date**: 2026-04-02

---

## Notes on AC Count

The mission brief estimated 37 REQ-level ACs + 5 spec-level ACs = 42 total. The actual count from spec.md is:

- REQ-015-001: 5 ACs (AC-001-001 through AC-001-005)
- REQ-015-002: 5 ACs (AC-002-001 through AC-002-005)
- REQ-015-003: 5 ACs (AC-003-001 through AC-003-005)
- REQ-015-004: 5 ACs (AC-004-001 through AC-004-005)
- REQ-015-005: 5 ACs (AC-005-001 through AC-005-005)
- REQ-015-006: 7 ACs (AC-006-001 through AC-006-007)
- REQ-015-007: 6 ACs (AC-007-001 through AC-007-006)
- REQ-015-008: 7 ACs (AC-008-001 through AC-008-007)
- Spec-level: 5 ACs (AC-SPEC-001 through AC-SPEC-005)

**Total: 50 ACs.** All 50 are mapped below. All are testable. No spec defects identified requiring CARTOGRAPHER escalation.

---

## Coverage Table

| AC | REQ | Testable? | Test Method |
|----|-----|-----------|-------------|
| AC-001-001 | REQ-015-001 | YES | Count rows in proof-status-table.md — must be exactly 17. Verify rows 5 and 13 are present (the two SPECULATION rows). FAIL if count ≠ 17 or if any row is identified as "combined" or omitted. |
| AC-001-002 | REQ-015-001 | YES | Inspect each of the 17 rows for presence of all 6 required fields: (1) claim identifier, (2) primary evidence source (DOI, arxiv ID, or artifact path), (3) evidence grade (A/B/C/D), (4) proof category (P1-P5), (5) proof status label, (6) "What Would Constitute Full Proof" cell. FAIL if any field is empty for any row, except that the "What Would Constitute Full Proof" cell must be non-empty specifically for every non-P1 row. |
| AC-001-003 | REQ-015-001 | YES | Search proof-status-table.md rows 5 and 13 for the exact string "SPECULATION: no empirical grounding". Verify neither row contains "probable," "supported," or "likely" as a softening qualifier. FAIL if the exact phrase is absent from either row, or if softening language is present. |
| AC-001-004 | REQ-015-001 | YES | Inspect rows 6, 7, 8, 9, and 10 (the five CA overlay rows). Verify each row's Proof Status cell contains "GATE-CONDITIONED on U-CA-004". Verify each row's blocking reference cell cites U-015-001. FAIL if any of the five overlay rows omits either element. |
| AC-001-005 | REQ-015-001 | YES | Inspect rows 1 and 2. Verify row 1 (NS-003-A) contains "PROVEN (component level" and cites arxiv:2510.09355. Verify row 2 (NS-003-B) contains "PROVEN (component level" and cites arxiv:2603.17244. Verify both rows also carry the PARTIAL qualifier for Echelon-specific deployment. Attempt retrieval of both arxiv IDs to confirm they resolve to real preprints. FAIL if any element is absent or if either arxiv ID does not resolve. |
| AC-002-001 | REQ-015-002 | YES | Locate the search record artifact. Verify it states execution on both Semantic Scholar and Google Scholar. Verify the exact query string `("Generator-Critic" OR "generation-validation loop") AND ("belief revision" OR "AGM postulates") AND ("multi-agent" OR "artifact store")` appears verbatim in the record — not paraphrased. FAIL if either database is absent or if the query string differs from the specified conjunction. |
| AC-002-002 | REQ-015-002 | YES | Inspect the search record for four required fields: (1) date of execution to the day, (2) query string as run verbatim, (3) result count per database, (4) disposition of each result (not matching / matching / partial match). FAIL if any of the four fields is absent or incomplete. |
| AC-002-003 | REQ-015-002 | YES | If result count is zero for the conjunction query: verify the novelty claim is phrased as "no prior literature found in the reviewed corpus as of [date]" — not as "no prior literature exists." Verify the phrase "no prior literature exists" (or equivalent universal negation) is absent. FAIL if zero-result phrasing uses a universal negation without the boundary qualifier. |
| AC-002-004 | REQ-015-002 | YES | If any result matches the combination (Generator-Critic + AGM belief revision + multi-agent context): verify the artifact escalates rather than satisfying the AC. Verify a differential analysis is present distinguishing NS-003 from the prior work. FAIL if a matching result is present but the artifact claims novelty without escalation. Note: if zero results were returned this AC is vacuously satisfied — state this explicitly in the test record. |
| AC-002-005 | REQ-015-002 | YES | Verify the search record is stored as a standalone artifact file (not embedded inline in proof-status-table.md or another artifact). FAIL if the search record is only accessible as an embedded subsection of another document rather than as a standalone file with its own path. |
| AC-003-001 | REQ-015-003 | YES | Inspect the baseline dataset artifact. Verify every logged record contains all five required fields: (a) prompt token count per invocation, (b) completion token count per invocation, (c) agent identifier, (d) spec run ID, (e) codebase identifier. FAIL if any record omits any of the five fields, or if the dataset contains records with partial fields that are not flagged as incomplete. |
| AC-003-002 | REQ-015-003 | YES | Count the number of distinct spec runs in the baseline dataset. Must be at least 3. If the dataset was assembled from forward-looking instrumented runs (not post-hoc), verify at least 3 instrumented runs have completed. FAIL if run count < 3. |
| AC-003-003 | REQ-015-003 | YES | Verify the dataset includes per-agent-type summary statistics. For each agent type present in the data, verify: mean, median, and 90th-percentile token count (prompt + completion) are reported. FAIL if any agent type present in the dataset has missing summary statistics, or if any of the three statistics (mean, median, 90th-percentile) is absent for any agent type. |
| AC-003-004 | REQ-015-003 | YES | Verify the baseline dataset artifact is stored in a machine-readable format (structured JSON or CSV — not prose, not markdown table without a parseable header row). Verify a human-readable summary is also present alongside the machine-readable file. FAIL if only prose is provided, or if the machine-readable format is non-standard and cannot be parsed without proprietary tools. |
| AC-003-005 | REQ-015-003 | YES | Verify the artifact explicitly states whether token counts were obtained by (a) post-hoc estimation of prior runs or (b) live instrumentation of new runs. Verify the accuracy difference between the two methods is acknowledged. FAIL if the method is not stated or if the method is stated but the accuracy characteristics are not distinguished. |
| AC-004-001 | REQ-015-004 | YES | Verify the annotation artifact states: which runs were selected (by run ID or equivalent identifier), that the selection is between 3 and 5 runs inclusive, that each selected run contains at least DISCOVER and ASSESS agent outputs, and that runs are from the stated corpus (runs 008-014 per the spec). FAIL if fewer than 3 or more than 5 runs are selected, or if any selected run lacks both DISCOVER and ASSESS outputs. |
| AC-004-002 | REQ-015-004 | YES | Verify the annotation is performed per section (not per artifact). Verify three classification categories are used: IN-SCOPE, OUT-OF-SCOPE, BORDERLINE. Verify IN-SCOPE and OUT-OF-SCOPE definitions are applied at the section level (not the assertion level within a section). FAIL if annotation is done at artifact level rather than section level, or if only two classification categories are used. |
| AC-004-003 | REQ-015-004 | YES | If a second annotator was available: verify inter-annotator agreement is reported (Cohen's kappa or percentage agreement). If only one annotator: verify an explicit statement of this limitation is present in the artifact. FAIL if neither the agreement metric nor the single-annotator limitation statement is present. |
| AC-004-004 | REQ-015-004 | YES | Inspect the output artifact for four required content elements: (1) violation rate per agent type (OUT-OF-SCOPE sections / total sections per agent type), (2) overall violation rate across all annotated runs, (3) the three most frequent violation patterns by type. FAIL if any of the three required elements is absent or if the violation rate formula is not applied as stated. |
| AC-004-005 | REQ-015-004 | YES | Verify BORDERLINE sections are excluded from the violation rate numerator. Verify the count of BORDERLINE sections is reported separately. FAIL if BORDERLINE sections are included in the violation rate numerator, or if the BORDERLINE count is reported but not separated from OUT-OF-SCOPE in the rate calculation. |
| AC-005-001 | REQ-015-005 | YES | Verify the scan artifact states which runs were included. Verify coverage includes at minimum runs 008-014 (or whichever subset have both DISCOVER and ASSESS artifacts). Verify each run is treated as one observation unit. FAIL if fewer than 3 runs are covered, or if the run selection rationale is absent. |
| AC-005-002 | REQ-015-005 | YES | Verify the contradiction detection method is stated explicitly. Verify the method is applied to structured sections, not prose paragraphs. Verify the comparison method (exact string match, semantic similarity with threshold, or LLM classifier) is identified by name and its precision/recall characteristics are documented. FAIL if the detection method is described only in prose terms without naming the specific technique, or if precision/recall characteristics are absent. |
| AC-005-003 | REQ-015-005 | YES | Verify the output artifact reports all four required statistics: (1) total artifact pairs scanned, (2) total contradictions detected, (3) contradiction rate per run (contradictions / artifact pairs), (4) contradiction rate per adjacent agent pair where data is available. FAIL if any of the four elements is absent. |
| AC-005-004 | REQ-015-005 | YES | Verify a random sample of at least 5 detected contradictions is included in the artifact with manual review annotations confirming or denying each as a true positive. Verify a false positive rate estimate is derived from this sample. FAIL if the sample is fewer than 5, if the review is not included in the artifact, or if no false positive rate estimate is derived from the sample. |
| AC-005-005 | REQ-015-005 | YES | Verify the artifact states whether the detected rate represents an upper bound (method has known false positives) or a lower bound (method misses soft contradictions in prose). FAIL if neither bound characterization is present, or if the artifact asserts the rate is exact without addressing detection method limitations. |
| AC-006-001 | REQ-015-006 | YES | Verify the experiment design document names a specific test codebase (named repository, not "any suitable codebase"). Verify a rationale for the selection is provided. For the recommended codebase (Echelon extension), verify the path or public equivalent is stated. FAIL if the codebase is not named, or if no rationale is provided. |
| AC-006-002 | REQ-015-006 | YES | Verify the evaluation set size for NS-003-A is stated as N ≥ 30 agent invocations. Verify the rationale for this minimum is provided (the document states statistical power or equivalent reasoning). FAIL if N < 30 or if no rationale is given for the minimum. |
| AC-006-003 | REQ-015-006 | YES | Verify the evaluation set for NS-003-B specifies N ≥ 20 artificially contradicted artifact pairs. Verify the contradiction injection method is named (rule-based, LLM adversarial, or manual) and the protocol is stated (how contradictions are introduced step by step). FAIL if N < 20, if the injection method is unnamed, or if the injection protocol is described only in abstract terms without step-by-step procedure. |
| AC-006-004 | REQ-015-006 | YES | Verify the NS-003-A success metric formula appears verbatim: first-pass compliance rate = (invocations where Critic accepts on attempt 1) / (total invocations). Verify the acceptance threshold is ≥ 0.70 (or 0.80 per the amended spec — confirm against final spec.md version). Verify the three zones (≥ threshold = PASS, 0.50 to threshold = INCONCLUSIVE/redesign, < 0.50 = FAIL/redesign) are all defined with actions. FAIL if the formula is absent, threshold is not stated as a number, or any zone's action is missing. |
| AC-006-005 | REQ-015-006 | YES | Verify the NS-003-B success metric formula appears verbatim: contradiction catch rate = (injected contradictions correctly flagged as ConflictSignal) / (total injected contradictions). Verify the acceptance threshold is ≥ 0.80. Verify the false positive rate formula is present: FPR = (ConflictSignal on non-contradicted pairs) / (total non-contradicted pairs), with threshold ≤ 0.20. Verify "correctly flagged" is defined (field-specific ConflictSignal before commit). FAIL if any formula, threshold, or definition is absent. |
| AC-006-006 | REQ-015-006 | YES | Verify the document includes implementation phases covering: (1) schema formalization, (2) prototype implementation of Generator-Critic loop and belief graph, (3) measurement run. Verify phases are stated without calendar days (technology-agnostic). FAIL if any of the three phases is absent, or if the timeline includes calendar day estimates rather than phase descriptions. |
| AC-006-007 | REQ-015-006 | YES | Apply the TPE five-item checklist: (1) metric formula stated in full — verify both FPCR and CCR/FPR formulas are complete; (2) acceptance threshold is a specific number — verify ≥ 0.80 for FPCR and CCR; (3) evaluation set construction method stated — verify both N=30 invocation distribution and N=20 contradiction injection protocol; (4) test codebase selection stated — verify named codebase with rationale; (5) decision rule stated — verify PASS/PARTIAL/FAIL criteria with actions. FAIL if any of the five items cannot be confirmed from the document text alone. |
| AC-007-001 | REQ-015-007 | YES | Verify the calibration artifact states the number of adjacent artifact pairs examined (N). Verify N ≥ 9 or, if N < 9, an explicit statement of the small-sample limitation is present. Verify the pairs are DISCOVER→ASSESS (or other adjacent agent stages). FAIL if N is not stated, if N < 9 without a limitation statement, or if the stage pairing is not DISCOVER→ASSESS or equivalent adjacent pair. |
| AC-007-002 | REQ-015-007 | YES | Verify each artifact pair is evaluated against the stated question and scored 0-100% per pair. Verify the three scoring anchors (0-20%, 40-60%, 80-100%) are applied consistently. Verify the definition of "predictable" (explicitly stated OR direct logical consequence of upstream finding) is applied. Verify the borderline 50% rule is applied. Verify the evaluation method (human, LLM-as-evaluator, or structured extraction) is named and applied consistently. FAIL if any element of the scoring rubric is absent from the artifact or if scores are provided without the rubric being stated. |
| AC-007-003 | REQ-015-007 | YES | Verify the calibration output reports all five aggregate statistics: mean, median, minimum, maximum, and standard deviation of prediction accuracy across all pairs. FAIL if any of the five statistics is absent. |
| AC-007-004 | REQ-015-007 | YES | Verify the break-even formula is present: break-even accuracy = (token cost of one forward model prediction call) / (token cost of one full downstream agent invocation). If REQ-015-003 is complete, verify the formula is instantiated with measured token counts from the baseline dataset. If REQ-015-003 is not yet complete, verify the formula is in symbolic form and marked as pending. FAIL if the formula is absent entirely, or if numerical values are substituted without citing REQ-015-003 baseline data. |
| AC-007-005 | REQ-015-007 | YES | Verify the calibration artifact carries the label "SPECULATION" on the 40-70% token reduction claim. Verify the label is not softened to "probable," "likely," or "supported" based on the calibration result. Verify the upgrade condition (N=50+ prototype runs) is stated. FAIL if the SPECULATION label is absent, softened, or if the label is removed based on the retrospective calibration result alone. |
| AC-007-006 | REQ-015-007 | YES | Verify the calibration artifact includes a go/no-go recommendation using the three-outcome scheme: GO (mean ≥ break-even AND SD < 30%), NO-GO (mean < break-even), INCONCLUSIVE (within 10pp of break-even OR SD ≥ 30%). Verify the recommendation maps to one of the three outcomes. FAIL if the three-outcome scheme is not applied, if the recommendation is stated without a mapping to the scheme, or if the SD condition is omitted. |
| AC-008-001 | REQ-015-008 | YES | Verify exactly three conditions are defined: (A) Naive Baseline with minimal prompting description, (B) Expert-Prompt Baseline citing current production prompts, (C) CA-Structured Overlay naming the first overlay to test. Verify all three conditions specify the same LLM version. Verify the rationale for the first overlay selection is present. FAIL if fewer than three conditions are defined, if the LLM version is not specified as identical across all three, or if the first overlay rationale is absent. |
| AC-008-002 | REQ-015-008 | YES | Verify the LLM version lock requirement is stated — the exact API model string must be recorded at experiment execution time. Verify the version lock rule is stated: no batch spans a model version update; the batch restart rule is present (pre-update runs discarded if update occurs mid-batch). FAIL if version lock is mentioned only in abstract terms without the restart rule, or if the requirement applies only to some conditions rather than all three. |
| AC-008-003 | REQ-015-008 | YES | Verify N ≥ 10 per condition is stated as the minimum. Verify N=20 per condition is the 80% power target. Verify the power rationale is present (effect size 0.5 SD, Mann-Whitney U or equivalent, α=0.05). Verify the staged execution protocol is present (start N=10, double to N=20 on INCONCLUSIVE). FAIL if any of the four elements is absent. |
| AC-008-004 | REQ-015-008 | YES | Verify the task selection criterion is stated: one fixed codebase for all conditions. Verify the rationale for single codebase over stratified multi-codebase is present. Verify the test codebase is named (or specified by selection rules). Verify the generalizability limitation is acknowledged. FAIL if the codebase selection criterion is absent, if no rationale is given for single vs multi-codebase, or if the generalizability limitation is not acknowledged. |
| AC-008-005 | REQ-015-008 | YES | Verify at least two primary metrics are defined with formulas. For AQS: verify the formula is present (sum of four dimension scores / 12), verify all four dimensions are named, verify 0-3 scoring anchors are present for each dimension. For SVR: verify the formula is present (OUT-OF-SCOPE sections / total sections), verify the section definition is stated, verify IN-SCOPE/OUT-OF-SCOPE/BORDERLINE classification rules are stated. FAIL if fewer than two metrics have formulas, if any dimension anchor is absent, or if any classification rule is absent. |
| AC-008-006 | REQ-015-008 | YES | Verify a pre-registered decision rule is present with three named outcomes: POSITIVE, NEGATIVE, INCONCLUSIVE. Verify POSITIVE requires all three sub-criteria (≥10pp AQS difference, ≥15% relative SVR reduction, p < 0.05 by stated statistical test). Verify each outcome maps to a specific action. Verify the statistical test is named (Mann-Whitney U or equivalent). FAIL if the three-outcome rule is absent, if any sub-criterion for POSITIVE is missing, or if any outcome lacks an action mapping. |
| AC-008-007 | REQ-015-008 | YES | Verify the specification states that one overlay is tested at a time. Verify the first overlay is named with rationale. Verify the testing order for subsequent overlays (contingent on POSITIVE first result) is stated for all five overlays. Verify an early termination rule is present (NEGATIVE on any overlay stops the program). FAIL if any of the four elements is absent. |
| AC-SPEC-001 | Spec-level | YES | Scan all artifacts produced under spec 015 for verdict language containing "believed," "expected," or "likely" without a following citation. Verify each unique arxiv ID and DOI cited across all artifacts resolves to a retrievable paper whose content is consistent with the cited claim and cited metric. FAIL if any verdict uses uncited belief language, or if any cited identifier does not resolve to a retrievable paper. |
| AC-SPEC-002 | Spec-level | YES | Scan all artifacts for any of the five CA overlay names (Goal Stack, ACT-R Typed Buffer, LIDA Broadcast, GWT Bounded Workspace, Episodic Memory). For each occurrence, verify the overlay is described as GATE-CONDITIONED on U-CA-004. Verify no occurrence describes the overlay as "proven," "supported," "validated," or "ready for implementation." FAIL if any overlay occurrence lacks the gate-conditioned qualifier, or if any overlay is stated as ready for implementation. |
| AC-SPEC-003 | Spec-level | YES | Locate the proof-status-table.md rows for NOVEL-004 40-70% token reduction (row 5) and the corresponding use case row (row 13). Locate the NOVEL-004 discussion in the REQ-015-007 calibration artifact. Verify "SPECULATION: no empirical grounding" appears in all three locations. Verify the upgrade condition (N=50+ prototype runs) is stated in at least one location. FAIL if the label is absent from any of the three required locations, or if the label has been softened. |
| AC-SPEC-004 | Spec-level | YES | Apply the REP checklist to the novelty search record artifact: (1) exact query string present verbatim, (2) both databases named with access method, (3) date of execution stated to the day, (4) result count per database stated, (5) a third party could re-execute the identical search from the record without asking clarifying questions. FAIL if any of the five reproducibility elements is absent. |
| AC-SPEC-005 | Spec-level | YES | Apply the TPE five-item checklist to REQ-015-006 (ns003-experiment-design.md) and REQ-015-008 (u-ca-004-experiment-spec.md): (1) metric formula stated in full, (2) acceptance threshold is a specific number, (3) evaluation set construction method stated, (4) test codebase named or fully specified, (5) decision rule pre-registered with thresholds for each outcome. A third party with access to spec 014 and spec 015 must be able to answer all five questions from the document text alone. FAIL on either document if any of the five items requires external clarification. |

---

## Coverage Summary

| Total ACs | Testable | Not Testable | Spec Defects Flagged |
|-----------|----------|--------------|----------------------|
| 50 | 50 | 0 | 0 |

All 50 ACs are testable using the procedures defined above. No AC requires running the NS-003 or U-CA-004 experiments to evaluate. No AC is ambiguous to the point where a binary PASS/FAIL verdict cannot be determined from the artifact text. No spec defects are flagged for CARTOGRAPHER.

The coverage is complete. The test strategy, test architecture, and this coverage map together define the full SENTINEL verification layer for spec 015.
