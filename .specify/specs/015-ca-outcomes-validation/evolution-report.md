# Evolution Report — Spec 015
**Agent**: FINALIZE | **Run**: squad-1775154996 | **Date**: 2026-04-02
**From**: Spec 014 (cognitive-architecture-llm-framing) claimed outcomes
**To**: Spec 015 (ca-outcomes-validation) proof status verdicts

---

## How Spec 015 Evolved Spec 014's Claims

| Claim | Spec 014 Status | Spec 015 Status | Change |
|-------|----------------|-----------------|--------|
| NS-003-A: Generator-Critic (86%+ compliance) | "86%+ compliance (Grade A)" — cited NL2GenSym but not differentiated from Echelon-specific validity | PROVEN (component level, NL2GenSym, arxiv:2510.09355) / PARTIAL (Echelon-specific, requires NS-003 prototype experiment) | Stratified into two distinct verdicts. Component-level proof is confirmed. Echelon-specific proof is explicitly NOT YET ESTABLISHED and requires the prototype experiment specified in ns003-experiment-design.md. The distinction prevents a third party from reading "86% proven" as "Echelon is proven." |
| NS-003-B: AGM Belief Revision (93.3% accuracy) | "93.3% (Grade A)" — cited Kumiho but same stratification gap as NS-003-A | PROVEN (component level, Kumiho, arxiv:2603.17244) / PARTIAL (Echelon-specific) | Same stratification applied. WHY2 also resolved the threshold conflict: AC-006-005 now uses ≥0.80 (matching Proof Topology Table) rather than the ≥0.75 that appeared in the WHY1 draft. The 0.80 threshold is now consistent across all documents. |
| NS-003-C: Novelty of combination | "no prior literature (13 sources reviewed)" — not a formal systematic search; no reproducible query string | NOVELTY CONFIRMED as of 2026-04-02 — systematic 8-query search with reproducible search record (U-015-002), verbatim query strings, disposition of each result, limitations acknowledged | Strengthened from a literature review coverage claim to a reproducible systematic search. BugGen (arxiv:2506.10501) identified as the closest structural analogue — a finding spec 014 did not surface. The novelty claim is now defensible by a third party who can re-execute the search. |
| NOVEL-004 (mechanism): Predictive Coding inter-agent protocol | Described as novel with Speculative Decoding analogy (SRC-A5) and Rao & Ballard theoretical grounding | P3 — NOT PROVEN for agent-level prediction. Speculative Decoding analogy is structural, not identical. No prototype run exists. | Correctly labeled as requiring a prototype. The analogy is preserved as motivating evidence but is not treated as proof. Calibration approach (REQ-015-007) specified. |
| NOVEL-004 (40-70% token reduction) | "speculation: needs measurement" — labeled in spec 014 original answer but without formal permanence criteria | SPECULATION: no empirical grounding — label formalized with permanence rule: N=50+ prototype measurement runs required to upgrade; retrospective calibration cannot remove the label | Moved from an informal disclaimer to a formal, permanent label with an explicit upgrade criterion. The permanence rule (AC-SPEC-003) prevents future incremental evidence from being used to soften the label without meeting the N=50 threshold. |
| CA Overlay — Goal Stack | "conditioned on U-CA-004 INCONCLUSIVE" — acknowledged the gate but no experiment specification | GATE-CONDITIONED on U-CA-004 — pre-registered experiment specification produced (u-ca-004-experiment-spec.md): three conditions, N≥10 per condition, AQS evaluation rubric, pre-registered decision rule with POSITIVE/NEGATIVE/INCONCLUSIVE outcomes mapped to actions | Moved from acknowledged gate to actionable gate spec. The squad can now make a resource allocation decision with full experiment specification in hand. The INCONCLUSIVE label in spec 014 was a status description; the gate spec in spec 015 is a precondition for action. |
| CA Overlay — ACT-R Typed Buffer | "conditioned on U-CA-004 INCONCLUSIVE" + "Lost in the Middle" as Grade A support for context ordering problem | P4 GATE-CONDITIONED — evidence correctly stratified: "Lost in the Middle" (SRC-A4) is Grade A for the attention non-uniformity problem, but Grade C for the typed buffer as solution. The problem is Grade A proven; the solution is not. | Evidence stratification added. Spec 014 correctly cited the paper but did not distinguish between "the problem this addresses is proven" and "this mechanism solves it." The distinction is now explicit in the proof status table. |
| CA Overlay — LIDA Broadcast | "conditioned on U-CA-004 INCONCLUSIVE" | P4 GATE-CONDITIONED, Grade C evidence (Franklin et al. 2014 theoretical). Requires labeled evaluation of prior runs for missed critical findings baseline before any reduction can be measured. | Unchanged in proof category; now explicitly requires REQ-015-004 baseline measurement as a precondition for any future measurement of the overlay's effect. |
| CA Overlay — GWT Bounded Workspace | "conditioned on U-CA-004 INCONCLUSIVE" | P4 GATE-CONDITIONED, Grade C evidence (Baars 1988 theoretical). Requires scope violation rate baseline from REQ-015-004. | Same pattern as LIDA Broadcast. Baseline dependency made explicit. |
| CA Overlay — Episodic Memory | "conditioned on U-CA-004 INCONCLUSIVE" | P4 GATE-CONDITIONED, Grade B evidence (MemGPT). Requires content-addressing scheme definition and prior run corpus indexing — preconditions that are not yet designed. | The MemGPT reference is retained as Grade B evidence. Two additional preconditions surfaced that were not explicit in spec 014: the content-addressing scheme must be defined before retrieval precision can be measured. |
| AC-3 Constraint Propagation | "proven for CSP domain" — correct but did not distinguish domain-specific from LLM-context analog | P2 — PROVEN for CSP domain / NOT PROVEN for LLM semantic constraint injection | Correctly split into two statements. The CSP proof (Mackworth 1977, Bessiere 2006) is real. The application to LLM semantic constraint injection is a design-level claim requiring a prototype. Both parts are now explicit in the proof status table row. |
| Use case — ASSESS contradicts DISCOVER caught at write-time | "covered by NS-003 design" — directional claim | P2 — SUPPORTED BY DESIGN. Integration test specified: inject known contradictory artifact pair (20 pairs), measure ConflictSignal fires before commit for ≥80% of pairs. | Upgraded from a design-level assertion to a design-level claim with a concrete binary integration test criterion. |
| Use case — 40-70% token reduction for repeated codebases | "speculation" | P5 SPECULATION: no empirical grounding — same permanence rule as NOVEL-004 token reduction (N=50+ to upgrade) | Formalized with the same permanence criteria. The two P5 rows are now explicitly linked — upgrading either requires the same prototype measurement standard. |
| Use case — WHY rejects spec 3x → COMMANDER knows in advance | Asserted as a Goal Stack use case | P4 GATE-CONDITIONED — requires U-CA-004 POSITIVE + prototype demonstrating COMMANDER reads goal stack and modifies dispatch before third rejection, on N=10+ labeled sequences | Added a concrete evaluation criterion (labeled rejection event sequences, N=10+) that was not present in spec 014's use case description. |
| Use case — ACT-R buffer delivers only relevant context | Asserted as an ACT-R overlay use case | P4 GATE-CONDITIONED + PARTIALLY SUPPORTED — the problem (LLM attention non-uniformity) is Grade A proven; the typed buffer as solution is Grade C. Requires context ordering experiment with AQS comparison (N=20 pairs). | Same problem/solution stratification as the CA Overlay row for ACT-R. Evidence distinction now propagated to the use case row. |

---

## Net Changes Summary

**Strengthened (higher confidence warranted by spec 015 evidence)**
- NS-003-C novelty claim: moved from 13-source literature review to 8-query systematic search with reproducible record and identified closest analogue (BugGen).
- NOVEL-004 and token reduction use case SPECULATION labels: formalized with explicit permanence criteria.
- All 5 CA overlays: moved from INCONCLUSIVE acknowledgment to pre-registered gate experiment specification.

**Unchanged in proof category but more precisely scoped**
- NS-003-A and NS-003-B: proof category unchanged (P1/PARTIAL) but the stratification between component-level and Echelon-specific proof is now explicit in every row.
- AC-3: proof category unchanged (P2) but correctly split across two domains.
- NS-003-B threshold: corrected from 0.75 (WHY1 draft) to 0.80 (WHY2 final) to match mental-model.md Proof Topology Table.

**No claim was downgraded**
No spec 014 claim was found to be false or contradicted by the spec 015 evidence review. The evolution is stratification and formalization, not contradiction.
