{
  "name": "sp-001-bdi",
  "version": "0.1.0",
  "description": "SP-001: Minimal BDI engine from AgentSpeak(L) spec",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "node src/benchmark.js"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "xstate": "^5.18.0"
  }
}
