# Managed Phase 2: feasibility, strategy and intent alignment

## Approval and boundary

The user approved the inline Phase 2 proposal after completion of the managed
Lexicon/Phase 1 checkpoint milestone at `2194f08f`. Continue in the existing
isolated worktree through GATEKEEPER feasibility, STRATEGIST overview and TRACKER
alignment, including their native structural gates. Stop before executing
`phase3-specialists`. Preserve native PASS/KILL/DEFER routing, repair limits,
timing, guided/semi behavior and Banzai decisions through existing COMMANDER.
No installation, migration, live model spending, push, merge, legacy build,
AGENTS.md or CLAUDE.md changes. Implementation stays inline; review is read-only.

## Existing owners

Extend managed assignments, retained rounds and provider receipts rather than
creating an orchestration subsystem. Prosaic owns neutral role loading and
provider interpretation for Codex and Claude. The existing identity authority
owns IDs/history; Phase 2 derived documents have no requirement creation or
revision authority. Native Squad owns routing, human decisions, timing and
completion. Existing guarded publication owns all canonical writes, including
structural reports and graph outputs. Do not bypass managed exclusion guards.

This is required downstream convergence under DEFER-000004/000005, not general
publication rollout, lifecycle authoring or input-domain expansion.

## Phase 2 document contracts

Feasibility consumes the exact released Phase 1 approval and current source,
quality/debt and Lexicon evidence. It authors `feasibility.md`,
`prioritization.md`, `estimates.md`, `mvp-scope.md`, and the native conditional
`kill-report.md`. It returns PASS/KILL/DEFER as evidence for native routing, not
structural certification or mutable controller fields. DEFER proposes scope
adjustments in these outputs; only the existing WHAT repair route may revise
canonical requirements using existing identity/revision controls.

STRATEGIST consumes the actual released PASS feasibility-gate successor and
authors only `strategic-overview.md`. TRACKER consumes that actual predecessor,
the original accepted intent and feasibility scope, and authors only
`intent-alignment-check.md`. ALIGNED/DRIFT retain native policy. STOP_AND_ASK
uses the native sealed clarification decision and existing COMMANDER in eligible
Banzai mode; the model cannot write the answer or confer approval on itself.

References in all documents remain bound to accepted identities. New unknown or
issue definitions are not implicitly admitted as a side effect of prose.
Neutral producer/reviewer contracts use the existing managed semantic protocol;
they do not become new policy owners or a new COMMANDER role.

## Captured structural evaluation

The existing `governance_structural_gate` remains the policy owner for both
feasibility and alignment. Separate its deterministic evaluation from live input
reading/report writing. Captured artifact, cross-reference and template text are
evaluated without live input reads or report writes; bundled validator resources
(the Lexicon grammar) may load. Exact report bytes go through guarded publication.
Reuse structural checks, attempt normalization, repair/iteration caps and warn
versus block exhaustion behavior. Existing native callers retain their current
file-based interface and evidence-write-failure behavior.

Missing captures do not authorize rereading a live replacement. An absent required
template blocks the captured route rather than silently dropping section checks.
Disabled/non-structural configuration retains native bypass behavior. Unsupported
input/path selections fail closed until their exact capture is admitted.

## Retention, branches and recovery

Bind every round to its actual released predecessor, configuration, complete
read set, role and provider settings. Preserve prior gate findings, counters and
attempts through repairs. Unknown provider completion requires reconciliation;
known retained responses replay without another call or charge. Publish derived
documents, reports and graph changes together through existing completion proofs.

PASS continues through strategy/alignment. KILL retains its native terminal
outcome and kill report. DEFER returns through the existing WHAT path, invalidates
stale quality/Lexicon authority under native rules, and uses the existing defer
limit and escalation. Do not create a second repair budget. Preserve the native
Phase 2 timing window across dispatch/restart; agents never control it.

## Acceptance

Use scripted Codex/Claude providers and real retained Phase 1 approval ancestry.
Cover PASS/KILL/DEFER, structural repair/no-progress/exhaustion, disabled gates,
alignment clarification for guided/semi/Banzai, current debt preservation and
stale evidence refusal. Verify exact restart before/after publication and native
state application, no duplicate calls/charges, unchanged requirement identities,
and no Phase 3 execution. Distinguish fresh corridors, retained continuations,
targeted regressions and live acceptance. Performance optimization remains deferred.
