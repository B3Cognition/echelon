# Echelon Grounded Review Register

**Last full review snapshot:** `docs/findings/2026-06-28-echelon-grounded-source-review-refresh.md`
**Last full review HEAD:** `b0aa12a847efca83793c6b7ed7f489e0e8254954`
**Last delta review snapshot:** `docs/findings/2026-06-24-egr-delta-review-after-egr-011.md`
**Last delta review HEAD:** `07279388913057066329ccdf2285d1fa821e7987`
**Last updated:** 2026-07-04

## Operating Model

This register is the living tracking surface for grounded review findings. Keep
dated snapshots immutable enough to preserve context, and update this file
whenever repository contributions change the evidence, priority, owner, or
status of a finding.

### Status Values

- `open`: confirmed and not yet addressed.
- `in-progress`: implementation or design work has started.
- `fixed`: addressed in source and verified.
- `superseded`: replaced by a newer finding or architecture decision.
- `accepted-risk`: intentionally not fixed for now, with rationale.

### Delta Review Protocol

When the repo changes:

1. Compare the new head against `Last full review HEAD`.
2. Review only changed files first.
3. Re-open the full source review if workflow, harness, state, sandbox, memory,
   or CLI boundaries changed substantially.
4. For each affected finding, update `Evidence`, `Status`, `Next action`, and
   `Review notes`.
5. Confirm any implemented EGR has a corresponding `CHANGELOG.md` entry under
   `[Unreleased]`.
6. Advance `Last delta review HEAD` after the delta review is complete.

Suggested command:

```bash
git diff b0aa12a847efca83793c6b7ed7f489e0e8254954..HEAD -- src extension docs tests
```

### EGR Completion Gate

An EGR implementation is not complete until the same change set includes:

- Source and test changes for the finding.
- A `CHANGELOG.md` `[Unreleased]` entry that names the EGR ID and explains the
  user-visible or operator-visible impact.
- This register updated with the finding status, evidence, and review note.
- Verification commands and outcomes captured in the implementation thread or PR
  description.

This is deliberately part of the EGR contract rather than an optional release
cleanup step: EGR work usually changes safety, harness behavior, or operating
assumptions, so downstream operators need the change surfaced in the changelog at
the same time as the code lands.

## Current Findings

| ID | Priority | Status | Finding | Evidence | Next action |
|---|---|---|---|---|---|
| EGR-001 | P0 | fixed | Missing deterministic `echelon_result` schema validation before state updates. | `src/harness/echelon_result_schema.py`, `src/harness/squad_provider.py`, `src/harness/squad_state.py`, `tests/kernel/test_echelon_result_schema.py`, `tests/kernel/test_squad_provider.py`, `tests/kernel/test_squad_state.py` | Fixed: parsed agent results are validated before state mutation; invalid results block with a clear reason. |
| EGR-002 | P1 | fixed | Phase A readiness and quality gates were partly deterministic and partly LLM-routed. | `src/harness/phase_a_readiness.py`, `src/harness/squad.py`, `src/echelon/cli.py`, `tests/unit/test_phase_a_readiness.py`, `tests/unit/test_cli_next_step_escalation.py`, `tests/unit/test_cli_continue.py`, `tests/integration/test_squad_controller.py` | Fixed: shared Phase A readiness validation blocks incomplete or blocked/interrupted Phase A states before build-ready guidance or finalization. |
| EGR-003 | P1 | fixed | Host-side LLM tool boundaries were mostly prompt-governed. | `src/harness/llm_tool_policy.py`, `src/harness/config.py`, `src/harness/llm_provider.py`, `src/harness/review_loop.py`, `src/echelon/cli.py`, `extension/config-template.yml`, `tests/unit/test_llm_tool_policy.py`, `tests/unit/test_cli_llm_tool_policy.py`, `tests/unit/test_llm_provider.py`, `tests/unit/test_review_loop.py`, `tests/unit/test_config.py` | Fixed: unsafe host CLI permission-bypass flags fail closed unless explicitly configured with approval metadata. |
| EGR-004 | P1 | fixed | Sandboxing existed, but sandbox recommendation needed to be explicit. | `src/harness/sandbox_suggestion.py`, `src/harness/init.py`, `src/echelon/cli.py`, `src/harness/verify_detection.py`, `src/harness/app_runtime_detection.py`, `tests/unit/test_sandbox_suggestion.py`, `tests/unit/test_cli_harness_init_summary.py` | Fixed: `echelon harness init` writes and summarizes an evidence-backed sandbox suggestion report. |
| EGR-005 | P1 | fixed | Human-in-the-loop blocking was real but decision capture needed structure. | `src/harness/blocked_decision.py`, `src/harness/squad_state.py`, `src/harness/escalation.py`, `src/echelon/cli.py`, `tests/unit/test_blocked_decision.py`, `tests/unit/test_escalation.py`, `tests/unit/test_cli_resume_escalation_options.py`, `tests/kernel/test_squad_state.py` | Fixed: blocked squad runs persist typed decisions and resume metadata. |
| EGR-006 | P2 | fixed | Review loops existed, but generic draft/critique/repair/re-check was not reusable. | `src/harness/repair_loop.py`, `tests/unit/test_repair_loop.py`, `src/harness/ralph.py`, `src/harness/review_loop.py`, `src/harness/squad.py` | Fixed as a primitive: `RepairLoop` exists; follow-on EGR-019 tracks adoption in existing loops. |
| EGR-007 | P2 | fixed | Internalization was split between real codegen memory and prompt-level learning. | `src/codegen/memory/kb_schema_validator.py`, `tests/unit/test_kb_schema_validator.py`, `knowledge-base/kb-schema.md`, `src/codegen/memory/*`, `extension/agents/learning/*` | Fixed: deterministic KB validation exists for durable pattern/pitfall learning writes. |
| EGR-008 | P2 | fixed | Role surface area was high relative to machine-checkable contracts. | `src/harness/role_contracts.py`, `src/harness/phase_graph.py`, `extension/agents/**/*.md`, `extension/workflow/definition.yaml`, `tests/unit/test_role_contracts.py`, `tests/kernel/test_phase_graph.py` | Fixed: routed roles have contract validation for required `echelon_result` fields, outputs, and allowlists. |
| EGR-009 | P3 | accepted-risk | RCA pipeline is not implemented as a first-class capability in this source tree. | No dedicated incident/RCA pipeline found under `src/` or `extension/workflow/`; user confirmed a separate RCA pipeline already exists and should be integrated later from its actual sources. | Parked pending grounded integration of the separate RCA pipeline; do not implement from the original review alone. |
| EGR-010 | P1 | fixed | GitOps lacked a deterministic pre-push/pre-commit secret scan gate. | `src/harness/secret_scan.py`, `src/harness/gitops.py`, `tests/unit/test_secret_scan.py`, `tests/integration/test_gitops_safety.py`, `tests/integration/test_gitops_commit_push.py` | Fixed: `GitOpsManager.commit()` scans staged files before committing and blocks high-confidence findings. |
| EGR-011 | P2 | fixed | Per-phase `state_updates` allowlists were not enforced. | `extension/workflow/definition.yaml`, `src/harness/echelon_result_schema.py`, `src/harness/phase_graph.py`, `src/harness/role_contracts.py`, `src/harness/squad.py`, `src/harness/squad_executors.py`, `src/harness/squad_state.py`, tests under `tests/kernel/` and `tests/unit/` | Fixed: phases declare `allowed_state_updates`, static validation requires them, and runtime writes enforce them. |
| EGR-012 | P1 | fixed | Pre-dispatch agent `state_updates` bypassed per-phase allowlists. | `src/harness/squad_executors.py::_run_pre_dispatch()`, `tests/kernel/test_squad_executors_journal.py` | Fixed: pre-dispatch agents validate through the parent phase allowlist before state mutation. |
| EGR-013 | P1 | fixed | COMMANDER judgment state updates bypassed allowlists. | `src/harness/squad.py::JUDGMENT_STATE_UPDATE_KEYS`, `src/harness/squad.py::_apply_judgment_state_updates()`, `tests/integration/test_squad_controller.py::TestCommanderJudgmentStateUpdates` | Fixed: COMMANDER judgment writes use a narrow deterministic allowlist. |
| EGR-014 | P2 | fixed | Allowed `state_updates` were enforced but not fully injected into agent prompts. | `src/harness/squad_executors.py::_allowed_state_updates_contract()`, prompt assembly tests in `tests/kernel/test_squad_executors_journal.py` | Fixed: normal, pre-dispatch, staged, and conditional prompts disclose allowed state-update keys. |
| EGR-015 | P1 | fixed | Normal agent dispatch wrote journal entries before allowlist validation could block invalid state updates. | `src/harness/squad_executors.py::AgentExecutor.execute()`, `src/harness/echelon_result_schema.py`, `extension/workflow/definition.yaml`, `tests/kernel/test_squad_executors_journal.py` | Fixed: normal agent dispatch validates before journal, cost, recovery, or state handling. |
| EGR-016 | P1 | fixed | Whole-workflow contract validation was incomplete. | `src/harness/workflow_validator.py`, `scripts/bash/dry-run.sh`, `tests/kernel/test_workflow_validator.py`, `tests/kernel/test_phase_graph.py` | Fixed: executable workflow transitions now have deterministic validation for shape, supported keys, target phases, condition syntax, actions, and `state_update` blocks; dry-run runs the validator when the Python harness source is available. |
| EGR-017 | P1 | fixed | Safety documentation drift: README still described unconditional Claude permission bypass. | `README.md`, `src/harness/llm_tool_policy.py`, `extension/config-template.yml`, `tests/unit/test_readme_tool_policy_docs.py` | Fixed: README terminal CLI docs now describe the fail-closed host tool-policy contract and the explicit `allow_unsafe_host_execution` plus `approval_reason` opt-in for unsafe provider bypass flags. |
| EGR-018 | P1 | fixed | Python journal writers did not enforce the canonical journal-entry schema. | `src/harness/journal_entry_validator.py`, `src/harness/squad.py::_write_journal_entries()`, `src/harness/squad_executors.py::_write_journal_entries()`, `extension/workflow/journal-entry-types.yaml`, `tests/unit/test_journal_entry_validator.py`, `tests/kernel/test_squad_executors_journal.py` | Fixed: Python journal writers now validate against the canonical type registry and append DR-001 `schema_warning` siblings for invalid registered entries while preserving unknown future types. |
| EGR-019 | P2 | fixed | The reusable repair-loop primitive was not adopted by existing Ralph/review/squad loops. | `src/harness/repair_loop.py`, `src/harness/coordinator.py`, `tests/unit/test_repair_loop.py`, `tests/unit/test_coordinator_review_reentry.py`, `tests/unit/test_review_loop.py` | Fixed: the Phase 3 review-fix/re-entry cycle in `StrategyCoordinator` now runs through `RepairLoop` while preserving existing review terminal and re-entry semantics. |
| EGR-020 | P2 | fixed | Active role catalog and public architecture narrative needed reconciliation. | `README.md`, `docs/agent-role-catalog.md`, `docs/bbs/echelon-technical-dossier.md`, `extension/extension.yml`, `extension/workflow/definition.yaml`, `tests/kernel/test_agent_role_catalog_docs.py` | Fixed: public docs now distinguish 53 registered agent roles, 45 active-routed manifest roles, 8 manifest-only roles, one workflow-only alias, and 15 support prompt files. |
| EGR-021 | P2 | fixed | Extension/deployed-copy drift was a common operator footgun. | `src/harness/extension_drift.py`, `src/echelon/cli.py`, `README.md`, `tests/unit/test_extension_drift.py`, `tests/unit/test_cli_status.py` | Fixed: terminal `status`, `run`, `continue`, and `resume` warn when the installed `.specify/extensions/echelon` copy differs from a trusted source extension, while ignoring project-local config. Trusted sources are explicit `ECHELON_EXTENSION_SOURCE`, installed source metadata, or a verified editable checkout. |
| EGR-022 | P2 | fixed | Core shell contract tests remained outside pytest collection. | `tests/contract/no_new_deps.py`, `tests/unit/test_no_new_deps_pytest.py`, `tests/contract/registry_sync.py`, `tests/unit/test_registry_sync_pytest.py`, `tests/contract/language_rules.py`, `tests/unit/test_language_rules_pytest.py`, `tests/contract/static_contracts.py`, `tests/unit/test_static_contracts_pytest.py`, `tests/run-all.sh`, `tests/README.md`. | Fixed: deterministic repository, registry, language-rule, prompt, KB, and static schema contracts now run under pytest. Remaining shell tests intentionally cover shell helpers, filesystem/process behavior, locks/corruption, integration/e2e flows, and benchmarks. |
| EGR-023 | P1 | fixed | Python journal writers still persisted invalid registered journal entries as first-class records with a warning sibling. | `src/harness/journal_entry_validator.py`, `src/harness/squad.py::_write_journal_entries()`, `src/harness/squad_executors.py::_write_journal_entries()`, `extension/templates/echelon-result-template.yaml`, `extension/workflow/journal-entry-types.yaml`, `tests/unit/test_journal_entry_validator.py`, `tests/kernel/test_squad_executors_journal.py` | Fixed: Python writer boundaries now use quarantine mode for invalid registered entries, replacing them with canonical `schema_warning` records while preserving unknown future types. The canonical result template now shows schema-complete `insight.data`. |
| EGR-024 | P1 | fixed | Prompt and example migration for journal entries was not statically enforced across all agent/phase files. | `src/harness/journal_prompt_validator.py`, `tests/unit/test_journal_prompt_validator.py`, `tests/kernel/test_prompt_references.py`, `extension/workflow/journal-entry-types.yaml`, migrated prompt examples under `extension/agents/`, `extension/commands/`, and `extension/workflow/phases/` | Fixed: concrete `echelon_result.journal_entries` examples are now scanned against the canonical registry; registered examples require schema-complete `data`, and concrete unregistered types fail the prompt-reference test. |
| EGR-025 | P1 | fixed | Workflow transition validation still allowed semantically wrong condition fields and could miss result/state contract drift. | `src/harness/workflow_validator.py`, `tests/kernel/test_workflow_validator.py` | Fixed: workflow validation now rejects unresolved condition fields such as `alignment = ALIGNED`, while accepting explicit result fields, known config/derived predicates, current/prior phase `allowed_state_updates`, transition `state_update` keys, and declared output fields. |
| EGR-026 | P1 | fixed | Verdict contracts were duplicated across workflow transitions, phase specs, agent prompts, and templates. | `src/harness/verdict_contract_validator.py`, `tests/kernel/test_verdict_contract_validator.py`, `tests/kernel/test_prompt_references.py`, `extension/agents/control/tracker.md`, `extension/workflow/phases/phase2-tracker-alignment.md`, `extension/templates/intent-alignment-check-template.md` | Fixed: explicit phase routing verdict contracts are compared against workflow transition verdicts, related agent prompts, and related templates. Legacy workflow verdicts remain allowed only when the phase spec explicitly declares them. |
| EGR-027 | P1 | fixed | `echelon continue` recovery could infer the first missing artifact producer instead of the next required workflow phase after a bad persisted state. | `src/echelon/cli.py`, `tests/unit/test_cli_continue.py` | Fixed: continue recovery now detects completed tracker alignment with `intent-alignment-check.md` present and incomplete `phase3-specialists`, then resumes at `phase3-specialists` before considering missing HOW artifacts. |
| EGR-028 | P2 | fixed | GUARDIAN mode config naming was inconsistent across docs/prompts and kernel/config. | `README.md`, `extension/workflow/phases/phase3-specialists.md`, `extension/agents/specialists/guardian.md`, `extension/agents/control/commander.md`, `extension/config-template.yml`, `extension/echelon-config.yml`, `tests/contract/static_contracts.py`, `tests/unit/test_static_contracts_pytest.py`. | Fixed: public docs and dispatch prompts now use `specialists.guardian_mode`; workflow conditions keep the derived predicate `guardian_mode = ...`; legacy top-level config fallback remains implementation-compatible but is no longer the public contract. |
| EGR-029 | P1 | fixed | Phase 4 finalization can declare `READY TO BUILD` while run-local Phase A artifacts are not published to the project-visible spec directory. | `src/harness/squad.py`, `src/echelon/cli.py`, `src/echelon/artifact_index.py`, `tests/integration/test_squad_controller.py`, `tests/unit/test_cli_continue.py`. | Fixed: `phase4-document` publishes run-local Phase A artifacts into the project-visible spec directory, writes `ARTIFACTS.md`, records `published_spec_dir`, and blocks if the published build inputs are incomplete. `echelon continue` now routes done-but-unpublished runs back to `phase4-document` instead of claiming build-ready. |
| EGR-030 | P1 | fixed | Lexicon Gate can replace the standard rich `spec.md` contract by default, removing spec-kit metadata and human-facing sections that downstream users expect. | `extension/echelon-config.yml`, `extension/config-template.yml`, `extension/workflow/phases/phase1-what.md`, `extension/agents/exploration/cartographer.md`, `extension/agents/solution/orchestrator.md`, `docs/pipeline-matrix.md`, `tests/contract/static_contracts.py`, `tests/unit/test_static_contracts_pytest.py`, `tests/unit/test_tasks_config.py`. | Fixed: `spec.md` remains the canonical rich spec-kit feature specification; CARTOGRAPHER derives `requirements.lexicon.md` for controlled-grammar validation; the tasks gate validates against configured `spec_ref`; docs/config/tests now preserve the dual-artifact contract. |
| EGR-031 | P1 | fixed | `echelon status` roadmap hardcodes a stale Phase A phase list instead of deriving from the externalized workflow definition. | PR #21 / commit `07279388913057066329ccdf2285d1fa821e7987`; `src/echelon/cli.py::_ROADMAP_PHASES`; `extension/workflow/definition.yaml` includes real Phase A nodes missing from the roadmap such as `phase1-synthesizer`, `phase1-modeler`, `phase1-tracker`, `phase1-why1`, and `checkpoint-assess`; `tests/unit/test_status_roadmap.py` only checks the hardcoded length/order. | Fixed: `echelon status` derives the roadmap from the primary forward path in `workflow/definition.yaml`, uses the installed extension workflow at runtime with a source-tree fallback, and counts phases from `completed_phases` even when the same phase is currently being re-dispatched. |
| EGR-032 | P1 | fixed | Codegen RUNNABLE phase wording overclaims boot/render verification while the implementation performs only static composition evidence checks. | PR #19 / commit `aff5aa4dc0071a836d8586d076bb29072d8aa3df`; `src/codegen/runner/runnable_gate.py::_browser_probe()` explicitly performs build plus static component-mount detection with no browser/backend/render check; `extension/workflow/phases/codegen-6c-runnable.md` still says the app boots and primary surface renders; `src/codegen/delivery/verification_manifest.py` correctly describes `runnable_gate` as static composition. | Fixed: RUNNABLE phase and DELIVER precondition wording now describe build plus static feature-composition evidence, explicitly avoid boot/render/runtime claims, and reserve browser/runtime verification for a future gate; runnable tests enforce the wording boundary. |
| EGR-033 | P1 | fixed | `requirements.lexicon.md` now exists as a derived validation artifact, but its artifact status and relationship to `spec.md`, `plan.md`, and `tasks.md` are not fully enforced. It is listed as a Phase 1 output and consumed by validator paths, but not represented in `ARTIFACTS.md`, Phase A readiness, or a deterministic freshness/ID-equivalence contract. | EGR-030 follow-up; `extension/workflow/definition.yaml` lists `requirements.lexicon.md` only as `phase1-what` output; `src/echelon/artifact_index.py` omits it; `extension/workflow/phases/phase3-plan.md` prose omits `spec.md` while `definition.yaml` includes it; build/codegen Phase A validation still only verifies core rich artifacts; `requirements.lexicon.md` has no source hash/freshness check. GitHub issue #54. | Fixed: `requirements.lexicon.md` is registered in `ARTIFACTS.md` as a derived requirements index; Lexicon parser accepts leading `SOURCE` metadata; `lexicon validate --source-ref spec.md` enforces source hash freshness and REQ/AC/ERROR ID projection from canonical `spec.md`; CARTOGRAPHER invokes that source-ref validation; `phase3-plan` context-pack prose now includes canonical `spec.md`. |
| EGR-034 | P1 | fixed | The RE CodeGraph bridge executed a vendored CodeGraph dist without a deterministic provenance/version/integrity contract. | `extension/scripts/node/re/codegraph-adapter.js` imports `./vendor/codegraph/dist/index`; `extension/scripts/node/re/vendor/codegraph/package.json` reports `@colbymchenry/codegraph` `0.7.2`; 290 tracked dist files are vendored under `extension/scripts/node/re/vendor/codegraph/dist`; previous tests only asserted install wiring and optional global CLI `CODEGRAPH_CLI_VERSION="1.0.1"`. GitHub issue #55. | Fixed: `extension/scripts/node/re/vendor/codegraph/echelon-vendor.json` records package name/version, source tarball, npm integrity, license evidence, payload hash, update procedure, runtime import, and optional global CLI split; `PROVENANCE.md` documents the same contract; `tests/kernel/test_codegraph_integration_contract.py` verifies manifest/version/hash/import-path assumptions. |
| EGR-035 | P1 | fixed | Project context and memory reconciliation are not first-class in the squad path: GOLDDIGGER Mode 2 is documented but not executed, canonical prior specs are not reliably converted into next-run context, WIP artifacts are not consistently summarized for later phases, and MemPalace lacks hash/lifecycle reconciliation before reuse. | `src/echelon/context_metadata.py`, `src/echelon/context_reconciliation.py`, `src/echelon/context_builder.py`, `src/harness/squad.py`, `src/harness/squad_executors.py`, `src/harness/squad_state.py`, `src/codegen/memory/mempalace_writer.py`, `src/codegen/memory/requirements_miner.py`, `extension/workflow/definition.yaml`, `extension/config-template.yml`, `extension/echelon-config.yml`, and focused tests under `tests/unit/`, `tests/kernel/`, and `tests/integration/`. | Fixed: finalized canonical specs generate feature metadata and reconciled context, WIP artifacts remain run-local, MemPalace retrieval is hash/lifecycle checked before prompt use, and GOLDDIGGER Mode 2 queue processing is executable. |
| EGR-036 | P1 | fixed | CARTOGRAPHER did not have a precise operational contract for the Understanding and Lexicon CLIs during amendment passes, so live `echelon resume` output showed the agent guessing `understanding validate`, hitting JSON parse errors, and reading source files to discover `understanding scan` and Lexicon source-contract behavior. | Live resume transcript attached in the implementation thread; `extension/agents/exploration/cartographer.md` described requirement formatting and Lexicon goals but did not name the canonical `understanding scan ... --output` diagnostic command or forbid guessed validation subcommands; `extension/workflow/phases/phase1-what.md` did not include the tool contract in the dispatch prompt. | Fixed: CARTOGRAPHER now has an explicit Validation Tool Contract: `understanding scan "{spec_dir}/spec.md" --enhanced --per-req --json --output /tmp/cartographer-understanding.json` for diagnostic scoring only, no guessed `understanding validate`, and `lexicon validate ... --source-ref ... --json` as the authoritative Lexicon gate. Static pytest coverage enforces the contract. |
| EGR-037 | P1 | fixed | Agent and phase prompts could still contain uncontracted tool references, allowing LLMs to guess command names, output shapes, or source-discovery paths during live runs. | EGR-036 was found from a live `echelon resume` transcript where CARTOGRAPHER guessed `understanding validate` and read `src/understanding` files to discover the real hidden `scan` command. Later EGR-038 found SAGE using the wrong Understanding JSON root. Before this fix, only one-off prompt contracts were statically protected. | Fixed: `tests/contract/prompt_tool_contracts.py` now scans agent and phase prompts for executable tool references that lack an exact nearby command, Skill id, slash command, or first-class tool name. Static pytest coverage caught and fixed ambiguous CHIEF verification, GOLDDIGGER re-extract, SAGE consensus Understanding, and verify-spec harness-command references. |
| EGR-038 | P1 | fixed | SAGE's Understanding follow-up handoff could still read enhanced JSON with the wrong root shape, producing empty behavioral-transition evidence and jq errors during live resume. | Live resume transcript showed SAGE first extracting empty behavioral handoff data, then failing with `Cannot index array with string "behavioral_analysis"` before discovering that Understanding JSON is an array. `extension/agents/exploration/appendices/sage-understanding-followup-reference.md` documented `[0]` paths at the top but later instructed `behavioral_analysis.transitions[]`; `extension/agents/exploration/sage.md` repeated the unindexed path. | Fixed: SAGE now extracts `.[0].behavioral_analysis.transitions`, uses `// []` for missing transition lists, preserves `-` for null guard/action/outcome fields, and documents empty transition lists as SENTINEL handoff warnings rather than complete coverage. Static pytest coverage enforces the handoff contract. |
| EGR-039 | P1 | fixed | `echelon resume` could still end in vague manual recovery when the re-dispatched phase failed with `missing_echelon_result`, even though the failed phase was known and retryable. | Live resume transcript after checkpoint option A: checkpoint-assess produced a valid BLOCKED `echelon_result`; `echelon resume` accepted the answer and resumed into `phase1-why2`; SAGE emitted no final `echelon_result`; the controller blocked with `missing_echelon_result`, but next-step output said `RUN BLOCKED — manual recovery required` and `no human question, safe rewind target, or retryable dispatch was recorded` despite `last_dispatch.phase_id = phase1-why2`. Root cause: recovery suppressed `last_dispatch.phase_id` when the phase already appeared in `completed_phases`, which is wrong for re-dispatched phases. | Fixed: retryable dispatch-block recovery now trusts the latest `last_dispatch.phase_id` even if a previous pass of the same phase is in `completed_phases`. Regression coverage verifies `missing_echelon_result` after human resume routes `echelon continue` back to `phase1-why2` instead of manual recovery. |
| EGR-040 | P1 | fixed | Existing project runtime configs can preserve stale Lexicon task `spec_ref` values after source contract changes, causing agents to compensate manually instead of relying on deterministic config. | Live `/tmp/echelon.out` from a current `echelon run` showed ORCHESTRATOR reading `spec_ref: spec.md`, observing that rich `spec.md` yields false task-orphan findings, and overriding to `requirements.lexicon.md` by reasoning. Current source config already uses `lexicon_gate.artifacts.tasks.spec_ref: requirements.lexicon.md` in `extension/echelon-config.yml` and `extension/config-template.yml`, so the issue is migration or runtime validation of existing `.specify/extensions/echelon/echelon-config.yml` copies. | Fixed: terminal CLI now detects stale project config before Phase A dispatch. `echelon status` shows a compatibility warning; `echelon run` and `echelon resume` block before agent dispatch when `lexicon_gate.artifacts.tasks.spec_ref` does not match the derived Lexicon spec artifact path. |
| EGR-041 | P1 | fixed | Full fulfillment refresh cache can reuse stale `fulfillment-report.md` after uncommitted source/test changes, causing Ralph to block on obsolete requirement evidence. | Attached live `echelon harness run` fragment showed verified build slices writing new source/tests and `.harness-build-status.json`, then `fulfillment refresh: cached (full verify-spec cache hit)` followed by a block on a report still describing the earlier `T-001` skeleton. `src/harness/fulfillment_runner.py` keyed full refresh cache on git commit plus spec-input hash; before the checkpoint commit, implementation files can change while the commit remains unchanged. | Fixed: full verify-spec cache keys now include a deterministic implementation-input content hash covering source, tests, and build manifests; fulfillment report metadata records `implementation_input_hash`; regression coverage verifies cache invalidation when source changes without a commit change. |
| EGR-042 | P1 | fixed | Harness blocker UX can contradict itself and use slash-command guidance in terminal CLI output. | Attached live harness fragment printed `HARNESS — BLOCKED (same_failure_repeat)` with question text saying the same failure occurred `3 or more times`, but context said `Same failure detected 1 time(s)`. The same banner advised `Run /speckit-harness-resume with your answer` in a terminal `echelon harness run` context. | Fixed: repeated-failure context now reports the actual consecutive streak, threshold, and fingerprint count; terminal harness escalation banners/files instruct users to append `## Answer` and run `echelon harness resume <spec_id>` instead of the spec-kit slash command. |
| EGR-043 | P1 | fixed | Verify-spec asks Claude for specialized build subagent types that are not dispatchable in the terminal harness registry. | `/tmp/echelon.harness.run.3` showed repeated `Agent type 'speckit-echelon-spec-fulfillment-auditor' not found` failures before the LLM fell back to `claude`/`general-purpose` and inlined markdown role files from `.claude/agents/`. The runtime sync copies `extension/agents/**/*.md` into `.claude/agents/`, but the source markdown lacks Claude custom-agent frontmatter. | Fixed: runtime extension sync now wraps copied agent prompts in Claude custom-agent frontmatter when the source prompt lacks it, preserving existing frontmatter when present. |
| EGR-044 | P1 | fixed | Harness task-progress bookkeeping can lag implemented slices, leaving `tasks.md` unchecked while code/tests exist and pass. | `/tmp/echelon.harness.run.3` final verify-spec output recorded `tasks.md` marking `T-002` through `T-022` unchecked while corresponding library code and tests existed and passed; progress-integrity still reported only `T-001 DONE`. | Fixed: build slices that report `completed_task_ids` now hard-block before verification if Ralph cannot mark those IDs DONE in canonical `tasks.md`. |
| EGR-045 | P1 | fixed | Harness build can proceed with a placeholder constitution instead of blocking or making the missing authority explicit. | `/tmp/echelon.harness.run.3` reported `constitution.md recovered — it's an unfilled template, so I'll rely on spec/plan/test-strategy for the actual rules` and continued into BUILD_INIT. This makes constitutional authority prompt-level rather than deterministic. | Fixed: `constitution.md` is now a required Phase A build input, placeholder markers fail readiness, and Phase 4 publishes the project constitution snapshot into the build-ready spec directory. |
| EGR-046 | P2 | fixed | CodeGraph evidence pre-map can overstate confidence through term-match-only evidence, forcing the LLM mapper to correct deterministic overreach. | `/tmp/echelon.harness.run.3` showed CodeGraph producing 106 medium-confidence rows with an empty fallback queue, then IMPLEMENTATION-MAPPER downgraded many rows because the cited evidence was generic term matches such as frame/theme/error symbols rather than behavior-specific implementation or measured runtime proof. | Fixed: term-match-only source/test evidence is now low-confidence fallback evidence unless direct requirement IDs or coverage-map links support the row. |
| EGR-047 | P1 | fixed | Legacy `scripts/bash/dry-run.sh` failed as a validation gate because it mixed repository-root and extension-root assumptions and retained stale command-wrapper role checks. | `bash scripts/bash/dry-run.sh` treated the repo root as the extension root; `bash scripts/bash/dry-run.sh extension` still checked repo-owned paths under the extension; the role-separation check expected the thin `echelon.run.md` wrapper to contain legacy `Role Separation` text even though COMMANDER and the Python squad harness now own that contract. GitHub issue #68. | Fixed: dry-run now resolves `REPO_ROOT` and `EXT_ROOT` explicitly, validates repo-owned and extension-owned assets in their proper locations, checks the current thin-wrapper plus COMMANDER role-separation contract, and has pytest coverage for both documented invocation styles. |
| EGR-048 | P1 | fixed | `echelon harness run` can still dispatch the build LLM when Phase A build inputs contain a placeholder `constitution.md`. | `/tmp/echelon.harness.run.4` lines 54 and 92 show the harness/build prompt detecting an unfilled constitution template and proceeding by relying on plan/ADRs; lines 66-68 declare BUILD_INIT complete and dispatch IMPLEMENTER. The copied constitution files contain unresolved markers such as `[PROJECT_NAME]`, `[PRINCIPLE_1_NAME]`, and `[CONSTITUTION_VERSION]`. GitHub issue #69. | Fixed: terminal harness-run preflight now validates published Phase A build inputs with the shared readiness validator before parent polyrepo dispatch or local build dispatch; `build-1-init.md` no longer copies or repairs `constitution.md` and hard-stops on missing/template snapshots. |
| EGR-049 | P2 | fixed | Provider session-limit failures are recorded in harness state but surface as raw provider text without clear blocked-run recovery guidance. | `/tmp/echelon.harness.run.4` lines 121-126 end with raw `You've hit your session limit` output. Persisted state records `status: blocked`, `build_status: provider_session_limit`, `termination_reason: build_incomplete`, and `salvage_commit`, but terminal output does not show a dedicated blocked banner, salvage details, or next safe command. | Fixed: provider session-limit failures now render as first-class harness blocks in the Ralph stop banner, `echelon status` next-step output, and harness delivery summary, including provider reset hints, salvage branch/commit/verification, retry guidance, and token accounting where available. |
| EGR-050 | P1 | fixed | Constitution ownership and path semantics are inconsistent across prompts and harness paths, making spec-kit's `.specify/memory/constitution.md` only partly authoritative. | CHIEF correctly owns `.specify/memory/constitution.md` via `speckit.constitution`, but `chief.md` and `phase1-what.md` allow manual placeholder repair, `build-1-init.md` and `codegen-A-preamble.md` copy constitution into spec dirs as recovery, `phase3-how.md` allows ARCHITECT to output `constitution.md`, and `artifact_index.py` labels the snapshot owner as Planner. GitHub issue #71. | Fixed: CHIEF retries through `speckit.constitution` or blocks instead of editing directly; non-CHIEF prompts no longer copy, repair, append, or output `constitution.md`; codegen/finalize validate snapshots; artifact metadata marks CHIEF ownership; static tests enforce the source-of-truth contract. |
| EGR-051 | P1 | fixed | Harness recovery can fail on salvage/checkpoint cherry-pick when untracked Phase A/spec artifacts collide with files added by the recovered commit. | Live `echelon harness resume 001` after provider session-limit salvage failed with Git's `untracked working tree files would be overwritten by merge` error for many `specs/001-themed-ascii-animation/*` files, including `spec.md`, `tasks.md`, `requirements.lexicon.md`, diagrams, and `harness-run-history.json`. GitHub issue #72. | Fixed: recovery now preflights untracked paths that collide with the recovered commit. Identical duplicates are removed before cherry-pick and restored as tracked files; differing duplicates are backed up under `.echelon/recovery-backups/<commit>/...` before cherry-pick, and `echelon harness resume` reports the backup count and directory. |
| EGR-052 | P1 | fixed | Constitution context-pack usage is incomplete and inconsistent after constitution source-of-truth hardening. | `phase1-what.md` does not include constitution in CARTOGRAPHER's spec-authoring context, `phase3-plan.md` omits `constitution.md` while ORCHESTRATOR expects it, `phase3-consensus.md` includes constitution for ASSESS2 but not PLAN2, and `architect.md` still contains stale `.specify/memory/constitution.md` fallback language. GitHub issue #73. | Fixed: CARTOGRAPHER, ORCHESTRATOR PLAN, SAGE WHY3, and ORCHESTRATOR PLAN2 now receive read-only constitution context; ARCHITECT no longer claims fallback constitution creation/edit authority; static contract tests enforce context-pack and prompt alignment. |
| EGR-053 | P2 | fixed | Echelon lacks a controlled single-phase repair/replay command for targeted workflow fixes. | Users can continue/resume/rewind limited checkpoints, but cannot run `phase1-constitution` or another specific phase directly without either running too much workflow or bypassing COMMANDER/state/journal contracts. GitHub issue #76. | Fixed: `echelon phase list` lists workflow phase IDs, and `echelon phase run <phase-id> [--spec <id>]` dispatches exactly one phase through the normal squad executor/state/journal path, records manual replay provenance, targets the resolved spec directory, refreshes published artifacts, then stops for explicit `echelon continue`. |
| EGR-054 | P1 | fixed | Echelon treated branchless workspace folders and child implementation repositories inconsistently, causing polyrepo source-root discovery, harness target selection, and reverse-engineering evidence to depend on ad hoc path guesses. | Branch `workspace-source-roots-design`; design and plan in `docs/superpowers/specs/2026-06-18-workspace-source-roots-design.md` and `docs/superpowers/plans/2026-06-18-workspace-source-roots.md`; implementation across `src/echelon/workspace_model.py`, `src/echelon/target_detection.py`, `src/echelon/orchestrator.py`, `src/harness/state.py`, `src/harness/target_state.py`, RE agents/prompts, `docs/workspace-model.md`, and `src/echelon/workspace_git_migration.py`. GitHub issue #70. | Fixed on the feature branch: workspaces are Git-backed orchestration roots with zero or more source roots; RE emits and prefers `workspace-manifest.json`; harness resolves targets from source roots and persists workspace/source metadata; branchless workspaces are blocked for new runs with legacy recovery only; a one-time migration script initializes lightweight workspace Git without staging child source repos. |
| EGR-055 | P1 | fixed | `echelon harness resume` rejects repaired `harness_error` state after targeted Phase A repair and can retain stale spec artifact paths. | Live `echelon phase run phase1-constitution` repaired a placeholder constitution, but subsequent `echelon harness resume 001` failed with `Spec '001' is blocked for a different reason: 'harness_error'`. Persisted state also pointed `spec_dir`, `spec_file`, and `tasks_file` at a different spec outside the current project. GitHub issue #77. | Fixed: harness resume now refreshes persisted spec artifact paths from the current project and retries previous `harness_error` runs only after deterministic preflight confirms a resolvable spec directory, canonical non-empty `tasks.md`, and Phase A readiness; otherwise it remains blocked with explicit blockers. |
| EGR-056 | P1 | fixed | Harness build worktrees can use stale Phase A artifacts after project-visible preflight passes. | Live `echelon harness resume 001` after EGR-055 accepted the repaired state, created a build worktree from feature branch `001-themed-ascii-animation`, and the build agent found template markers in both worktree `specs/001-themed-ascii-animation/constitution.md` and worktree `.specify/memory/constitution.md`, even though the project-visible files were placeholder-clean. GitHub issue #78. | Fixed: Ralph now materializes current project-visible Phase A spec inputs and canonical constitution into each generated build worktree before build LLM dispatch, then validates the worktree copy with the shared Phase A readiness gate and blocks deterministically if it is still invalid. |
| EGR-057 | P1 | fixed | `echelon harness resume` requires git recovery for Phase A readiness blocks with no salvage work. | Live `echelon harness resume 001` after EGR-056 still failed before retry with `Project has tracked changes; commit/stash them before recovery`, even though current state was `termination_reason=build_incomplete`, `build_reason=Phase A artifacts are not build-ready`, and had no `salvage_commit` or checkpoint commit to recover. GitHub issue #79. | Fixed: build-incomplete states caused by Phase A readiness blockers and lacking salvage/checkpoint/target commits now skip cherry-pick recovery, rerun deterministic Phase A preflight, and retry the harness directly; true salvage/checkpoint build-incomplete states still use recovery. |
| EGR-058 | P1 | fixed | Ralph escalates repeated full-spec `fulfillment-gaps` even when inner fixes complete new canonical task IDs. | Live `/tmp/echelon.harness.run.5` showed `echelon harness resume` successfully completing T-001, then inner fixes completing T-002 and T-005 with `.harness-build-status.json` markers, but final state still had `status=blocked`, `termination_reason=blocker_escalation`, repeated `fulfillment-gaps`, and `build.task_results` only recorded T-001. GitHub issue #80. | Fixed: Ralph now preserves `completed_task_ids` from inner feedback invocations, applies them to canonical `tasks.md`/state before checkpointing, passes scoped completion context to fulfillment refresh, and treats a sole remaining full-spec `fulfillment-gaps` failure after task progress as an outer-loop continuation boundary rather than same-failure escalation. |
| EGR-059 | P1 | fixed | `echelon harness resume` rejects legitimate `blocker_escalation` state and tells users to use `harness run` to resume. | Live `echelon harness resume 001 max_outer=10` output printed `Spec '001' is blocked for a different reason: 'blocker_escalation'` followed by `Use 'echelon harness run <spec_id>' to resume`, contradicting the harness recovery command contract. GitHub issue #81. | Fixed: `blocker_escalation` is now an accepted no-recovery resume reason that delegates back into the harness coordinator; pending unanswered escalation files still render the existing answer guidance, and unsupported blocked reasons now distinguish retrying `harness resume` from discarding state with `harness run --reset`. |
| EGR-060 | P1 | fixed | Missing `verify_command` recovery can loop between `harness resume` and `harness init` after auto-detection already failed. | Live `echelon harness resume 001` printed `Option 1 — auto-detect: echelon harness init`; `echelon harness init` then reported `No verify_command detected: no high-confidence test runner detected` and still printed `echelon harness run <spec_id>` as next step; a second `harness resume` repeated the same init advice. GitHub issue #82. | Fixed: `harness init` now prints manual `verify_command` setup as the next step when detection declines, and `harness resume` uses persisted detection metadata to stop recommending another init once auto-detection already failed or was ambiguous. |
| EGR-061 | P1 | fixed | Top-level approved LLM tool policy can be silently ignored by the harness when a nested `harness:` section exists. | Live `/tmp/echelon.harness.run.7` showed repeated Claude `Write` and `Bash` permission denials even though project config had top-level `llm.tool_policy.allow_unsafe_host_execution: true`; the effective nested harness config only had `harness.llm.cli: claude`, so the approval never reached the provider. GitHub issue #83. | Fixed: harness config loading now treats top-level `llm` as lower-precedence compatibility defaults for `harness.llm`, while nested `harness.llm` overrides specific values. Approved top-level tool policy now reaches the harness provider and adds the intended Claude permission-bypass flag. |
| EGR-062 | P1 | fixed | Provider session limits during verify-spec fulfillment refresh were flattened into ordinary verification failures, so Ralph could enter the fix loop even though the provider budget was exhausted; stale fulfillment reports also lacked clear invalidation context against the newer checkpoint. | Live `/tmp/echelon.harness.run.8` completed a substantial implementation checkpoint (`b942068`, 20/24 tasks), then hit Claude session limit during verify-spec. Existing `fulfillment-report.md` still advertised evidence for older checkpoint `7e7b0d1`, while state ended `build_status=provider_session_limit`. GitHub issue #84. | Fixed: `FulfillmentRunner` now returns `provider_session_limit` for verify-spec provider exhaustion and includes stale report commit context when present; Ralph treats that result as a first-class provider-session-limit checkpoint block and does not dispatch feedback/fix prompts for it. |
| EGR-063 | P2 | fixed | Echelon lacked a benchmark-backed way to test whether cleansing non-spec LLM-consumed artifacts improves build speed or quality. | `src/echelon/benchmark.py`, `src/echelon/cli.py`, `extension/workflow/definition.yaml`, `extension/workflow/phases/phase-exp-constitution-quality.md`, `extension/workflow/phases/phase-exp-tasks-quality.md`, `extension/workflow/phases/phase-exp-adr-quality.md`, `tests/unit/test_benchmark.py`, `tests/unit/test_cli_phase.py`, `tests/kernel/test_phase_graph.py`, GitHub issue #85. | Fixed: benchmark fixtures and variants are deterministic, experimental artifact-quality phases are manually runnable, and benchmark summaries compare build outcome metrics without changing the default workflow. |
| EGR-064 | P1 | fixed | `echelon harness resume` can still block on tracked changes in the user's current checkout even when the selected checkpoint commit is already present on the target feature branch. | Live resume after `/tmp/echelon.harness.run.8` failed with `Project has tracked changes; commit/stash them before recovery`; target checkout was on `master` with a tracked `.specify/memory/constitution.md` edit, while checkpoint `b942068` was already contained in branch `001-themed-ascii-animation`. GitHub issue #86. | Fixed: harness recovery now checks whether the recovered commit is already contained in the resolved target branch before dirty-worktree validation. Already-present checkpoints return `applied=False` without checkout/cherry-pick; dirty-tree blocking remains for commits that still need application. |
| EGR-065 | P1 | fixed | Ralph can salvage a provider-limit worktree with `tasks.md` regressed to pending while Python-owned harness state still records completed tasks. | `/tmp/echelon.harness.run.9` ended blocked correctly on provider session limit with salvage commit `0f2e1a8465d0226801f0608ff34a09df00b865f5`; state recorded 7 completed tasks (`T-023`, `T-024`, `T-003`, `T-012`, `T-015`, `T-019`, `T-022`), but retained worktree `iter-4/specs/001-themed-ascii-animation/tasks.md` had 0 checked canonical task rows. `python -m harness validate-task-progress ...` reproduced the mismatch. The likely boundary is `_sync_phase_a_inputs_into_worktree`, which copies stale project-visible Phase A inputs over the worktree before build/resume. | Fixed: Ralph now reconciles copied worktree `tasks.md` from `state.build.task_results` after Phase A input sync and before readiness validation, preserving completed task rows across stale source artifact copies. |
| EGR-066 | P2 | fixed | Build agents can diverge from the declared sequential quality-gate workflow by skipping gates by reasoning or dispatching them concurrently. | `extension/workflow/definition.yaml` declares build gate order as `SPEC GUARD -> CODE REVIEWER -> TEST GUARDIAN`, but `/tmp/echelon.harness.run.9` showed CODE REVIEWER/TEST GUARDIAN skipped by vacuity for a markdown-only spike and later showed final-slice SPEC GUARD, CODE REVIEWER, and TEST GUARDIAN dispatched concurrently. | Fixed: the `echelon.build` harness command and `build-2-implement.md` now state that SPEC GUARD, CODE REVIEWER, and TEST GUARDIAN are sequential hard gates, not a parallel batch. CODE REVIEWER/TEST GUARDIAN may not be skipped by vacuity; skips require an explicit workflow-approved condition and journaled rationale. The generic COMMANDER prompt remains judge/governance-oriented rather than owning build gate execution. |
| EGR-067 | P2 | fixed | Quality-gate reporting can say ruff is clean while formatting checks still fail. | `/tmp/echelon.harness.run.9` reported `ruff check` clean, but `ruff format --check tests/performance/test_perf.py tests/performance/test_perf_pty.py` still failed with two files that would be reformatted. | Fixed: the Python language rule loaded by CODE REVIEWER now requires both `ruff check` and `ruff format --check` before reporting Python style, lint, or formatting as clean. Static prompt coverage enforces that the generic CODE REVIEWER stays language-neutral and Python-specific Ruff policy lives in `knowledge-base/language-rules/python.md`. |
| EGR-068 | P2 | fixed | Verify-spec CodeGraph evidence can fall back to broad low-confidence source exploration even when CodeGraph indexed the codebase successfully and source/test call relationships exist. | `/tmp/echelon.harness.run.9` lines 538, 553, 1185, 1191, 2453, and 2581 show all 106 canonical IDs landing in the low-confidence fallback queue. The retained `codegraph-evidence-map.json` had counts `low=106`, `medium=0`, `high=0`; every evidence reason was `term_match:*`, while `codegraph-summary.json` showed a ready index with 565 symbols and 523 call edges. The coverage map listed rich requirement-to-test-case IDs but not concrete test file/function paths, so `coverage_map_path_match` never fired; the mapper also ignored `call_graph` edges such as performance tests calling `Engine::run`, `CapabilityDetector::detect`, and `Scene::update`. Latest retained `iter-4` artifacts also referenced `iter-3` source paths, suggesting stale-root detection is missing. | Fixed: CodeGraph evidence refresh now rejects stale exports whose `repo_path` differs from the current project root before writing summaries, falling back to a fresh vendored bridge export when available. The deterministic mapper now uses call edges from requirement-anchored test symbols to lift directly called implementation symbols, while pure term-match-only evidence remains low-confidence fallback. |
| EGR-069 | P1 | fixed | `echelon harness resume` can reject a valid provider-limit salvage commit because later generated verification artifacts leave the preserved worktree dirty. | Live `CLAUDE_CONFIG_DIR=~/.claude-personal-gmail echelon harness resume 001 max_outer=10` on 2026-07-02 failed with `Preserved worktree has uncommitted tracked changes` for `runs/build-20260701-222341-118320/worktrees/default/iter-4`. That worktree was already at salvage commit `0f2e1a8465d0226801f0608ff34a09df00b865f5`, but later verification reran and modified tracked generated metrics `docs/perf/perf-metrics.json` and `docs/perf/perf-metrics-pty.json`. `src/harness/recovery.py::_find_preserved_worktree_source()` checked dirty tracked files before checking the state-recorded `salvage_commit`, so recovery never reached the valid commit. | Fixed: preserved-worktree recovery now resolves explicitly state-recorded checkpoint or salvage commits before applying the dirty-worktree guard. Dirty preserved worktrees still block heuristic fallback commit discovery when no recorded commit can be used. |
| EGR-070 | P1 | fixed | `echelon land` can reject a successfully converged harness build because readiness and fulfillment preflight read stale artifacts from the current checkout instead of the verified feature branch. | `/tmp/echelon.harness.run.10` ended with `LAND — SPEC NOT READY` and `current status is (missing)` even though harness state was `converged`, 24/24 tasks were DONE, verify-spec reported `106/106 IMPLEMENTED`, and branch `001-themed-ascii-animation` contained `spec.md` frontmatter `status: ready_to_land`. Branch HEAD also included a final verification/status-artifact commit after the implementation checkpoint, so strict `verified_commit == HEAD` would be a second false block. GitHub issue #87. | Fixed: land preflight now falls back to the resolved feature branch snapshot when current checkout artifacts would block. Fulfillment reports verified at an ancestor are accepted only when later branch commits do not change implementation inputs or semantic spec inputs; dirty-tree/merge protections remain on the mutating preparation path. |
| EGR-071 | P1 | fixed | Workspace Git initialization guidance and migration can accidentally encourage tracking spec-kit runtime state under `.specify/`, including `.specify/memory/constitution.md`, even though Echelon publishes a tracked read-only constitution snapshot under `specs/<id>-*/constitution.md`. | Live test workspace `codex_echelon_test` showed `.specify/memory/constitution.md` as a tracked modified file while `.specify/extensions/`, `.specify/workflows/`, and other runtime files were untracked. Source evidence: `src/echelon/cli.py::_workspace_git_preflight()` printed `git add .gitignore .specify specs`; `src/echelon/workspace_git_migration.py` staged `.specify` during first Git initialization. GitHub issue #88. | Fixed: workspace Git guidance and migration now add `/.specify/` to `.gitignore` and stage only `.gitignore` plus `specs/`. Documentation clarifies that `.specify/` is local runtime state in generated projects and that `specs/<id>-*/constitution.md` is the tracked governance handoff. |
| EGR-072 | P1 | fixed | `echelon land` can still require manual conflict resolution during the `.specify/` runtime-state transition, breaking the expected autonomous run/continue/resume → harness run/resume → land path. | Live `echelon land 001 --prepare-only` stopped with conflicts in `.gitignore` and `.specify/memory/constitution.md`. Source evidence: `src/harness/land.py::prepare_feature_branch()` only autoresolved `.gitignore` when it was the sole conflict, and `_continue_feature_branch_preparation()` returned blocked before attempting any autoresolution. GitHub issue #89. | Fixed: land preparation and `--continue` now apply a narrow deterministic resolver for known land conflicts: union add/add `.gitignore` and remove `.specify/*` runtime files from the Git index. Source/spec artifact conflicts still block. |
| EGR-073 | P1 | fixed | Echelon still accepts too many implicit workspace shapes, causing scaffold/tracking/source-root ambiguity to surface late during continue, harness recovery, or land. | User direction after EGR-071/EGR-072: define how projects are scaffolded, what is tracked, what is ignored, where source roots live, and avoid supporting free-form layouts. Design: `docs/superpowers/specs/2026-07-02-workspace-contract-v1-design.md`. GitHub issue #90. | Fixed: Workspace Contract v1 is now represented in deterministic code and docs. Project config resolution prefers committed `.echelon/config.yml`, supports ignored `.echelon/local.yml`, and falls back to legacy extension config only during migration. `echelon harness init` and `echelon init` create/use canonical config for new workspaces. `discover_workspace()` honors explicit configured source roots before heuristic discovery, including planning-only `sources: []`. `echelon workspace doctor` validates Git/runtime/spec/source-root contract, and `echelon workspace migrate` copies legacy config, ignores/untracks runtime state, and stages canonical workspace files. Current harness command docs and key agent gate prompts now use canonical-first config guidance. |
| EGR-074 | P1 | fixed | `echelon land --continue` can crash after a successful merge commit in local/no-origin workspaces, then fail to resume because no merge is left in progress. | Live `echelon land 001 --continue` in `/Users/michalbachorik/work/codex_echelon_test` created `Merge master into 001-themed-ascii-animation`, then failed on `git push origin 001-themed-ascii-animation` because the local test repo has no `origin`. A retry would see a clean feature branch with no `MERGE_HEAD`. GitHub issue #91. | Fixed: land now skips prepared-branch publication when `origin` is absent or local, still pushes for non-local remotes, and `--continue` treats a clean feature branch that already contains the default branch as prepared after push-only failures. |
| EGR-075 | P1 | fixed | `echelon land --continue` can still fail after verification because generated perf metrics leave the feature branch dirty before the final direct merge checkout. | Live retry after EGR-074 advanced past prepared-branch push, then `git checkout master` failed because verification modified tracked `docs/perf/perf-metrics.json` and `docs/perf/perf-metrics-pty.json`. The same local/no-origin path would also fail after merge when pushing the default branch. GitHub issue #92. | Fixed: direct land now discards allowlisted generated verification drift before final merge, blocks on any other tracked dirty files, and skips landed default-branch push when `origin` is absent or local. |
| EGR-076 | P1 | fixed | Local/no-origin `echelon land --continue` can still fail after a successful default-branch merge because remote feature-branch deletion is mandatory, and a retry from the feature checkout does not recognize that the default branch already contains the feature. | Live retry after EGR-075 merged `001-themed-ascii-animation` into `master` and skipped default-branch push, then blocked on `git push origin --delete 001-themed-ascii-animation` because the local test repo has no `origin`. A following retry from the feature branch reported `no merge in progress to continue` even though `master` already contained the feature. GitHub issue #93. | Fixed: landing cleanup now skips remote feature-branch deletion when `origin` is absent or local, while preserving remote-head and delete-failure blocks for non-local remotes. Land also recognizes already-merged feature branches from either checkout, switches to default for cleanup, and completes local branch/worktree/status cleanup. |
| EGR-077 | P2 | fixed | Successful `echelon land` can still exit with an `EOFError` because the optional run-archive prompt calls `input()` in noninteractive contexts. | Live retry after EGR-076 completed land and deleted the local feature branch, then printed the archive prompt and crashed with `EOFError: EOF when reading a line`. GitHub issue #94. | Fixed: post-land run archival now skips with a clear message when stdin is noninteractive or unavailable; interactive terminals still get the archive prompt. |
| EGR-078 | P2 | fixed | Workspace Contract v1 misses `.echelon/recovery-backups/`, leaving recovery runtime state untracked after migration while `workspace doctor` still reports no findings. | Live check of `/Users/michalbachorik/work/codex_echelon_test` after EGR-073 migration showed `?? .echelon/recovery-backups/` while `echelon workspace doctor` reported `Buildable: yes` and `Findings: none`. GitHub issue #95. | Fixed: migration now ignores and untracks `.echelon/recovery-backups/`, doctor validates existing recovery backups as runtime state, and workspace docs/gitignore list the path as local runtime. |
| EGR-079 | P2 | fixed | CLI command taxonomy exposes Phase B as internal `harness` jargon, leaves landing as a separate top-level command, and treats `echelon help` as an unknown command before printing usage. | User review of `echelon help` output showed a mixed flat command list: top-level Phase A lifecycle commands, `workspace` and `spec` subcommands, `phase`, `harness`, and top-level `land`; `echelon help` printed `unknown command 'help'`. GitHub issue #96. | Fixed: `echelon delivery init/run/resume/land` is now the canonical Phase B namespace; `harness` and top-level `land` remain compatibility aliases; `echelon help` exits cleanly; help/README now present grouped workspace/spec/phase/delivery commands. |
| EGR-080 | P1 | fixed | Echelon lacked an implemented TECH WRITER documentation gate, so completed harness work could pass without an auditable README/CHANGELOG impact decision. | Existing standalone plan lacked an EGR binding; no `extension/agents/build/tech-writer.md`, no `build-8-documentation` workflow phase, no `src/harness/documentation_gate.py`, and no Ralph enforcement existed. GitHub issue #97. Implementation plan: `docs/superpowers/plans/2026-07-03-egr-080-tech-writer-documentation-gate.md`. | Fixed: TECH WRITER is registered and routed through `build-8-documentation` before finalization, Ralph enforces `documentation-impact-report.md` before publish, and README/CHANGELOG document the Keep a Changelog contract. |
| EGR-081 | P1 | fixed | `echelon workspace init` can fail with a raw `docker: command not found` from `deploy-init.sh` after already prompting/writing MemPalace state. | Live `echelon workspace init` in `/Users/michalbachorik/work/wkspace_test` validated default `deploy.type=http`, prompted for MemPalace wing, then invoked `extension/scripts/bash/deploy-init.sh`, where the HTTP Traefik path hardcoded `docker` and surfaced `/deploy-init.sh: line 237: docker: command not found`. The preceding `docker network create ... || echo network already exists` also masked the missing command as successful progress. GitHub issue #104. | Fixed: `workspace init` now treats HTTP deploy provisioning as best-effort workspace setup, not a hard workspace prerequisite. Missing Docker, unreachable daemon, timed-out `docker info`, and OS execution errors write `deploy.enabled: false`, skip HTTP deploy infra with an actionable warning, then continue MemPalace/config bootstrap and finish successfully. Explicit `deploy.enabled: false` skips deploy provisioning entirely. CLI deploy mode skips the HTTP/Traefik preflight. |
| EGR-082 | P2 | fixed | Artifact-quality benchmark variants can contaminate each other when run sequentially from the same working tree. | Manual benchmark protocol review after EGR-063 showed that running baseline, constitution, tasks, and ADR variants without resetting to the original Phase A artifact commit lets later variants inherit earlier cleanse mutations. GitHub issue #105. | Fixed: real `echelon benchmark run` now requires `--baseline-ref <ref>` and wraps variant execution with deterministic `git reset --hard <ref>` plus `git clean -fd -e runs/benchmarks/` before and after the variant, preserving benchmark summaries while returning the workspace to the committed baseline. |
| EGR-083 | P1 | fixed | WHY/SAGE quality routing treated non-boolean `quality_scores[*].pass` values as truthy, so a failed quality gate could incorrectly advance to checkpoint/Phase 2. | Live `/tmp/banzai.1` from `echelon spec run` produced `phase1-why2` `verdict: FAIL` with failing scores (`overall: 0.745`, `structure: 0.660`, `testability: 0.679`) but persisted `state.json.quality_scores[0].pass: "WHY2-iter-0"` instead of a boolean. `src/harness/condition_evaluator.py::_get("quality_gates.pass")` used Python truthiness, making the non-empty string pass and routing `phase1-why2 -> checkpoint-assess` instead of the deterministic `quality_gates.fail -> phase1-what` repair path. The SAGE/phase prompt output contract also made this misuse easy by overloading `pass` as an apparent pass identifier. | Fixed: `src/harness/quality_scores.py` centralizes boolean-only pass handling, threshold/verdict derivation, and shape validation. `src/harness/squad_executors.py` and `src/harness/squad.py` normalize WHY quality scores before validation/routing/state advance; `src/harness/echelon_result_schema.py` rejects non-boolean raw `quality_scores.pass`; `src/harness/condition_evaluator.py` no longer applies Python truthiness. SAGE/WHY2 prompt examples use `pass_id` for iteration labels. Regression coverage includes string-pass routing, schema rejection, evaluator semantics, and prompt contract tests. |

## Next EGR Backlog

| Priority | Recommendation | Why it matters | Suggested files/modules to change | Expected impact |
|---|---|---|---|---|
| P3 | EGR-009: Integrate external RCA pipeline from source. | Adds incident/RCA capability without inventing duplicate behavior. | Future RCA integration adapter, workflow namespace, docs/tests after source is available | Source-grounded RCA flow tied into Echelon. |

## Review Notes

| Date | Reviewed HEAD | Notes |
|---|---|---|
| 2026-06-29 | `working tree on main` | Added EGR-043 through EGR-046 from `/tmp/echelon.harness.run.3`: missing Claude custom-agent registration for verify-spec build agents, stale task-progress bookkeeping despite implemented code/tests, placeholder constitution allowed into harness build, and CodeGraph term-match confidence overreach. |
| 2026-06-29 | `working tree on main` | EGR-043 through EGR-046 implemented: synced runtime agents now get Claude custom-agent frontmatter; reported completed task IDs that cannot be reconciled to `tasks.md` hard-block; Phase A readiness requires a non-placeholder published constitution snapshot; CodeGraph term-match-only source/test rows remain low-confidence fallback evidence. Verification: focused affected slice passed with 194 tests; full `pytest -q` passed with 2530 tests and 22 skipped. |
| 2026-06-29 | `working tree on main` | EGR-047 implemented: `scripts/bash/dry-run.sh` now accepts both repo-root and extension-root invocations, validates the modern thin-wrapper/COMMANDER contract, and no longer fails on stale root assumptions. Verification: `bash scripts/bash/dry-run.sh`, `bash scripts/bash/dry-run.sh extension`, and `pytest tests/unit/test_dry_run_script.py -q` passed. |
| 2026-06-29 | `992c0bf` | Added EGR-048 and EGR-049 from `/tmp/echelon.harness.run.4`: terminal `echelon harness run` still allows placeholder constitution into build dispatch, and provider session-limit failures need first-class blocked UX with salvage/retry guidance. GitHub issues #69 and #70 created. |
| 2026-06-29 | `992c0bf` | Added EGR-050 after focused constitution review: Echelon needs one robust constitution contract across spec-kit canonical memory, published spec snapshots, CHIEF ownership, build/codegen consumption, and amendment-candidate flow. GitHub issue #71 created. |
| 2026-06-29 | `working tree on main` | EGR-048 implemented: `echelon harness run` now blocks on incomplete or placeholder Phase A build inputs before LLM build dispatch, including parent polyrepo dispatch. `build-1-init.md` now treats `constitution.md` as a preflight-validated read-only snapshot and no longer copies it from `.specify/memory`. Verification: focused CLI/readiness/static tests passed. |
| 2026-06-29 | `working tree on main` | EGR-050 implemented: constitution ownership is now CHIEF/spec-kit only; non-CHIEF phases consume read-only snapshots or emit amendment candidates; codegen/finalize stop on missing/template snapshots; the journal registry removed COMMANDER placeholder repair; static tests enforce the contract. Verification: focused static, CHIEF, artifact-index, amendment, prompt-reference, and JSON freshness checks passed. |
| 2026-06-30 | `working tree on main` | EGR-049 implemented: provider session-limit failures now persist reset/provider details and render dedicated blocked UX across Ralph, `echelon status`, and harness delivery summary. Verification: focused provider-limit regression slice passed; full `pytest -q` passed. |
| 2026-06-30 | `working tree on main` | EGR-051 implemented: `echelon harness resume` recovery now handles untracked file collisions before cherry-pick, converting identical duplicates into tracked recovered files and backing up differing local copies. Verification: focused harness recovery tests passed; full `pytest -q` passed with 2539 tests and 22 skipped. |
| 2026-06-30 | `working tree on main` | Added EGR-052 after focused constitution context review: Phase A now has a strong constitution source-of-truth and build-readiness contract, but CARTOGRAPHER/ORCHESTRATOR/PLAN2 context packs and ARCHITECT prompt wording still need alignment so constitution is consistently available as read-only governance during spec and planning work. GitHub issue #73 created. |
| 2026-06-30 | `working tree on main` | EGR-052 implemented: constitution context is now passed to CARTOGRAPHER, ORCHESTRATOR PLAN, SAGE WHY3, and ORCHESTRATOR PLAN2 as read-only governance; ARCHITECT prompt wording now escalates missing/template constitution instead of invoking `speckit.constitution`; static contract tests cover the context-pack and prompt boundary. Verification: focused static contract, workflow validator, phase graph, JSON freshness, and diff checks passed. |
| 2026-06-30 | `1e0b7e8` | Added EGR-053 for a controlled single-phase repair/replay command. Desired surface: `echelon phase list` and `echelon phase run <phase-id>`, generic across workflow phases, with deterministic context resolution from existing artifacts, target/canonical artifact publishing, no automatic transition, and preserved COMMANDER state/journal ownership. GitHub issue #76 created. |
| 2026-06-30 | `working tree on main` | EGR-053 implemented: `echelon phase list` and `echelon phase run <phase-id> [--spec <id>]` now support targeted phase repair/replay through the normal squad executor and state/journal contracts. Manual runs record `last_dispatch.manual_phase_run` and `manual_phase_runs[]`, target project-visible spec directories when resolvable, refresh published artifact indexes/constitution snapshots, and stop after one phase. Verification: focused CLI/state/phase-graph/controller tests passed. |
| 2026-06-30 | `workspace-source-roots-design` | Added EGR-054 for the workspace/source-root model branch and associated it with GitHub issue #70. EGR-054 implementation includes the Git-backed workspace model, source-root target resolution, RE workspace manifests, harness metadata propagation, branchless-workspace blocking with legacy recovery, and one-time lightweight workspace Git migration. Verification before this register update: focused workspace/harness regression set passed with 331 tests after rebasing onto main. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-055 after live harness recovery failure: `echelon harness resume` now treats repaired `harness_error` state as retryable only after deterministic artifact preflight and refreshes stale persisted spec artifact paths before dispatch. GitHub issue #77 created. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-056 after live harness retry still saw template constitution in the generated build worktree. Ralph now syncs current project-visible Phase A artifacts into the worktree and validates that copy before LLM build dispatch. GitHub issue #78 created. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-057 after Phase A readiness retry incorrectly entered git recovery and refused tracked constitution repair changes. `echelon harness resume` now skips recovery for Phase-A-only build-incomplete states with no recoverable work commit. GitHub issue #79 created. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-058 after reviewing `/tmp/echelon.harness.run.5`: Ralph applied outer build task progress but ignored inner-fix `completed_task_ids`, so useful T-002/T-005 progress was invisible and repeated full-spec fulfillment gaps escalated incorrectly. Inner feedback now preserves task IDs, updates task/state progress, checkpoints after progress, and exits the inner loop on sole remaining full-spec fulfillment gaps. GitHub issue #80 created. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-059 after live `echelon harness resume` rejected `blocker_escalation` and told the user to use `harness run` to resume. `blocker_escalation` now flows through `harness resume`; unsupported reasons no longer use contradictory run-to-resume wording. GitHub issue #81 created. |
| 2026-06-30 | `working tree on main` | Added and implemented EGR-060 after live missing-verify recovery loop: `harness init` declined verify auto-detection but still suggested harness run, and `harness resume` continued recommending another init. Missing-verify UX now uses detection metadata and prioritizes manual `verify_command` when detection already failed. GitHub issue #82 created. |
| 2026-07-01 | `working tree on main` | Added and implemented EGR-061 after `/tmp/echelon.harness.run.7` showed Claude Write/Bash permission blocks despite an approved top-level LLM tool policy. Harness config now inherits top-level `llm` into `harness.llm` as compatibility defaults, with nested harness values taking precedence. GitHub issue #83 created. |
| 2026-07-01 | `working tree on main` | Added and implemented EGR-062 after `/tmp/echelon.harness.run.8` showed provider session exhaustion during verify-spec after implementation progress had already been checkpointed. Fulfillment refresh now preserves stale-report provenance in the failure reason, and Ralph stops immediately as a provider-session-limit checkpoint instead of dispatching feedback work against an exhausted provider. GitHub issue #84 created. |
| 2026-07-01 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-064 after provider-limit resume still failed because recovery required a clean current checkout before noticing the checkpoint was already on the target feature branch. Recovery now treats target-branch-contained commits as already present and avoids checkout/cherry-pick cleanup. GitHub issue #86 created. |
| 2026-07-02 | `working tree on main` | Added EGR-065 through EGR-067 from `/tmp/echelon.harness.run.9`: provider-limit salvage can retain `tasks.md` progress mismatches after Phase A input sync, build quality gates can diverge from the declared sequence, and ruff-clean reporting can miss `ruff format --check` failures. |
| 2026-07-02 | `working tree on main` | EGR-065 implemented: Phase A input sync now reapplies completed task progress from Python-owned harness state before readiness validation, so stale project-visible `tasks.md` copies cannot erase checked rows in provider-limit salvage worktrees. Verification: focused Ralph task-progress regression slice passed. |
| 2026-07-02 | `working tree on main` | EGR-067 implemented: CODE REVIEWER's Python language rule now distinguishes `ruff check` from `ruff format --check` and the generic agent prompt remains language-neutral. Verification: `pytest tests/unit/test_code_reviewer_templates.py -q` passed. |
| 2026-07-02 | `working tree on main` | Added EGR-068 after re-reviewing `/tmp/echelon.harness.run.9` verify-spec output: CodeGraph indexed the codebase, but deterministic evidence confidence stayed entirely low because only term matches were used, coverage-map rows lacked concrete file/function anchors, call-graph edges were ignored, and retained artifacts showed stale iter-root paths. |
| 2026-07-03 | `codex/egr-080-tech-writer-documentation-gate` | Added EGR-080 for the previously planned but unfiled TECH WRITER documentation gate. GitHub issue #97 created; implementation moved to a dedicated branch separate from artifact-quality benchmark work. |
| 2026-07-03 | `codex/egr-080-tech-writer-documentation-gate` | EGR-080 implemented: added TECH WRITER, routed build finalization through a documentation phase, introduced `src/harness/documentation_gate.py`, and wired Ralph to enforce documentation impact reports before convergence. Verification captured in branch test runs. |
| 2026-07-03 | `codex/egr-081-dockerless-workspace-init` | Added and implemented EGR-081 after `echelon workspace init` failed with raw `docker: command not found` from the HTTP deploy script in a Dockerless workspace. Python init now writes `deploy.enabled: false` when HTTP deploy runtime is unavailable, skips deploy provisioning instead of failing workspace bootstrap, honors explicit `deploy.enabled: false`, and prints actionable Docker/Podman/deploy-type guidance. Also repaired stale EGR-080 routing/catalog expectations that kept full pytest red after TECH WRITER landed. Verification: full `pytest -q` passed with 2686 passed, 22 skipped. GitHub issue #104 created. |
| 2026-07-03 | `codex/egr-063-artifact-quality-benchmark` | EGR-063 implemented experimental artifact-quality benchmarking with constitution, tasks, and ADR cleanse variants. Verification: `pytest tests/unit/test_benchmark.py tests/unit/test_cli_phase.py tests/kernel/test_phase_graph.py tests/kernel/test_workflow_validator.py -q` passed. |
| 2026-07-03 | `codex/egr-082-benchmark-reset` | Added and implemented EGR-082 after benchmark protocol review showed cleanse variants need to reset to a committed Phase A baseline between runs. Verification: `python -m pytest tests/unit/test_benchmark.py -q` passed. GitHub issue #105 created. |
| 2026-07-04 | `working tree on main` | Added EGR-083 after reviewing `/tmp/banzai.1`: WHY2 emitted failing Understanding scores but persisted `quality_scores[0].pass` as the string `"WHY2-iter-0"`, and deterministic routing treated that non-empty string as truthy. Required fix: boolean-only quality pass normalization/validation at harness boundaries, deterministic derivation for missing/non-boolean WHY pass flags, regression coverage for string pass values, and SAGE prompt/schema cleanup so `pass` cannot be used as an identifier. |
| 2026-07-04 | `codex/egr-083-quality-pass-normalization` | EGR-083 implemented: WHY quality scores are normalized before executor validation, transition routing, and state advance; raw schema validation rejects non-boolean `quality_scores.pass`; the condition evaluator no longer treats strings as passing; SAGE/WHY2 prompts now use `pass_id` for iteration labels. Verification: focused evaluator/controller/schema/prompt slice passed. |
| 2026-07-02 | `working tree on main` | EGR-066 implemented: `echelon.build` and `build-2-implement.md` now explicitly require sequential SPEC GUARD → CODE REVIEWER → TEST GUARDIAN execution, forbid parallel batching of the three gates, and forbid vacuity skips without a workflow-approved skip condition plus journaled rationale. The generic COMMANDER prompt remains judge/governance-oriented. Verification: `pytest tests/unit/test_build_quality_gate_sequence.py tests/unit/test_registry_sync_pytest.py -q` passed. |
| 2026-07-02 | `working tree on main` | EGR-068 implemented: verify-spec CodeGraph refresh now rejects stale `repo_path` exports before summary generation and the evidence mapper lifts directly called implementation symbols from requirement-anchored tests via CodeGraph call edges. Pure term-match-only evidence remains low-confidence. Verification: `pytest tests/unit/test_harness_main_codegraph_evidence.py tests/unit/test_codegraph_evidence_mapper.py -q` passed. |
| 2026-07-02 | `working tree on main` | Added and implemented EGR-069 after live `/tmp/echelon.harness.run.10` failed before recovery because a preserved worktree with a valid salvage commit had later generated perf metric edits. Recovery now selects state-recorded checkpoint/salvage commits before enforcing the dirty-worktree guard. Verification: `pytest tests/unit/test_harness_recovery.py -q` passed. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-070 after `/tmp/echelon.harness.run.10` showed a converged, fully fulfilled feature branch blocked by `echelon land` because current-checkout spec metadata was stale. Land preflight now validates feature-branch snapshots when current artifacts would falsely block, while preserving dirty-tree protection for actual checkout/merge operations. GitHub issue #87 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-071 after live workspace status showed tracked `.specify/memory/constitution.md` fighting with spec-kit runtime state. Workspace Git initialization/migration now ignores `.specify/` and stages only `.gitignore` plus `specs/`; README documents the tracked constitution handoff as `specs/<id>-*/constitution.md`. GitHub issue #88 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-072 after `echelon land 001 --prepare-only` still required manual conflict resolution for `.gitignore` plus `.specify/memory/constitution.md`. Land now autoresolves known runtime-state conflicts in both fresh prepare and `--continue` recovery. GitHub issue #89 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added EGR-073 and drafted Workspace Contract v1 design after deciding that new Echelon workspaces need a strict scaffold/tracking/source-root contract instead of late free-form recovery. Canonical committed config target: `.echelon/config.yml`; `.specify/` remains ignored runtime state. GitHub issue #90 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Implemented the first EGR-073 loader slice: `load_config()` and `get_full_resolved_config()` now use canonical `.echelon/config.yml` before legacy `.specify/extensions/echelon/echelon-config.yml`; `.echelon/local.yml` is supported and ignored. Verification: `pytest tests/unit/test_config.py -q` passed. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Implemented the second EGR-073 slice: `echelon harness init` now writes `.echelon/config.yml` for new workspaces and keeps writing legacy `echelon-config.yml` only when that legacy file already exists without a canonical config. Verification: `pytest tests/unit/test_harness_init_verify.py tests/unit/test_harness_init_app_runtime.py tests/unit/test_cli_harness_init_summary.py tests/unit/test_config.py -q` passed. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Implemented the third EGR-073 slice: CLI harness run/resume preflights and project config compatibility checks now use the same canonical-first config selector, so `.echelon/config.yml` workspaces are not rejected as uninitialized. Verification: `pytest tests/unit/test_cli_harness_run.py tests/unit/test_cli_harness_resume.py tests/unit/test_cli_config_compatibility.py tests/unit/test_cli_harness_init_summary.py tests/unit/test_config.py -q` passed. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Completed EGR-073: workspace model now honors explicit `.echelon/config.yml` source roots before heuristic discovery; `echelon workspace doctor` validates Git/runtime/spec/source-root contract; `echelon workspace migrate` copies legacy config to `.echelon/config.yml`, ignores/untracks runtime state, and stages canonical workspace files; init/docs/prompts were updated to canonical-first config guidance. Verification: full `pytest -q` passed with 2659 passed, 22 skipped in 162.80s. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Fixed EGR-073 migration regression found in `codex_echelon_test`: legacy workspaces with broad `/.echelon/` in `.gitignore` caused `git add .echelon/config.yml` to fail, and reruns did not stage an already-created canonical config. Migration now appends unignore rules for `.echelon/config.yml`, ignores only local/runtime `.echelon` paths, stages existing canonical config after ignore repair, and doctor reports ignored canonical config. Verification: live `echelon workspace migrate --write` staged `.echelon/config.yml`; full `pytest -q` passed with 2662 passed, 22 skipped in 161.44s. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-074 after live `echelon land 001 --continue` failed in a local/no-origin repo after the merge commit had already succeeded. Land now skips prepared-branch pushes for absent/local origins and makes `--continue` idempotent when the feature branch is already prepared. GitHub issue #91 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-075 after live `echelon land 001 --continue` then failed on dirty generated perf metrics before direct merge. Land now discards allowlisted generated verification drift, blocks unsafe dirty files, and skips default-branch push for absent/local origins. GitHub issue #92 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-076 after local land merged successfully but still blocked status/local cleanup on deleting a nonexistent remote feature branch, then failed to recognize an already-merged default branch from the feature checkout. Landing cleanup now skips remote branch deletion for absent/local origins and completes cleanup from already-merged state. GitHub issue #93 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-077 after successful land still exited with an archive-prompt EOFError in noninteractive execution. Post-land archival now skips safely without changing the successful land result. GitHub issue #94 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-078 after post-migration workspace inspection found `.echelon/recovery-backups/` left unignored while doctor passed. Migration/doctor/docs now treat recovery backups as runtime state. GitHub issue #95 created. |
| 2026-07-02 | `codex/artifact-quality-benchmark-design` | Added and implemented EGR-079 after CLI taxonomy review. `delivery` is now the canonical Phase B namespace with compatibility aliases for `harness` and `land`; `echelon help` is first-class; README/help were grouped around workspace/spec/phase/delivery. GitHub issue #96 created. |
| 2026-06-23 | `eeb490899655c0796ec9d9c187eb52fe1195427f` | Initial grounded review register created from repository evidence. |
| 2026-06-23 | `176da20ace1d485545724467e3c757a7259e760f` | EGR-001 implemented with deterministic `echelon_result` validation in provider and state advance paths. Verification: `pytest tests/kernel -q` passed with 532 tests. |
| 2026-06-23 | `working tree on codex/egr-002-phase-a-readiness` | EGR-002 implemented shared deterministic Phase A readiness validation in CLI next-step/continue paths and the squad `phase4-document` finalization gate. |
| 2026-06-23 | `working tree on codex/egr-005-blocked-decisions` | EGR-005 implemented typed blocked decisions and resume metadata. |
| 2026-06-23 | `working tree on codex/egr-004-sandbox-suggestion` | EGR-004 implemented deterministic sandbox suggestion reports. |
| 2026-06-23 | `working tree on codex/egr-003-tool-boundaries` | EGR-003 implemented deterministic host-side LLM tool policy and fail-closed unsafe bypass. |
| 2026-06-23 | `working tree on codex/egr-006-repair-loop` | EGR-006 introduced `harness.repair_loop`. |
| 2026-06-23 | `working tree on codex/egr-007-memory-validation` | EGR-007 introduced deterministic KB schema validation. |
| 2026-06-23 | `working tree on codex/egr-008-role-contracts` | EGR-008 introduced deterministic routed-role contract validation. |
| 2026-06-23 | `665c7acbd3a6a2fae60a617e39c4a1aa7abfd808` | Delta review after EGR-008 completed; EGR-010 and EGR-011 were promoted. |
| 2026-06-23 | `working tree on codex/egr-010-secret-scan` | EGR-010 implemented deterministic staged-file secret scanning in GitOps. |
| 2026-06-24 | `working tree on codex/egr-011-state-update-allowlists` | EGR-011 implemented per-phase `allowed_state_updates` declarations and enforcement. |
| 2026-06-24 | `34b4857d5f9aa1cb74c30cddae169e77b7552009` | Delta review after EGR-011 completed; EGR-012, EGR-013, and EGR-014 were promoted. |
| 2026-06-24 | `working tree on codex/egr-012-pre-dispatch-allowlist` | EGR-012 implemented pre-dispatch allowlist validation. |
| 2026-06-24 | `working tree on codex/egr-013-judgment-allowlist` | EGR-013 implemented COMMANDER judgment update validation. |
| 2026-06-24 | `working tree on codex/egr-014-allowed-updates-prompts` | EGR-014 disclosed allowed state-update keys in dispatch prompts. Verification included full `pytest` with 2318 passed and 22 skipped. |
| 2026-06-25 | `working tree on codex/egr-015-agent-executor-validation` | EGR-015 moved normal agent result validation before journal writes. Verification included full `pytest` with 2320 passed and 22 skipped plus `bash tests/run-all.sh` passing on retry. |
| 2026-06-26 | `f73d950700efe02c9ed1caae0bd359bb9d9a802f` | Full refreshed source review completed after recovery/consensus hardening. EGR-001 through EGR-015 remain fixed, EGR-009 remains accepted-risk, and EGR-016 through EGR-022 define the refreshed next backlog. Most recent full suite before this review: `pytest` passed with 2366 passed and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-016 implemented deterministic workflow-definition validation and dry-run preflight wiring. Verification: `pytest tests/kernel/test_workflow_validator.py tests/kernel/test_phase_graph.py -q` passed with 35 tests; direct validator reported `workflow definition valid`; `bash -n scripts/bash/dry-run.sh` passed. |
| 2026-06-26 | `working tree on main` | EGR-017 fixed README/tool-policy documentation drift and added a public-doc regression test. Verification: `pytest tests/unit/test_readme_tool_policy_docs.py tests/unit/test_llm_tool_policy.py tests/unit/test_config.py -q` passed with 19 tests; `rg -n "claude -p <prompt> --dangerously-skip-permissions" README.md` returned no matches. |
| 2026-06-26 | `working tree on main` | EGR-018 added Python journal-entry validation at both write boundaries while preserving DR-001 warn-then-allow semantics. Verification: `pytest tests/unit/test_journal_entry_validator.py tests/kernel/test_squad_executors_journal.py tests/integration/test_journal_append_helper.py -q` passed with 58 tests; `pytest tests/kernel -q` passed with 572 tests. |
| 2026-06-26 | `working tree on main` | Post-EGR-018 tightening review concluded warn-then-allow should be transitional, not the end state. Added EGR-023 to track schema-complete prompt migration, static prompt/example validation, and eventual strict handling for invalid registered journal entries. Evidence: 114 `journal_entries` mentions across agent/phase prompts and existing sparse examples in tests/prompts. |
| 2026-06-26 | `working tree on main` | EGR-023 implemented Python writer quarantine mode for invalid registered journal entries and tightened the canonical `echelon_result` journal-entry template. Follow-up EGR-024 tracks static validation and scattered prompt/example migration. Verification: `pytest tests/unit/test_journal_entry_validator.py tests/kernel/test_squad_executors_journal.py tests/integration/test_journal_append_helper.py -q` passed with 60 tests; `bash tests/unit/test-json-freshness.sh` passed; `pytest tests/kernel -q` passed with 573 tests. |
| 2026-06-26 | `working tree on main` | EGR-024 added static validation for concrete prompt journal-entry examples, registered durable phase/constitution event types, and migrated examples to schema-complete `data` payloads. Verification: `pytest tests/unit/test_journal_prompt_validator.py tests/kernel/test_prompt_references.py -q` passed with 46 tests; `bash tests/unit/test-json-freshness.sh` passed; `pytest tests/kernel -q` passed with 574 tests. |
| 2026-06-26 | `working tree on main` | EGR-021 added deterministic installed-extension drift detection for terminal CLI commands and documented the update path. Follow-up tightened source resolution so packaged installs do not guess machine-local checkout paths; drift warnings require `ECHELON_EXTENSION_SOURCE`, installed source metadata, or a verified editable checkout. Verification: `pytest tests/unit/test_extension_drift.py tests/unit/test_cli_status.py tests/unit/test_readme_tool_policy_docs.py -q` passed with 19 tests; `pytest tests/kernel -q` passed with 574 tests; `pytest -q` passed with 2408 tests and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-020 reconciled the public role-count narrative with the source tree: 53 registered agent roles, 45 active-routed manifest roles, 8 manifest-only roles, 1 workflow-only alias, and 15 support prompt files. Verification: `pytest tests/kernel/test_agent_role_catalog_docs.py tests/unit/test_readme_tool_policy_docs.py -q` passed with 2 tests; `bash tests/test-unit-registry-sync.sh` passed; `pytest tests/kernel -q` passed with 575 tests; `pytest -q` passed with 2411 tests and 22 skipped. |
| 2026-06-26 | `466e8089a40d279b25915e00f1932be1594a28d5` | Live `echelon continue` transcript review found a Phase 2 tracker routing contract mismatch: workflow transitions read `alignment`, while TRACKER emitted `verdict`. Fix committed separately; added EGR-025 through EGR-027 to track broader prevention for workflow condition fields, verdict contract duplication, and continuation recovery from already-persisted bad states. |
| 2026-06-26 | `working tree on main` | EGR-019 adopted `RepairLoop` in the coordinator-owned Phase 3 review-fix/re-entry cycle. Verification: `pytest tests/unit/test_coordinator_review_reentry.py tests/unit/test_review_loop.py tests/unit/test_repair_loop.py -q` passed with 11 tests. |
| 2026-06-26 | `working tree on main` | EGR-022 migration step: moved the no-new-dependencies repository-policy check from `tests/unit/test-no-new-deps.sh` into pytest via `tests/contract/no_new_deps.py` and `tests/unit/test_no_new_deps_pytest.py`; updated `tests/README.md` so pytest is the primary test path. |
| 2026-06-26 | `working tree on main` | EGR-022 migration step: moved the extension registry sync contract from `tests/test-unit-registry-sync.sh` into pytest via `tests/contract/registry_sync.py` and `tests/unit/test_registry_sync_pytest.py`. Verification before deletion: original shell test passed with 6 checks; migrated pytest passed with 7 tests. |
| 2026-06-26 | `working tree on main` | EGR-025 tightened workflow transition validation so syntactically valid but unresolvable condition fields fail preflight validation. Verification: `pytest tests/kernel/test_workflow_validator.py -q` passed with 23 tests; `pytest tests/kernel/test_workflow_validator.py tests/kernel/test_phase_graph.py -q` passed with 38 tests; `pytest tests/kernel -q` passed with 578 tests; `pytest -q` passed with 2428 tests and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-026 added static verdict-contract validation and migrated the stale tracker repair hint to canonical `ALIGNED | DRIFT | STOP_AND_ASK`. Verification: `pytest tests/kernel/test_verdict_contract_validator.py -q` passed with 3 tests; `pytest tests/kernel/test_verdict_contract_validator.py tests/kernel/test_prompt_references.py -q` passed with 47 tests; `pytest tests/kernel -q` passed with 582 tests; `pytest -q` passed with 2432 tests and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-027 hardened `echelon continue` for already-affected tracker fallthrough state: completed tracker alignment plus missing specialist phase now resumes `phase3-specialists` before HOW artifact repair. Verification: `pytest tests/unit/test_cli_continue.py -q` passed with 14 tests; `pytest tests/unit/test_cli_continue.py tests/unit/test_cli_next_step_escalation.py tests/unit/test_run_readiness.py -q` passed with 30 tests; `pytest tests/unit -q` passed with 1521 tests; `pytest -q` passed with 2434 tests and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-022 migration step: moved the language-rule file contract from `tests/unit/test-language-rules-exist.sh` into pytest via `tests/contract/language_rules.py` and `tests/unit/test_language_rules_pytest.py`. Verification before deletion: original shell test passed; migrated pytest passed with 1 test; migrated EGR-022 pytest set passed with 10 tests; `pytest tests/unit -q` passed with 1522 tests; `pytest -q` passed with 2435 tests and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-022 completed by moving remaining deterministic static contract shell checks into `tests/contract/static_contracts.py` and `tests/unit/test_static_contracts_pytest.py`, deleting 14 shell tests after equivalence checks passed, and wiring the migrated contracts into `tests/run-all.sh`. Migration exposed separate GUARDIAN config naming drift, tracked as EGR-028. Verification: original migrated shell checks passed before deletion; `pytest tests/unit/test_no_new_deps_pytest.py tests/unit/test_registry_sync_pytest.py tests/unit/test_language_rules_pytest.py tests/unit/test_static_contracts_pytest.py -q` passed with 24 tests; `pytest tests/unit -q` passed with 1536 tests; `pytest -q` passed with 2449 passed and 22 skipped. |
| 2026-06-26 | `working tree on main` | EGR-028 reconciled GUARDIAN config naming around `specialists.guardian_mode` in README, phase dispatch prompt, and GUARDIAN prompt, with static pytest coverage rejecting deprecated `guardian.mode` in public sources. Verification: `pytest tests/unit/test_static_contracts_pytest.py -q` passed with 15 tests; `pytest tests/kernel/test_accessors.py tests/kernel/test_evaluator_predicates.py tests/kernel/test_workflow_validator.py -q` passed with 146 tests; `pytest tests/unit -q` passed with 1537 tests; `pytest -q` passed with 2450 passed and 22 skipped. |
| 2026-06-26 | `working tree on main` | Live repaired-run review after EGR-027 found `phase3-how` and `phase3-plan` produced `plan.md` and `tasks.md` under the run-local spec dir, but `phase4-document` was a harness no-op and the project-visible `specs/001-themed-ascii-animation` remained stale. Added EGR-029 to fix publishing/readiness semantics. |
| 2026-06-26 | `working tree on main` | EGR-029 implemented: Phase 4 now publishes active run-local Phase A artifacts to the project-visible spec directory, writes `ARTIFACTS.md`, records `published_spec_dir`, validates the published build target, and routes old done-but-unpublished runs back through `phase4-document`. Verification: targeted integration and CLI continue tests passed. |
| 2026-06-27 | `working tree on main` | Live spec comparison found Lexicon Gate changed `spec.md` from the standard rich spec-kit feature specification into controlled grammar (`ARTIFACT: SPEC` / `REQ:` blocks), removing feature metadata, user stories, acceptance-scenario sections, FR/NFR headings, entities, and success criteria from `spec.md`. Added `docs/pipeline-matrix.md` and EGR-030 to track the required contract reconciliation. |
| 2026-06-27 | `working tree on main` | EGR-030 implemented: Lexicon Gate now preserves rich `spec.md` as the canonical Phase A contract and derives `requirements.lexicon.md` for controlled-grammar validation. ORCHESTRATOR validates tasks against configured `spec_ref`; config, prompts, docs, and static/task config tests encode the dual-artifact contract. |
| 2026-06-27 | `working tree on main` | Filed EGR-033 after reviewing the post-EGR-030 artifact relationship. The intended simple model is `spec.md` as the semantic source of truth and `requirements.lexicon.md` as a compiled validation/index artifact with deterministic freshness and ID-projection checks, not a first-class agent context-pack replacement. |
| 2026-06-27 | `07279388913057066329ccdf2285d1fa821e7987` | Delta review of PR #19 and PR #21 completed after fetching `origin/main`. Added EGR-031 for hardcoded `echelon status` roadmap drift from `extension/workflow/definition.yaml`, and EGR-032 for RUNNABLE phase wording that claims boot/render verification while the implementation only performs static composition evidence checks. Verification of incoming tests: `pytest tests/unit/test_runnable_gate.py tests/unit/test_verification_manifest.py tests/unit/test_status_roadmap.py` passed with 22 tests. |
| 2026-06-27 | `working tree on main` | EGR-031 implemented: `echelon status` now derives the squad roadmap from the primary forward path in `workflow/definition.yaml`, includes the Phase A nodes added by the externalized graph, and keeps re-dispatched completed phases in the progress numerator. Verification: `pytest tests/unit/test_status_roadmap.py -q` and `pytest tests/unit/test_cli_status.py -q` passed. |
| 2026-06-27 | `working tree on main` | EGR-032 implemented: codegen RUNNABLE now describes the implemented evidence boundary as build plus static composition, not runtime boot/render proof. Verification: `pytest tests/unit/test_runnable_gate.py tests/unit/test_runnable_gate_integration.py tests/unit/test_verification_manifest.py -q` and `pytest tests/unit/test_runnable_contract.py -q` passed. |
| 2026-06-27 | `working tree on main` | EGR-033 implemented: `requirements.lexicon.md` is now a deterministic derived artifact in `ARTIFACTS.md`, `lexicon validate --source-ref spec.md` rejects stale hashes and invented/missing REQ/AC/ERROR IDs, and CARTOGRAPHER/phase docs pass the canonical `spec.md` source explicitly. Verification: focused artifact-index, Lexicon CLI/parser/validity, and static contract tests passed. |
| 2026-06-27 | `e7656ae2cba07674a72ba5fbe29976ee0178705c` | Full refreshed source review completed after EGR-033. EGR-001 through EGR-033 remain fixed, EGR-009 remains accepted-risk, and EGR-034 was added for the RE CodeGraph vendored dependency provenance/version/integrity contract. |
| 2026-06-27 | `working tree on main` | EGR-034 implemented: the vendored RE CodeGraph runtime package now has a machine-readable vendor manifest, provenance note, deterministic payload hash, npm tarball integrity, license evidence, update procedure, and explicit optional-global-CLI split. Verification: `pytest tests/kernel/test_codegraph_integration_contract.py -q` passed with 11 tests. |
| 2026-06-27 | `working tree on main` | Added EGR-035 for project context and memory reconciliation: finalized canonical specs must become safe durable context, WIP artifacts remain run-local, MemPalace retrieval must be reconciled against artifact hashes/lifecycle metadata, and GOLDDIGGER Mode 2 queue processing must become executable. Design and implementation plan were drafted before code changes. |
| 2026-06-27 | `working tree on main` | EGR-035 implemented project context and memory reconciliation. Verification: context metadata/reconciliation/builder tests passed; MemPalace writer/miner regression tests passed; GOLDDIGGER Mode 2 queue tests passed; focused workflow/context tests passed. |
| 2026-06-27 | `working tree on main` | EGR-036 implemented from live `echelon resume` output: CARTOGRAPHER now receives exact Understanding/Lexicon validation command contracts instead of discovering them from source during amendment. Verification: `pytest tests/unit/test_static_contracts_pytest.py::test_cartographer_tool_usage_contract -q` and `pytest tests/unit/test_static_contracts_pytest.py -q` passed. |
| 2026-06-27 | `working tree on main` | Added EGR-037 as the broader follow-up from the EGR-036 regression: prompt-level tool use is only safe where an exact command/skill/output contract is present and statically tested. The next step is a repository-wide scanner for uncontracted tool references in agent and phase prompts. |
| 2026-06-27 | `working tree on main` | EGR-038 implemented from live SAGE resume output: SAGE's Understanding follow-up appendix and prompt now use the indexed enhanced JSON path `.[0].behavioral_analysis.transitions`, empty-list fallback, and null-safe transition cells for SENTINEL handoff. Verification: focused static contract passed. |
| 2026-06-28 | `working tree on main` | EGR-037 implemented: added a prompt tool-contract scanner for agent and phase prompts, fixed the remaining ambiguous executable-tool references it found, and verified the scanner against synthetic and repository prompts. Verification: focused prompt/static/registry pytest set passed. |
| 2026-06-28 | `working tree on main` | Added EGR-039 from live resume transcript: checkpoint-assess result was valid, but a later SAGE WHY2 missing `echelon_result` after resume degraded to manual recovery instead of preserving the retryable failed phase. |
| 2026-06-28 | `working tree on main` | EGR-039 implemented: retryable dispatch-block recovery now preserves the latest `last_dispatch.phase_id` even when a previous pass of that phase is listed in `completed_phases`. Verification: `pytest tests/unit/test_cli_next_step_escalation.py tests/unit/test_cli_continue.py tests/unit/test_registry_sync_pytest.py -q` passed with 33 tests. |
| 2026-06-28 | `b0aa12a847efca83793c6b7ed7f489e0e8254954` | Full refreshed source review completed after EGR-039. EGR-001 through EGR-039 remain fixed, EGR-009 remains accepted-risk, and no new P0/P1/P2 EGR was promoted. Verification: `pytest tests/unit/test_cli_next_step_escalation.py tests/unit/test_cli_continue.py tests/unit/test_prompt_tool_contracts.py tests/unit/test_static_contracts_pytest.py tests/unit/test_registry_sync_pytest.py tests/kernel/test_squad_executors_journal.py tests/kernel/test_workflow_validator.py -q` passed with 137 tests. |
| 2026-06-29 | `working tree on main` | Added EGR-040 from `/tmp/echelon.out`: the run's project runtime config exposed stale `tasks.spec_ref: spec.md`, causing ORCHESTRATOR to bypass the configured value and validate tasks against `requirements.lexicon.md` by reasoning. Current source defaults are correct, so the tracked gap is migration/runtime validation for existing project configs. |
| 2026-06-29 | `working tree on main` | EGR-040 implemented: project config compatibility check now warns in `echelon status` and blocks `echelon run`/`echelon resume` before agent dispatch when the Lexicon tasks gate points at stale `spec.md` instead of the derived requirements artifact. Verification: `pytest tests/unit/test_cli_config_compatibility.py tests/unit/test_cli_status.py tests/unit/test_tasks_config.py tests/contract/static_contracts.py -q` passed with 17 tests. |
| 2026-06-29 | `working tree on main` | Added EGR-041 and EGR-042 from attached `/tmp/echelon.harness.run.2` fragment: full verify-spec cache reused stale fulfillment evidence after uncommitted build changes, and the repeated-failure blocker banner contradicted its own count while showing slash-command resume guidance in terminal output. |
| 2026-06-29 | `working tree on main` | EGR-041 implemented: full verify-spec cache keys now include implementation-input content, so uncommitted source/test/build-manifest changes invalidate stale fulfillment reports before Ralph applies the fulfillment gate. Verification: `pytest tests/unit/test_fulfillment_runner.py -q` passed with 17 tests; focused Ralph cached-refresh tests passed with 2 tests. |
| 2026-06-29 | `working tree on main` | EGR-042 implemented: repeated-failure escalation context now uses the actual consecutive streak instead of distinct fingerprint count, and terminal harness escalation guidance points to `echelon harness resume <spec_id>` after appending `## Answer`. Verification: `pytest tests/unit/test_fulfillment_runner.py tests/unit/test_escalation.py tests/unit/test_terminal_banner.py tests/unit/test_ralph_inner.py::TestSameFailureEscalation tests/unit/test_coordinator.py::TestStickyEscalationBlock -q` passed with 41 tests. |
| 2026-07-01 | `codex/artifact-quality-benchmark-design` | Added EGR-063 for benchmark-backed artifact cleansing across constitution, tasks, and ADRs. Design was approved before implementation planning; GitHub issue #85 tracks the implementation. |
