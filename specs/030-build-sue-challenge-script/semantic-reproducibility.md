# Semantic Reproducibility Report

- **Specification:** /Users/ladislavbihari/myWork/echelon/specs/030-build-sue-challenge-script/spec.md
- **Run date:** 2026-07-19
- **Readers:** 2 completed (1 dropped: R3(adversarial))
- **Semantic reproducibility:** 0.247 — fractured
- **Witnesses (behavioural incompatibilities):** 14
- **Ungrounded edges dropped:** R1=0, R2=0

## Per-requirement agreement (worst first)

| Requirement | Score | Readers | Mean edges | Assumption load | Near-misses |
|---|---|---|---|---|---|
| A-003 | 0.00 | 2 | 2.0 | 1.0 | 2 |
| A-004 | 0.00 | 2 | 2.0 | 1.0 | 1 |
| A-006 | 0.00 | 2 | 2.0 | 0.5 | 0 |
| A-007 | 0.00 | 2 | 2.0 | 1.0 | 1 |
| A-008 | 0.00 | 2 | 1.0 | 1.0 | 1 |
| A-009 | 0.00 | 2 | 2.0 | 0.5 | 2 |
| AC-001 | 0.00 | 2 | 4.0 | 0.0 | 1 |
| AC-002 | 0.00 | 2 | 1.5 | 0.0 | 0 |
| AC-003 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| AC-005 | 0.00 | 2 | 2.5 | 0.0 | 1 |
| AC-007 | 0.00 | 2 | 3.5 | 0.0 | 2 |
| AC-008 | 0.00 | 2 | 2.0 | 0.0 | 0 |
| AC-009 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| AC-010 | 0.00 | 2 | 1.5 | 0.0 | 1 |
| AC-011 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| AC-012 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| AC-013 | 0.00 | 2 | 2.5 | 0.0 | 1 |
| AC-014 | 0.00 | 2 | 3.0 | 0.0 | 0 |
| AC-015 | 0.00 | 2 | 3.0 | 0.0 | 1 |
| AC-019 | 0.00 | 2 | 2.5 | 0.0 | 0 |
| AC-020 | 0.00 | 2 | 3.0 | 0.0 | 1 |
| AC-021 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| AC-022 | 0.00 | 2 | 1.5 | 0.0 | 0 |
| FR-001 | 0.00 | 2 | 2.5 | 0.5 | 2 |
| FR-004 | 0.00 | 2 | 3.5 | 0.0 | 3 |
| FR-009 | 0.00 | 2 | 2.0 | 0.0 | 2 |
| FR-016 | 0.00 | 2 | 4.5 | 1.0 | 4 |
| FR-018 | 0.00 | 2 | 3.5 | 0.0 | 3 |
| FR-023 | 0.00 | 2 | 2.5 | 0.0 | 2 |
| FR-029 | 0.00 | 2 | 2.0 | 0.5 | 0 |
| FR-037 | 0.00 | 2 | 1.0 | 0.0 | 1 |
| FR-038 | 0.00 | 2 | 2.0 | 0.5 | 2 |
| FR-041 | 0.00 | 2 | 3.5 | 0.0 | 1 |
| FR-042 | 0.00 | 2 | 2.0 | 0.0 | 1 |
| NFR-001 | 0.00 | 2 | 2.0 | 1.0 | 1 |
| NFR-003 | 0.00 | 2 | 1.0 | 0.0 | 1 |
| OQ-001 | 0.00 | 2 | 2.0 | 1.0 | 1 |
| OQ-002 | 0.00 | 2 | 2.0 | 1.0 | 1 |
| REQ-002 | 0.00 | 2 | 1.5 | 1.0 | 0 |
| REQ-009 | 0.00 | 2 | 1.5 | 1.0 | 0 |
| REQ-017 | 0.00 | 2 | 1.0 | 1.0 | 0 |
| REQ-019 | 0.00 | 2 | 1.0 | 1.0 | 0 |
| REQ-028 | 0.00 | 2 | 1.0 | 1.0 | 0 |
| SC-002 | 0.00 | 2 | 1.5 | 0.0 | 1 |
| SC-003 | 0.00 | 2 | 1.5 | 0.0 | 1 |
| SC-004 | 0.00 | 2 | 1.5 | 0.0 | 0 |
| SC-005 | 0.00 | 2 | 2.0 | 0.0 | 0 |
| U-001 | 0.00 | 2 | 1.0 | 1.0 | 0 |
| U-002 | 0.00 | 2 | 1.0 | 1.0 | 0 |
| U-005 | 0.00 | 2 | 3.0 | 0.0 | 0 |
| U-006 | 0.00 | 2 | 2.0 | 0.0 | 0 |
| U-008 | 0.00 | 2 | 2.0 | 0.0 | 2 |
| U-009 | 0.00 | 2 | 2.0 | 0.5 | 1 |
| U-010 | 0.00 | 2 | 2.0 | 0.0 | 0 |
| FR-026 | 0.11 | 2 | 5.0 | 1.0 | 2 |
| FR-034 | 0.11 | 2 | 5.0 | 1.0 | 2 |
| FR-002 | 0.12 | 2 | 4.5 | 1.0 | 2 |
| FR-040 | 0.12 | 2 | 4.5 | 1.0 | 2 |
| FR-036 | 0.14 | 2 | 4.0 | 0.0 | 3 |
| AC-017 | 0.17 | 2 | 3.5 | 0.0 | 2 |
| FR-008 | 0.17 | 2 | 3.5 | 1.0 | 2 |
| FR-012 | 0.17 | 2 | 3.5 | 0.5 | 2 |
| FR-032 | 0.17 | 2 | 3.5 | 0.0 | 0 |
| AC-023 | 0.20 | 2 | 3.0 | 1.0 | 1 |
| ERR-002 | 0.20 | 2 | 3.0 | 0.0 | 1 |
| ERR-005 | 0.20 | 2 | 3.0 | 0.0 | 0 |
| FR-006 | 0.20 | 2 | 3.0 | 1.0 | 1 |
| FR-011 | 0.20 | 2 | 3.0 | 0.5 | 1 |
| FR-024 | 0.20 | 2 | 3.0 | 0.0 | 2 |
| FR-043 | 0.20 | 2 | 3.0 | 1.0 | 2 |
| U-003 | 0.20 | 2 | 3.0 | 0.0 | 0 |
| U-007 | 0.20 | 2 | 3.0 | 0.0 | 0 |
| AC-006 | 0.25 | 2 | 2.5 | 0.0 | 1 |
| FR-003 | 0.25 | 2 | 2.5 | 0.0 | 1 |
| FR-007 | 0.25 | 2 | 2.5 | 0.5 | 1 |
| FR-044 | 0.25 | 2 | 2.5 | 1.0 | 1 |
| SC-001 | 0.25 | 2 | 2.5 | 0.5 | 1 |
| A-001 | 0.33 | 2 | 2.0 | 1.0 | 1 |
| A-002 | 0.33 | 2 | 2.0 | 1.0 | 1 |
| A-005 | 0.33 | 2 | 2.0 | 1.0 | 1 |
| AC-018 | 0.33 | 2 | 2.0 | 0.0 | 1 |
| FR-022 | 0.33 | 2 | 2.0 | 1.0 | 1 |
| FR-027 | 0.33 | 2 | 2.0 | 0.0 | 1 |
| FR-028 | 0.33 | 2 | 4.0 | 0.0 | 2 |
| FR-030 | 0.33 | 2 | 6.0 | 1.5 | 1 |
| U-004 | 0.33 | 2 | 2.0 | 0.0 | 0 |
| FR-039 | 0.40 | 2 | 3.5 | 1.0 | 0 |
| A-012 | 0.50 | 2 | 1.5 | 0.5 | 0 |
| ERR-001 | 0.50 | 2 | 3.0 | 0.0 | 0 |
| FR-005 | 0.50 | 2 | 3.0 | 1.0 | 0 |
| FR-010 | 0.50 | 2 | 4.5 | 1.0 | 0 |
| FR-015 | 0.50 | 2 | 3.0 | 0.0 | 0 |
| FR-017 | 0.50 | 2 | 3.0 | 0.0 | 1 |
| FR-020 | 0.50 | 2 | 3.0 | 0.5 | 1 |
| FR-025 | 0.50 | 2 | 3.0 | 0.0 | 0 |
| FR-013 | 0.60 | 2 | 4.0 | 0.5 | 0 |
| FR-019 | 0.60 | 2 | 4.0 | 0.0 | 1 |
| ERR-004 | 0.75 | 2 | 3.5 | 0.5 | 0 |
| A-010 | 1.00 | 2 | 2.0 | 0.5 | 0 |
| A-011 | 1.00 | 2 | 1.0 | 0.5 | 0 |
| AC-004 | 1.00 | 2 | 3.0 | 0.0 | 0 |
| AC-016 | 1.00 | 2 | 3.0 | 0.0 | 0 |
| ERR-003 | 1.00 | 2 | 3.0 | 0.0 | 0 |
| FR-014 | 1.00 | 2 | 3.0 | 0.0 | 0 |
| FR-021 | 1.00 | 2 | 3.0 | 0.0 | 0 |
| FR-031 | 1.00 | 2 | 2.0 | 0.0 | 0 |
| FR-033 | 1.00 | 2 | 2.0 | 0.5 | 0 |
| FR-035 | 1.00 | 2 | 1.0 | 0.0 | 0 |
| FR-045 | 1.00 | 2 | 2.0 | 1.0 | 0 |
| NFR-002 | 1.00 | 2 | 3.0 | 1.0 | 0 |
| NFR-004 | 1.00 | 2 | 2.0 | 0.5 | 0 |
| NFR-005 | 1.00 | 2 | 2.0 | 0.0 | 0 |

## Divergence witnesses

### W1. AC-001 — given "a readable specification and available model command", when "the operator runs the script"

- **Reader R1 then:** two model calls occur, the report lands beside the specification, and exit code is 0
  > line 25: - **AC-001**: Given a readable specification with an available model command, when the operator runs the challenge script, then exactly 2 model calls occur (FR-008), the challenge report is written into the specification's directory (FR-034), with exit code 0.
- **Reader R2 then:** exactly 2 model calls occur, the report lands in the specification's directory, and exit code is 0
  > line 25: - **AC-001**: Given a readable specification with an available model command, when the operator runs the challenge script, then exactly 2 model calls occur (FR-008), the challenge report is written into the specification's directory (FR-034), with exit code 0.

### W2. AC-010 — given "any challenge run", when "it finishes with any exit code"

- **Reader R1 then:** the challenged specification content is unchanged
  > line 47: - **AC-010**: Given any challenge run, when the run finishes with any exit code, then the challenged specification file received exactly 0 writes, leaving its content unchanged (FR-042, FR-001).
- **Reader R2 then:** the challenged specification received 0 writes
  > line 47: - **AC-010**: Given any challenge run, when the run finishes with any exit code, then the challenged specification file received exactly 0 writes, leaving its content unchanged (FR-042, FR-001).

### W3. AC-011 — given "a prompt-recording stub", when "round 2 executes"

- **Reader R1 then:** the prompt holds two content blocks and zero round-1 metadata elements
  > line 48: - **AC-011**: Given a stub model command that records its prompt, when round 2 executes, then the recorded prompt holds exactly 2 content blocks — specification text plus question identifiers with texts (FR-021) — with exactly 0 round-1 categories, targets, line tags, or reasoning (FR-022).
- **Reader R2 then:** the recorded prompt has exactly the 2 blocks and none of the 4 round-1 elements
  > line 48: - **AC-011**: Given a stub model command that records its prompt, when round 2 executes, then the recorded prompt holds exactly 2 content blocks — specification text plus question identifiers with texts (FR-021) — with exactly 0 round-1 categories, targets, line tags, or reasoning (FR-022).

### W4. AC-012 — given "a directory-recording stub", when "either round executes"

- **Reader R1 then:** the recorded directory is a fresh temp directory distinct from the specification and invoking directories
  > line 49: - **AC-012**: Given a stub model command that records its working directory, when either round executes, then the recorded directory is exactly 1 newly created temporary directory under the platform's default temporary root, distinct from the specification's directory and from the invoking working directory (FR-010, AC-011).
- **Reader R2 then:** the recorded directory is a fresh temp directory distinct from the spec directory and the invoking directory
  > line 49: - **AC-012**: Given a stub model command that records its working directory, when either round executes, then the recorded directory is exactly 1 newly created temporary directory under the platform's default temporary root, distinct from the specification's directory and from the invoking working directory (FR-010, AC-011).

### W5. AC-019 — given "an unwritable specification directory", when "the script runs"

- **Reader R1 then:** exit code 1 with zero model calls launched
  > line 69: - **AC-019**: Given the specification's directory is not writable, when the operator runs the challenge script, then the exit code is 1 with exactly 0 model calls launched (FR-006, ERR-002).
- **Reader R2 then:** exit 1 with 0 model calls launched
  > line 69: - **AC-019**: Given the specification's directory is not writable, when the operator runs the challenge script, then the exit code is 1 with exactly 0 model calls launched (FR-006, ERR-002).

### W6. SC-004 — given "any challenge run", when "it terminates"

- **Reader R1 then:** wall-clock time is at most four timeouts plus sixty seconds
  > line 271: - **SC-004**: When any challenge run terminates, its wall-clock time is at most 4 timeout budgets plus 60 seconds (NFR-001, FR-011).
- **Reader R2 then:** wall-clock time is within 4 timeouts plus 60 seconds
  > line 271: - **SC-004**: When any challenge run terminates, its wall-clock time is at most 4 timeout budgets plus 60 seconds (NFR-001, FR-011).

### W7. FR-006 — given "a read-only specification directory", when "the script runs"

- **Reader R1 then:** exit code is 1 with zero model calls launched
  > line 121: - **FR-006**: If the specification's directory is not writable for the report, the challenge script MUST exit with code 1 after launching exactly 0 model calls (ERR-002, AC-019).
- **Reader R2 then:** it exits with code 1 before any model call
  > line 121: - **FR-006**: If the specification's directory is not writable for the report, the challenge script MUST exit with code 1 after launching exactly 0 model calls (ERR-002, AC-019).
  > line 98: - The specification's directory is read-only → detected before any model call, exit code 1 (AC-019, FR-006).

### W8. FR-043 — given "a stub executable configured as the model command", when "a full run executes"

- **Reader R1 then:** the run completes end-to-end with zero live model calls
  > line 225: - **FR-043**: When the model-command option names any operator-supplied command line, the challenge script MUST execute that command in place of the default, enabling stubbed runs with exactly 0 live model calls (AC-021, FR-003).
  > line 84: - **AC-021**: Given a stub executable configured as the model command, when a full challenge run executes, then the run completes end-to-end (FR-043, FR-003) using exactly 0 live model calls.
- **Reader R2 then:** the run completes end-to-end with 0 live model calls
  > line 225: - **FR-043**: When the model-command option names any operator-supplied command line, the challenge script MUST execute that command in place of the default, enabling stubbed runs with exactly 0 live model calls (AC-021, FR-003).
  > line 84: - **AC-021**: Given a stub executable configured as the model command, when a full challenge run executes, then the run completes end-to-end (FR-043, FR-003) using exactly 0 live model calls.

### W9. FR-007 — given "a quoted multi-word model-command value", when "the option is parsed"

- **Reader R1 then:** the first word is treated as the executable checked by fr-012
  > line 123: - **FR-007**: When parsing the model-command option, the challenge script MUST split the value into words per POSIX shell quoting conventions (`shlex` semantics, platform-independent), treating exactly 1 leading word as the executable checked by FR-012 (FR-003).
- **Reader R2 then:** exactly the first word is treated as the executable to availability-check
  > line 123: - **FR-007**: When parsing the model-command option, the challenge script MUST split the value into words per POSIX shell quoting conventions (`shlex` semantics, platform-independent), treating exactly 1 leading word as the executable checked by FR-012 (FR-003).

### W10. FR-030 — given "a round failing validation on both attempts", when "the second failure occurs"

- **Reader R1 then:** exit code is 3 and both attempts' stdout and stderr are saved as separate files under .sue-debug
  > line 189: - **FR-030**: On the second parse failure in the same round, the challenge script MUST exit with code 3 after saving the raw output of the failing calls into exactly 1 directory named `.sue-debug` beside the specification (ERR-004, AC-015). For a timed-out call, the saved raw output is whatever partial output was drained within the shutdown grace period of exactly 5 seconds (subprocess ended by process-group kill; the up-to-4 grace periods are counted inside NFR-001's +60-second allowance), possibly empty. The debug dump itself is best-effort: if it cannot be written, the exit-3 outcome stands and its single diagnostic line names the failed dump (governs over ERR-004's save wording). Dump lifecycle: both failing attempts of the round are saved as separate files (`round{N}-attempt{1,2}-stdout/stderr`); successive failing runs overwrite files of the same names and the directory is never cleared by the script.
  > line 65: - **AC-015**: Given a round's output fails validation on both the initial call plus the corrective retry, when the second failure occurs, then the exit code is 3, the raw output is saved into the debug dump directory (FR-030, ERR-004), with 0 reports written.
- **Reader R2 then:** the run exits 3 with raw output saved to .sue-debug and no report written
  > line 189: - **FR-030**: On the second parse failure in the same round, the challenge script MUST exit with code 3 after saving the raw output of the failing calls into exactly 1 directory named `.sue-debug` beside the specification (ERR-004, AC-015). For a timed-out call, the saved raw output is whatever partial output was drained within the shutdown grace period of exactly 5 seconds (subprocess ended by process-group kill; the up-to-4 grace periods are counted inside NFR-001's +60-second allowance), possibly empty. The debug dump itself is best-effort: if it cannot be written, the exit-3 outcome stands and its single diagnostic line names the failed dump (governs over ERR-004's save wording). Dump lifecycle: both failing attempts of the round are saved as separate files (`round{N}-attempt{1,2}-stdout/stderr`); successive failing runs overwrite files of the same names and the directory is never cleared by the script.
  > line 65: - **AC-015**: Given a round's output fails validation on both the initial call plus the corrective retry, when the second failure occurs, then the exit code is 3, the raw output is saved into the debug dump directory (FR-030, ERR-004), with 0 reports written.

### W11. FR-017 — given "round-1 output with duplicate identifiers", when "validation runs"

- **Reader R1 then:** one of the round's two attempts is consumed
  > line 153: - **FR-017**: If round-1 validation fails, including on duplicate question identifiers, the challenge script MUST classify the output as a parse failure consuming 1 of the round's 2 permitted attempts (FR-016, FR-028).
  > line 95: - Round 1 returns duplicate question identifiers → validation failure, corrective retry path (FR-017).
- **Reader R2 then:** the output is a parse failure consuming one attempt
  > line 153: - **FR-017**: If round-1 validation fails, including on duplicate question identifiers, the challenge script MUST classify the output as a parse failure consuming 1 of the round's 2 permitted attempts (FR-016, FR-028).
  > line 95: - Round 1 returns duplicate question identifiers → validation failure, corrective retry path (FR-017).

### W12. FR-019 — given "valid round-1 output with more than n questions", when "validation runs"

- **Reader R1 then:** exactly the first n remain and the header carries one truncation note
  > line 157: - **FR-019**: If valid round-1 output contains more than N questions (FR-002, default 15), the challenge script MUST keep only the first N questions in returned order, recording a truncation note in the report header (FR-036). The kept N questions define both the round-2 prompt and the FR-025 identifier set.
  > line 70: - **AC-020**: Given valid round-1 output holding more than N questions, when validation runs, then exactly N questions remain — the first N in returned order (FR-019) — with the report header carrying 1 truncation note (FR-036).
- **Reader R2 then:** exactly the first N in returned order remain and the header carries a truncation note
  > line 157: - **FR-019**: If valid round-1 output contains more than N questions (FR-002, default 15), the challenge script MUST keep only the first N questions in returned order, recording a truncation note in the report header (FR-036). The kept N questions define both the round-2 prompt and the FR-025 identifier set.
  > line 70: - **AC-020**: Given valid round-1 output holding more than N questions, when validation runs, then exactly N questions remain — the first N in returned order (FR-019) — with the report header carrying 1 truncation note (FR-036).

### W13. FR-033 — given "findings of both classes", when "ranking runs"

- **Reader R1 then:** every contradicted finding precedes every unanswerable finding with round-1 order kept inside each class
  > line 200: - **FR-033**: When ranking findings, the challenge script MUST place all CONTRADICTED findings before all UNANSWERABLE findings, preserving round-1 question order within each of the 2 classes (FR-032, AC-004).
- **Reader R2 then:** every CONTRADICTED finding precedes every UNANSWERABLE finding, each class in round-1 order
  > line 200: - **FR-033**: When ranking findings, the challenge script MUST place all CONTRADICTED findings before all UNANSWERABLE findings, preserving round-1 question order within each of the 2 classes (FR-032, AC-004).

### W14. NFR-004 — given "identical validated answers", when "assembly runs twice"

- **Reader R1 then:** the two report bodies are byte-identical excluding the run-date field
  > line 248: - **NFR-004**: When identical validated answers are assembled twice, the challenge script MUST produce 2 report bodies identical outside the run-date field (FR-032, FR-036).
  > line 249:   - **Category:** Reliability | **Measurable Target:** byte-identical report bodies across 2 assembly passes, run-date field excluded
- **Reader R2 then:** the two report bodies are byte-identical excluding the run-date line
  > line 248: - **NFR-004**: When identical validated answers are assembled twice, the challenge script MUST produce 2 report bodies identical outside the run-date field (FR-032, FR-036).
  > line 249:   - **Category:** Reliability | **Measurable Target:** byte-identical report bodies across 2 assembly passes, run-date field excluded

## Fracture lines (attributed, not verified — v3.1 adds counterfactual check)

- **A-001:**
  - (2×) > line 325: | A-001 | The model command can be driven non-interactively with prompt in, extractable JSON out | unvalidated (OQ-001 spike before HOW) | FR-008, FR-026 |
- **A-002:**
  - (2×) > line 326: | A-002 | Neutral temporary working directory suffices for the isolation intent | unvalidated (OQ-002 spike before HOW); residual limitation documented | FR-010, Limitations |
- **A-003:**
  - (4×) > line 327: | A-003 | Standalone means standalone: no orchestration imports or configuration reads | unvalidated (review gate) | FR-045 |
- **A-004:**
  - (4×) > line 328: | A-004 | The acceptance target retains its three named known issues | validated at base commit ef2643c9; re-verify or freeze before the run | SC-001, AC-023 |
- **A-005:**
  - (2×) > line 329: | A-005 | Both rounds fit model context limits for realistic specifications | unvalidated; observed at acceptance | Limitations |
- **A-006:**
  - (4×) > line 330: | A-006 | The specification's directory is writable | converted into a pre-flight check | FR-006 |
- **A-007:**
  - (4×) > line 331: | A-007 | Timeout applies per subprocess invocation with a fresh budget on retry | adopted as specified behavior | FR-011, FR-013 |
- **A-008:**
  - (2×) > line 332: | A-008 | Unit tests follow existing repository test conventions | validated (conventions exist) | FR-044 |
- **A-009:**
  - (4×) > line 333: | A-009 | Strict JSON tolerates extraction from wrapped output | adopted as the extraction contract | FR-026 |
- **AC-001:**
  - (10×) > line 25: - **AC-001**: Given a readable specification with an available model command, when the operator runs the challenge script, then exactly 2 model calls occur (FR-008), the challenge report is written into the specification's directory (FR-034), with exit code 0.
- **AC-002:**
  - (3×) > line 26: - **AC-002**: Given a completed challenge run, when the operator opens the challenge report, then the report header states exactly 4 facts: the specification path, the run date, the question count, plus the finding count (FR-036, AC-001).
- **AC-003:**
  - (5×) > line 27: - **AC-003**: Given a challenge report exists from a previous run, when the operator reruns the challenge script, then exactly 1 report file remains (FR-034, AC-002), holding only the new run's content.
- **AC-005:**
  - (5×) > line 29: - **AC-005**: Given a run completes with at least 1 finding, when the run finishes, then the terminal summary states the finding count per verdict class, listing the top 3 findings in rank order (FR-040, AC-001).
- **AC-006:**
  - (3×) > line 30: - **AC-006**: Given round 1 returns a valid empty question list, when the run continues, then round 2 is skipped (FR-020), the report records exactly 0 questions with 0 findings (FR-036), with exit code 0.
- **AC-007:**
  - (7×) > line 31: - **AC-007**: Given every round-2 verdict is ANSWERED, when the report is assembled, then the findings section states that exactly 0 findings were produced (FR-041), with the audit appendix holding every question (FR-038), with exit code 0.
- **AC-008:**
  - (4×) > line 45: - **AC-008**: Given a question received an ANSWERED verdict, when the report is rendered, then that question appears in the audit appendix with its quoted answering lines, inside exactly 1 collapsed section the reader can expand (FR-038, FR-032).
- **AC-009:**
  - (5×) > line 46: - **AC-009**: Given a round-2 answer cites evidence line numbers, when the report renders that answer, then the report quotes exactly 1 line of text per cited number, as read from the specification file (FR-039, FR-018).
- **AC-010:**
  - (5×) > line 47: - **AC-010**: Given any challenge run, when the run finishes with any exit code, then the challenged specification file received exactly 0 writes, leaving its content unchanged (FR-042, FR-001).
- **AC-011:**
  - (7×) > line 48: - **AC-011**: Given a stub model command that records its prompt, when round 2 executes, then the recorded prompt holds exactly 2 content blocks — specification text plus question identifiers with texts (FR-021) — with exactly 0 round-1 categories, targets, line tags, or reasoning (FR-022).
- **AC-012:**
  - (7×) > line 49: - **AC-012**: Given a stub model command that records its working directory, when either round executes, then the recorded directory is exactly 1 newly created temporary directory under the platform's default temporary root, distinct from the specification's directory and from the invoking working directory (FR-010, AC-011).
- **AC-013:**
  - (5×) > line 63: - **AC-013**: Given a specification path that does not exist or cannot be read, when the operator runs the challenge script, then the exit code is 1 with exactly 0 model calls launched (FR-005, ERR-001).
- **AC-014:**
  - (6×) > line 64: - **AC-014**: Given the model command's executable cannot be found, when the operator runs the challenge script, then the exit code is 2, the message includes exactly 1 installation pointer (FR-012, ERR-003), with 0 reports written.
- **AC-015:**
  - (6×) > line 65: - **AC-015**: Given a round's output fails validation on both the initial call plus the corrective retry, when the second failure occurs, then the exit code is 3, the raw output is saved into the debug dump directory (FR-030, ERR-004), with 0 reports written.
- **AC-017:**
  - (5×) > line 67: - **AC-017**: Given a model call exceeds its timeout budget of at most 300 seconds by default, when the timeout expires, then the call is classified as a parse failure (FR-011): exactly 1 retry is issued, with a second failure ending the run at exit code 3 (FR-030).
- **AC-018:**
  - (2×) > line 68: - **AC-018**: Given round-2 answers with a missing, duplicate, or unknown question identifier, when validation runs, then the output is classified as a parse failure (FR-025) consuming exactly 1 corrective retry per FR-028.
- **AC-019:**
  - (7×) > line 69: - **AC-019**: Given the specification's directory is not writable, when the operator runs the challenge script, then the exit code is 1 with exactly 0 model calls launched (FR-006, ERR-002).
- **AC-020:**
  - (6×) > line 70: - **AC-020**: Given valid round-1 output holding more than N questions, when validation runs, then exactly N questions remain — the first N in returned order (FR-019) — with the report header carrying 1 truncation note (FR-036).
- **AC-021:**
  - (5×) > line 84: - **AC-021**: Given a stub executable configured as the model command, when a full challenge run executes, then the run completes end-to-end (FR-043, FR-003) using exactly 0 live model calls.
- **AC-022:**
  - (3×) > line 85: - **AC-022**: Given the repository's automated test suite, when the challenge script's unit tests run, then all tests pass with exactly 0 network calls plus exactly 0 live model commands installed (FR-044, SC-002).
- **AC-023:**
  - (4×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **ERR-002:**
  - (4×) > line 235: - **ERR-002**: When the specification's directory is not writable, the challenge script rejects the run with exit code 1, launching exactly 0 model calls (FR-006, AC-019).
- **ERR-005:**
  - (4×) > line 238: - **ERR-005**: When a model call exceeds its timeout, the challenge script recovers through the parse-failure path: exactly 1 retry, then exit code 3 on a second failure (FR-011, AC-017).
- **FR-001:**
  - (5×) > line 111: - **FR-001**: When invoked, the challenge script MUST accept exactly 1 positional argument: the path of the specification file to challenge (FR-005, FR-042).
- **FR-002:**
  - (7×) > line 113: - **FR-002**: When invoked, the challenge script MUST accept a question-count option, defaulting to exactly 15, that caps round-1 questions (FR-015, FR-019). Non-numeric, non-integer, and below-1 values MUST be rejected on the exit-code-1 argument path, mirroring FR-004. No upper bound is enforced: the requested maximum passes to the round-1 instruction as-is, and the effective question count is bounded by the model's returned output, to which FR-019 truncation and NFR-001's local-processing allowance apply.
- **FR-003:**
  - (3×) > line 115: - **FR-003**: When invoked, the challenge script MUST accept a model-command option, defaulting to `claude`, that names exactly 1 challenge model command line (FR-007, FR-043).
- **FR-004:**
  - (7×) > line 117: - **FR-004**: When invoked, the challenge script MUST accept a timeout option, defaulting to exactly 300 seconds, that bounds each model call (FR-011, FR-013). Non-numeric, non-finite, zero, or negative values MUST be rejected on the exit-code-1 argument path.
- **FR-006:**
  - (6×) > line 121: - **FR-006**: If the specification's directory is not writable for the report, the challenge script MUST exit with code 1 after launching exactly 0 model calls (ERR-002, AC-019).
  - (1×) > line 98: - The specification's directory is read-only → detected before any model call, exit code 1 (AC-019, FR-006).
- **FR-007:**
  - (5×) > line 123: - **FR-007**: When parsing the model-command option, the challenge script MUST split the value into words per POSIX shell quoting conventions (`shlex` semantics, platform-independent), treating exactly 1 leading word as the executable checked by FR-012 (FR-003).
- **FR-008:**
  - (5×) > line 130: - **FR-008**: When a challenge run executes, the challenge script MUST perform exactly 2 logical model calls: round-1 question generation (FR-014) plus round-2 answering (FR-021) — except exactly 1 when valid round-1 output contains 0 questions, in which case FR-020 governs and round 2 never launches.
- **FR-009:**
  - (4×) > line 132: - **FR-009**: When filtering, ranking, or rendering after round 2, the challenge script MUST perform exactly 0 further model calls (FR-032, FR-040).
- **FR-011:**
  - (4×) > line 136: - **FR-011**: If a model subprocess exceeds its timeout budget (default 300 seconds, FR-004), the challenge script MUST end that call, classifying it as a parse failure routed to FR-028 (ERR-005).
- **FR-012:**
  - (5×) > line 138: - **FR-012**: If the model-command executable named by FR-007 cannot be found, the challenge script MUST exit with code 2, printing exactly 1 message that contains an installation pointer — defined testably as exactly 1 URL occurrence in the message (ERR-003, AC-014).
- **FR-016:**
  - (9×) > line 151: - **FR-016**: When round-1 output is received, the challenge script MUST validate that each question carries exactly 1 unique identifier, exactly 1 question text, exactly 1 target — a requirement identifier or `general` — a list of integer line references, plus exactly 1 category from FR-015 (FR-017). Round-1 line references are accepted as any integers, are not range-checked, and carry no downstream normative role in v1 (they are stripped from the round-2 prompt and never rendered); they are retained in the schema for later tiers.
- **FR-017:**
  - (4×) > line 153: - **FR-017**: If round-1 validation fails, including on duplicate question identifiers, the challenge script MUST classify the output as a parse failure consuming 1 of the round's 2 permitted attempts (FR-016, FR-028).
  - (2×) > line 95: - Round 1 returns duplicate question identifiers → validation failure, corrective retry path (FR-017).
- **FR-018:**
  - (7×) > line 155: - **FR-018**: When embedding the specification in either prompt, the challenge script MUST prefix every line with its line number, starting at exactly 1, making each cited reference checkable per FR-039 (FR-014, FR-021).
- **FR-019:**
  - (4×) > line 157: - **FR-019**: If valid round-1 output contains more than N questions (FR-002, default 15), the challenge script MUST keep only the first N questions in returned order, recording a truncation note in the report header (FR-036). The kept N questions define both the round-2 prompt and the FR-025 identifier set.
  - (2×) > line 70: - **AC-020**: Given valid round-1 output holding more than N questions, when validation runs, then exactly N questions remain — the first N in returned order (FR-019) — with the report header carrying 1 truncation note (FR-036).
- **FR-022:**
  - (2×) > line 168: - **FR-022**: The round-2 prompt MUST NOT contain round-1 categories, targets, line references, or round-1 reasoning — exactly 0 of these 4 elements may appear (FR-021, AC-011).
- **FR-023:**
  - (5×) > line 170: - **FR-023**: The round-2 instruction MUST direct the model to answer each question using only the specification text, assigning exactly 1 verdict per question from the 3-value set ANSWERED, UNANSWERABLE, CONTRADICTED (FR-024, FR-032).
- **FR-024:**
  - (4×) > line 172: - **FR-024**: When round-2 output is received, the challenge script MUST validate that each answer carries exactly 1 question identifier, exactly 1 verdict from FR-023, exactly 1 answer text, plus a list of integer evidence line references (FR-025).
- **FR-026:**
  - (8×) > line 181: - **FR-026**: When raw model output is received, the challenge script MUST extract exactly 1 JSON object from it, tolerating surrounding non-JSON text plus code fences (FR-016, FR-024). When more than 1 candidate object is extractable, the first extractable object wins, in this precedence: whole-output parse, first fenced block, first balanced-brace candidate. A candidate that parses to a non-object JSON value (for example a bare array) is a parse failure at that precedence level, not a fall-through. A fenced block is any triple-backtick fence regardless of language tag; the balanced-brace scan is string-literal-aware (brace characters inside JSON string literals never affect the balance count).
- **FR-027:**
  - (2×) > line 183: - **FR-027**: If exactly 0 JSON objects can be extracted from raw model output, the challenge script MUST classify that output as a parse failure routed to FR-028 (FR-026).
- **FR-028:**
  - (4×) > line 185: - **FR-028**: On the first parse failure in a round, the challenge script MUST issue exactly 1 corrective retry: the same prompt plus an appended corrective instruction naming the validation failure, echoing 0 lines of the prior output (FR-013, FR-030).
- **FR-029:**
  - (4×) > line 187: - **FR-029**: When the first failure in a round was a timeout (FR-011), the corrective retry MUST re-issue the same prompt with exactly 0 appended corrective text (FR-028).
- **FR-030:**
  - (8×) > line 189: - **FR-030**: On the second parse failure in the same round, the challenge script MUST exit with code 3 after saving the raw output of the failing calls into exactly 1 directory named `.sue-debug` beside the specification (ERR-004, AC-015). For a timed-out call, the saved raw output is whatever partial output was drained within the shutdown grace period of exactly 5 seconds (subprocess ended by process-group kill; the up-to-4 grace periods are counted inside NFR-001's +60-second allowance), possibly empty. The debug dump itself is best-effort: if it cannot be written, the exit-3 outcome stands and its single diagnostic line names the failed dump (governs over ERR-004's save wording). Dump lifecycle: both failing attempts of the round are saved as separate files (`round{N}-attempt{1,2}-stdout/stderr`); successive failing runs overwrite files of the same names and the directory is never cleared by the script.
  - (2×) > line 65: - **AC-015**: Given a round's output fails validation on both the initial call plus the corrective retry, when the second failure occurs, then the exit code is 3, the raw output is saved into the debug dump directory (FR-030, ERR-004), with 0 reports written.
- **FR-032:**
  - (5×) > line 198: - **FR-032**: When all answers are validated, the challenge script MUST partition them into exactly 2 groups: findings holding verdicts CONTRADICTED plus UNANSWERABLE (FR-033), audit entries holding verdict ANSWERED (FR-038).
- **FR-033:**
  - (2×) > line 200: - **FR-033**: When ranking findings, the challenge script MUST place all CONTRADICTED findings before all UNANSWERABLE findings, preserving round-1 question order within each of the 2 classes (FR-032, AC-004).
- **FR-034:**
  - (8×) > line 202: - **FR-034**: When a run succeeds, the challenge script MUST write exactly 1 report file named `socratic-challenge.md` in the specification's directory, replacing any previous report while keeping 0 historical copies (FR-035, AC-003). If the report path resolves to the challenged specification file itself, the run MUST reject with exit code 1 before any model call — FR-042 takes precedence and the challenged file is never written. If writing the report fails after a successful run, the run exits with code 1.
- **FR-036:**
  - (6×) > line 206: - **FR-036**: The report header MUST state exactly 4 base facts — specification path, run date, question count, finding count — plus the FR-019 truncation note when truncation occurred (AC-002). The run date is the ISO calendar date `YYYY-MM-DD` in the operator's local timezone, rendered as the single `**Run date:**` header bullet; NFR-004's byte-identical comparison excludes exactly that line.
- **FR-037:**
  - (2×) > line 208: - **FR-037**: Each findings entry MUST state exactly 4 elements: the verdict, the question, the target requirement identifier, plus the evidence rendered per FR-039 (FR-033).
- **FR-038:**
  - (4×) > line 210: - **FR-038**: The audit appendix MUST list every ANSWERED question with its answering lines, rendered as exactly 1 collapsed section the reader can expand (AC-008, FR-032).
- **FR-039:**
  - (3×) > line 212: - **FR-039**: For each cited evidence line number, the report MUST quote exactly 1 line of text from the specification file, stating the named gap from the answer text for UNANSWERABLE findings — satisfied by rendering the FR-024 answer text verbatim; no separate gap field exists in the answer schema (FR-018, AC-009). A cited line number outside the specification's line range MUST render as a deterministic `(not present in the specification)` marker rather than failing the run.
- **FR-040:**
  - (7×) > line 214: - **FR-040**: After writing the report, the challenge script MUST print a terminal summary stating the finding count per verdict class plus the top 3 findings in rank order, then exit with code 0 (AC-005, FR-034). Exit code 0 signals run success only, never the specification's verdict — a deliberate interface boundary recorded under Limitations.
- **FR-041:**
  - (7×) > line 216: - **FR-041**: When every verdict is ANSWERED, the report's findings section MUST state that exactly 0 findings were produced, with the audit appendix holding all questions plus exit code 0 (AC-007, FR-038).
- **FR-042:**
  - (4×) > line 218: - **FR-042**: Across every outcome, the challenge script MUST NOT modify the challenged specification file — exactly 0 writes to it occur (AC-010, FR-001).
- **FR-043:**
  - (6×) > line 225: - **FR-043**: When the model-command option names any operator-supplied command line, the challenge script MUST execute that command in place of the default, enabling stubbed runs with exactly 0 live model calls (AC-021, FR-003).
  - (2×) > line 84: - **AC-021**: Given a stub executable configured as the model command, when a full challenge run executes, then the run completes end-to-end (FR-043, FR-003) using exactly 0 live model calls.
- **FR-044:**
  - (3×) > line 227: - **FR-044**: The deliverable MUST include automated unit tests covering all 7 deterministic behavior groups — argument handling, prompt assembly, extraction, validation with identifier bijection, filtering plus ranking, report rendering, exit codes — runnable with exactly 0 live model access (AC-022, FR-043).
- **NFR-001:**
  - (4×) > line 242: - **NFR-001**: When a challenge run terminates on any path, its wall-clock duration MUST be at most 4 timeout budgets (FR-004, FR-013) plus 60 seconds of local processing.
- **NFR-003:**
  - (2×) > line 246: - **NFR-003**: The script's usage text MUST contain exactly 1 disclosure stating that challenged specification content is sent to the model provider via the model command (FR-003, FR-043).
- **NFR-004:**
  - (2×) > line 248: - **NFR-004**: When identical validated answers are assembled twice, the challenge script MUST produce 2 report bodies identical outside the run-date field (FR-032, FR-036).
  - (2×) > line 249:   - **Category:** Reliability | **Measurable Target:** byte-identical report bodies across 2 assembly passes, run-date field excluded
- **OQ-001:**
  - (4×) > line 305: | OQ-001 | How exactly is the prompt delivered to the model command, and with which output flags? Decides extraction design details and the stub replay contract. | FR-026, FR-028 implementation freeze; stub fixture design | unknowns.md U-001 (should-resolve-before-HOW) |
- **OQ-002:**
  - (4×) > line 306: | OQ-002 | Does a neutral temporary working directory fully satisfy the isolation intent, or does operator-level ambient context still load? | FR-010 limitation wording; possible suppression options | unknowns.md U-002 (should-resolve-before-HOW) |
- **REQ-002:**
  - (3×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **REQ-009:**
  - (3×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **REQ-017:**
  - (2×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **REQ-019:**
  - (2×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **REQ-028:**
  - (2×) > line 86: - **AC-023**: Given exactly 1 manual live acceptance run against the designated acceptance target (`specs/029-builder-spec-workbench/spec.md`), when the run completes, then a report exists whose findings overlap at least 1 of the 3 named known issues — (1) the REQ-009 "time order" vs AC-010 "most recent first" ordering conflict, (2) the REQ-017/REQ-019/REQ-028 quality-score recording loop, (3) the undefined active-run pointer of REQ-002 — within at most 3 total attempts (SC-001, FR-034).
- **SC-001:**
  - (3×) > line 268: - **SC-001**: When the manual live acceptance run executes against the designated acceptance target, a report is generated whose findings overlap at least 1 of the 3 named known issues, within at most 3 total attempts (AC-023, FR-034).
- **SC-002:**
  - (3×) > line 269: - **SC-002**: When the challenge script's unit tests run in the repository's test runner, all tests pass with exactly 0 network calls plus exactly 0 live model invocations (FR-044, AC-022).
- **SC-003:**
  - (3×) > line 270: - **SC-003**: When each failure class is deliberately triggered, it reproduces its assigned exit code — 1, 2, or 3 — plus exactly 1 diagnostic line (NFR-005, ERR-001).
- **SC-004:**
  - (5×) > line 271: - **SC-004**: When any challenge run terminates, its wall-clock time is at most 4 timeout budgets plus 60 seconds (NFR-001, FR-011).
- **SC-005:**
  - (4×) > line 272: - **SC-005**: When an operator on a fresh repository checkout runs a stubbed challenge, the run completes with exactly 1 command invocation plus 0 additional installed components (NFR-002, FR-043).
- **U-001:**
  - (2×) > line 305: | OQ-001 | How exactly is the prompt delivered to the model command, and with which output flags? Decides extraction design details and the stub replay contract. | FR-026, FR-028 implementation freeze; stub fixture design | unknowns.md U-001 (should-resolve-before-HOW) |
- **U-002:**
  - (2×) > line 306: | OQ-002 | Does a neutral temporary working directory fully satisfy the isolation intent, or does operator-level ambient context still load? | FR-010 limitation wording; possible suppression options | unknowns.md U-002 (should-resolve-before-HOW) |
- **U-003:**
  - (4×) > line 312: | U-003 retry prompt content | Corrective instruction names the validation failure, prior output not echoed; timeout retry is a plain re-issue | FR-028, FR-029 |
- **U-004:**
  - (2×) > line 313: | U-004 model command semantics | Command line split by shell quoting conventions; word 1 is the availability-checked executable | FR-007 |
- **U-005:**
  - (6×) > line 314: | U-005 degenerate outcomes | Empty question list → zero-question report, exit 0; all ANSWERED → clean report, exit 0; unwritable directory → pre-flight exit 1 | FR-006, FR-020, FR-041 |
- **U-006:**
  - (4×) > line 315: | U-006 line-number provenance | Specification presented with explicit 1-based line numbers; report quotes actual file lines by number | FR-018, FR-039 |
- **U-007:**
  - (2×) > line 140: - **FR-013**: When a corrective retry launches under FR-028, the challenge script MUST grant that retry exactly 1 fresh timeout budget equal to the FR-004 value (NFR-001). A model call that exits with non-zero status, or produces empty stdout, is classified as a failed call on the parse-failure path before any extraction — its output is never consumed even when it would parse (U-007).
  - (2×) > line 316: | U-007 exit-2 boundary | Exit 2 only for executable-not-found; every other launch or output failure takes the parse-failure path | FR-012, FR-030 |
- **U-008:**
  - (4×) > line 317: | U-008 over-cap and duplicates | Over-cap → deterministic truncation with report note; duplicate round-1 identifiers → parse failure | FR-019, FR-017 |
- **U-009:**
  - (2×) > line 296: - **Prompt exposure of challenged text**: The challenged specification is embedded verbatim in both prompts. A specification containing adversarial instructions could steer verdicts; the human reviewer of the report is the backstop (U-009).
  - (2×) > line 318: | U-009 adversarial specification text | Stated limitation; human reviewer is the backstop | Limitations |
- **U-010:**
  - (4×) > line 319: | U-010 report write semantics | Plain overwrite of the report file; no atomicity or concurrency guarantee claimed in v1 | FR-034 |
