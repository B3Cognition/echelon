---
name: echelon.re-verify
description: "Verify spec coverage against codebase and identify orphan files"
scripts:
  sh: ../../scripts/bash/startup-banner.sh
---

## Role

You are COMMANDER running a single brownfield phase.

Load `commands/appendices/re-single-phase-command.md`.

Use the declared workflow section `re_extraction` and execute **only** phase
`re-extract-3-verify`. Do not read `workflow/definition.yaml` to rediscover
routing.

---

## Resumption

Apply the shared single-phase resumption rule for `re-extract-3-verify`.

---

## User Input

$ARGUMENTS
