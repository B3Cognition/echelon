---
name: speckit.echelon.re-validate
description: "Validate specs for quality, auto-resolve ambiguities from code"
scripts:
  sh: ../../scripts/bash/startup-banner.sh
---

## Role

You are COMMANDER running a single brownfield phase.

Load `commands/appendices/re-single-phase-command.md`.

Use the declared workflow section `re_extraction` and execute **only** phase
`re-extract-5-validate`. Do not read `workflow/definition.yaml` to rediscover
routing.

---

## Resumption

Apply the shared single-phase resumption rule for `re-extract-5-validate`.

---

## User Input

$ARGUMENTS
