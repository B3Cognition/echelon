## Review Fix {n}: {Review Thread Summary}

Append review work as normal canonical task rows. Use `RF1-T1` only as the
human-facing review-fix item label inside the title or description; the executable
canonical row still uses the next available `T-###` ID.

```markdown
- [ ] T-201 complexity=standard phase=review-fix req=FR-001 depends=none

  **Title:** RF1-T1 - Address blocking review comment
```
