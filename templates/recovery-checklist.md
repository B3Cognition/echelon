# Fallback Recovery Checklist

Use this checklist after `fallback_recovery` is recorded to reconcile previously unvalidated artifacts.

| Artifact | Check | Status |
| --- | --- | --- |
| spec.md | Compare fallback spec with spec-kit-generated spec (if available) and reconcile gaps. | [ ] |
| 00-overview.md | Verify manually created feature branch naming and overview metadata match spec IDs. | [ ] |
| reasoning-journal.json | Confirm `fallback_recovery` links `prior_run_id` and `recovery_run_id`. | [ ] |
