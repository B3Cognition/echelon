## Fulfillment Gap Tasks

Append fulfillment-gap work as normal canonical task rows. Use `FG-T1` only as
the human-facing gap item label inside the title or description; the executable
canonical row still uses the next available `T-###` ID.

```markdown
- [ ] T-301 complexity=standard phase=fulfillment-gap req=FR-001 depends=none

  **Title:** FG-T1 - Implement missing fulfillment evidence for FR-001
```
